// In DynamicViewFields.jsx, update the file field rendering:
case 'file':
  if (field.disabled || field.displayOnly) {
    const value = watch(field.name);
    const hasFile = value && (value.fileName || value.downLoadFile);
    
    return (
      <div className="file-field">
        <label className="field-label">{field.label}</label>
        <div className={`file-display ${field.colSize === 12 ? 'file-display-large' : ''}`}>
          {!hasFile ? (
            <div className="no-file">
              <BsFileEarmarkX size={14} className="no-file-icon" />
              <span>No document uploaded</span>
            </div>
          ) : (
            <div className="file-item">
              <div className="file-info">
                <FaFileAlt size={14} className="file-icon" />
                <span className="file-name">
                  {value.fileName || `${field.label.replace(/[^a-zA-Z0-9]/g, '_')}.pdf`}
                </span>
              </div>
              <div className="file-actions">
                <Button 
                  variant="outline-primary" 
                  size="sm" 
                  onClick={() => handleView(value)}
                  className="file-btn view-btn"
                  title="View Document"
                >
                  <FaEye size={10} />
                  View
                </Button>
                <Button 
                  variant="outline-success" 
                  size="sm" 
                  onClick={() => handleDownload(value)}
                  className="file-btn download-btn"
                  title="Download Document"
                >
                  <FaDownload size={10} />
                  Download
                </Button>
              </div>
            </div>
          )}
        </div>
      </div>
    );
  }
