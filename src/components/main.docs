import React, { useState, useRef, useEffect, useCallback, useMemo } from "react";
import { useNavigate, useParams } from "react-router-dom";
import { toast } from "react-toastify";
import { Modal, Button, Form } from "react-bootstrap";
import "./Stepper.css";
import { useRegistration } from "../../context/ApplicationContext";
import ReviewStep from "./ReviewStep";
import { FormDataProvider } from "../../context/FormDataContext";
import { postData } from "../../../services/http.service.ts";
import { URLS_ENDPOINT } from "../../../utils/urlsConstants.ts";
import axios from "axios";
import { v4 as uuidv4 } from "uuid"; // Import UUID generator


const deepEqual = (obj1, obj2) => {
  return JSON.stringify(obj1) === JSON.stringify(obj2);
};

const generateFileId = (file) => {
  return `file-${Date.now()}-${Math.random().toString(36).substring(2, 9)}`;
};
// const  idetificationType= localStorage.getItem("selectedIdentificationType");
// const addressproofType = localStorage.getItem("selectedAddressProofType");
const extractFileDetails = (file, source,documentType) => {
  console.log("extractFileDetails", file, source);
  if (!file) return null;

  return {
    docId: file.fileId ,
    docName: file.docName,
    docType: documentType, 
    docSource: source,
  };
};

const createFormPayload = (formData, isFinalSubmit, userFormData, applicationNumber = "",isActive) => {
  console.log("createFormPayload", formData);
  const creditorBasicDetailsUuid = formData.creditorBasicDetailsUuid || "";
  const claimDetailsUuid = formData.claimDetailsUuid || "";
  const signatoryDetailsUuid = formData.signatoryDetailsUuid || "";

  return {
    // userInfo: userFormData?.fullloginDetails || {}, // Make sure to handle potential null
    uuid: applicationNumber || "", // Application number is generated on final submit
    applicationNumber: applicationNumber || "",
    creditorBasicDetails: {
      uuid: creditorBasicDetailsUuid,
      debtorName: formData.step1?.debtorName || "",
      creditorName: formData.step1?.CreditorName || "", 
      totalClaimAmount: parseFloat(formData.step1?.totalClaimAmount) || 0,
      debtDetails: formData.step1?.debtDetails || "",
      mutualCreditDetails: formData.step1?.mutualCreditDetails || "",
      status: "",
      // status : formData.status || "",
      isActive: isActive,
      isFinalSubmit: isFinalSubmit,
      identificationNumber: formData.step1?.identificationNumber || "",
      document: extractFileDetails(formData.step1?.identificationDocument, "Identification",formData.step1?.identificationType),
    },
    // auditDetails: {
    //   auditDetails: {
    //     // createdBy: userFormData?.fullloginDetails?.uuid || "", 
    //     // lastModifiedBy: userFormData?.fullloginDetails?.uuid || "",
    //     // createdTime: Date.now(),
    //     // lastModifiedTime: Date.now(),
    //   },
    // },
    claimDetails: {
      uuid: claimDetailsUuid,
      securityInterestAmount: parseFloat(formData.step2?.securityInterestAmount) || 0,
      guaranteeAmount: parseFloat(formData.step2?.guaranteeAmount) || 0,
      guarantorName: formData.step2?.guarantorName || "",
      guarantorAddress: formData.step2?.guarantorAddress || "",
      creditorAddress: formData.step2?.creditorAddress || "",
      creditorEmail: formData.step2?.creditorEmail || "",
      beneficiaryName: formData.step2?.beneficiaryName || "",
      beneficiaryAddress: formData.step2?.beneficiaryAddress || "",
      corporateDebtorSecurityAmount: parseFloat(formData.step2?.corporateDebtorSecurityAmount) || 0,
      corporateDebtorGuaranteeAmount: parseFloat(formData.step2?.corporateDebtorGuaranteeAmount) || 0,
      principalBorrowerName: formData.step2?.principalBorrowerName || "",
      principalBorrowerAddress: formData.step2?.principalBorrowerAddress || "",
      bankAccountNumber: formData.step2?.accountNumber || "",
      bankIfscCode: formData.step2?.ifscCode || "",
      bankBranchName: formData.step2?.branchName || "",
      addressProofNumber: formData.step2?.addressProofNumber || "",
      document: extractFileDetails(formData.step2?.addressProofDocument, "Addressproof",formData.step2?.addressProofType),
      // auditDetails: {},
    },
    signatoryDetails: {
      uuid: signatoryDetailsUuid,
      name: formData.step3?.signatoryName || "",
      position: formData.step3?.signatoryPosition || "",
      address: formData.step3?.signatoryAddress || "",
      document: extractFileDetails(formData.step3?.signatoryDocument, "Signature","Signature"),
    },
    status : "",
    isActive: isActive,
    isFinalSubmit:isFinalSubmit

  };
};

const getFileFieldsForStep = (stepNumber) => {
  switch (stepNumber) {
    case 1:
      return ["identificationDocument"];
    case 2:
      return ["addressProof"];
    case 3:
      return ["signatoryDocument"];
    default:
      return [];
  }
};

const processFileUploads = async (stepData, stepNumber) => {
  const fileFields = getFileFieldsForStep(stepNumber);
  const processedData = { ...stepData };

  for (const field of fileFields) {
    if (stepData[field] && stepData[field] instanceof File) {
      // In a real application, you would upload the file here
      // and get a server-side ID or URL.
      processedData[field] = {
        id: generateFileId(stepData[field]),
        name: stepData[field].name,
        size: stepData[field].size,
        type: stepData[field].type,
        uploadedAt: new Date().toISOString(),
      };
    } else if (stepData[field] && typeof stepData[field] === 'object' && stepData[field].name) {
      // Keep existing file details if not a new file
      processedData[field] = { ...stepData[field] };
    }
  }

  return processedData;
};

const Stepper = ({ stepsConfig = [], formAllData }) => {
  const { id } = useParams();
  const isEditMode = !!id;
  const [currentStep, setCurrentStep] = useState(1);
  const [isConfirmed, setIsConfirmed] = useState(false);
  const [isComplete, setIsComplete] = useState(false);
  const [isViewModalOpen, setIsViewModalOpen] = useState(false);
  const [hasPreviewed, setHasPreviewed] = useState(false);
  const [formData, setFormData] = useState({});
  const [margins, setMargins] = useState({ marginLeft: 0, marginRight: 0 });
  const stepRef = useRef([]);
  const navigate = useNavigate();
  const [currentSubmitHandler, setCurrentSubmitHandler] = useState(null);
  const [showModal, setShowModal] = useState(false);
  const [comments, setComments] = useState("");
  const [actionType, setActionType] = useState("");
  const [isSavingDraft, setIsSavingDraft] = useState(false);
  const[draftResponse, setDraftResponse] = useState(null);
  const [isSubmittingAction, setIsSubmittingAction] = useState(false);

  const {
    registrationData,
    handleStepDataFetch,
    updateRegistrationData,
    handleApplicatioNFormView,
    isDataLoaded,
  } = useRegistration();

  console.log("Registration Data:", registrationData);

  const userFormData = JSON.parse(localStorage.getItem("userFormData"));
  const isCRPUser = userFormData?.userType === "CRP";

  const filteredStepsConfig = useMemo(() => {
    if (isCRPUser) {
      return stepsConfig.filter((step) => step.name !== "Review & Submit");
    }
    return stepsConfig;
  }, [stepsConfig, isCRPUser]);

  useEffect(() => {
    if (id) {
      handleApplicatioNFormView({ uuid: id });
    }
  }, [id]);


  useEffect(() => {
    if (isEditMode && registrationData && !deepEqual(formData, registrationData)) {
      setFormData(registrationData);
    }
    else{
      setFormData({});
    }
  }, [isEditMode, registrationData]);

  // useEffect(() => {
  //   if (isEditMode && registrationData && !deepEqual(formData, registrationData)) {
  //     setFormData({
  //       ...registrationData,
  //       creditorBasicDetailsUuid: registrationData.creditorBasicDetails?.uuid || uuidv4(),
  //       claimDetailsUuid: registrationData.claimDetails?.uuid || uuidv4(),
  //       signatoryDetailsUuid: registrationData.signatoryDetails?.uuid || uuidv4(),
  //       step1: {
  //         debtorName: registrationData.creditorBasicDetails?.debtorName,
  //         CreditorName: registrationData.creditorBasicDetails?.creditorName,
  //         totalClaimAmount: registrationData.creditorBasicDetails?.totalClaimAmount,
  //         debtDetails: registrationData.creditorBasicDetails?.debtDetails,
  //         mutualCreditDetails: registrationData.creditorBasicDetails?.mutualCreditDetails,
  //         identificationType: registrationData.creditorBasicDetails?.document?.docType,
  //         identificationNumber: registrationData.creditorBasicDetails?.document?.docId,
  //         identificationDocument: registrationData.creditorBasicDetails?.document
  //           ? { id: registrationData.creditorBasicDetails.document.docId, name: registrationData.creditorBasicDetails.document.docName, type: registrationData.creditorBasicDetails.document.docType }
  //           : null,
  //       },
  //       step2: {
  //         securityInterestAmount: registrationData.claimDetails?.securityInterestAmount,
  //         guaranteeAmount: registrationData.claimDetails?.guaranteeAmount,
  //         guarantorName: registrationData.claimDetails?.guarantorName,
  //         guarantorAddress: registrationData.claimDetails?.guarantorAddress,
  //         creditorAddress: registrationData.claimDetails?.creditorAddress,
  //         creditorEmail: registrationData.claimDetails?.creditorEmail,
  //         beneficiaryName: registrationData.claimDetails?.beneficiaryName,
  //         beneficiaryAddress: registrationData.claimDetails?.beneficiaryAddress,
  //         corporateDebtorSecurityAmount: registrationData.claimDetails?.corporateDebtorSecurityAmount,
  //         corporateDebtorGuaranteeAmount: registrationData.claimDetails?.corporateDebtorGuaranteeAmount,
  //         principalBorrowerName: registrationData.claimDetails?.principalBorrowerName,
  //         principalBorrowerAddress: registrationData.claimDetails?.principalBorrowerAddress,
  //         accountNumber: registrationData.claimDetails?.bankAccountNumber,
  //         ifscCode: registrationData.claimDetails?.bankIfscCode,
  //         branchName: registrationData.claimDetails?.bankBranchName,
  //         addressProof: registrationData.claimDetails?.document
  //           ? { id: registrationData.claimDetails.document.docId, name: registrationData.claimDetails.document.docName, type: registrationData.claimDetails.document.docType }
  //           : null,
  //       },
  //       step3: {
  //         signatoryName: registrationData.signatoryDetails?.name,
  //         signatoryPosition: registrationData.signatoryDetails?.position,
  //         signatoryAddress: registrationData.signatoryDetails?.address,
  //         signatoryDocument: registrationData.signatoryDetails?.document
  //           ? { id: registrationData.signatoryDetails.document.docId, name: registrationData.signatoryDetails.document.docName, type: registrationData.signatoryDetails.document.docType }
  //           : null,
  //       },
  //     });
  //   } else if (!isEditMode && !isDataLoaded) {
  //     // Initialize with new UUIDs for a new form
  //     setFormData({
  //       creditorBasicDetailsUuid: uuidv4(),
  //       claimDetailsUuid: uuidv4(),
  //       signatoryDetailsUuid: uuidv4(),
  //     });
  //   }
  // }, [isEditMode, registrationData, formData, isDataLoaded]);

  useEffect(() => {
    const savedIsConfirmed = localStorage.getItem("isConfirmed");
    if (savedIsConfirmed) {
      setIsConfirmed(JSON.parse(savedIsConfirmed));
    }
  }, []);

  useEffect(() => {
    localStorage.setItem("isConfirmed", JSON.stringify(isConfirmed));
  }, [isConfirmed]);
  

  useEffect(() => {
    const newSubmitHandler = filteredStepsConfig[currentStep - 1]?.submitHandler;
    if (newSubmitHandler && newSubmitHandler !== currentSubmitHandler) {
      setCurrentSubmitHandler(() => newSubmitHandler);
    }
  }, [currentStep, filteredStepsConfig]);

  const updateSubmitHandler = useCallback((submitHandler) => {
    setCurrentSubmitHandler(() => submitHandler);
  }, []);

  useEffect(() => {
    if (stepRef.current.length > 0) {
      const firstStep = stepRef.current[0]?.offsetWidth || 0;
      const lastStep = stepRef.current[filteredStepsConfig.length - 1]?.offsetWidth || 0;
      setMargins({
        marginLeft: firstStep / 2,
        marginRight: lastStep / 2,
      });
    }
  }, [filteredStepsConfig.length]);

  const validateStepData = (step, data) => {
    const requiredFields = filteredStepsConfig[step - 1]?.requiredFields || [];
    for (let field of requiredFields) {
      if (!data[field]) {
        toast.error(`${field} is required.`);
        return false;
      }
    }
    return true;
  };



  const handleNext = async () => {
    if (!currentSubmitHandler) {
      console.error("No submit handler available.");
      return;
    }
  
    const currentStepData = await currentSubmitHandler();
    if (!currentStepData) {
      console.error("Form validation failed.");
      return;
    }
  
    if (!validateStepData(currentStep, currentStepData)) {
      return;
    }
  
    const processedStepData = await processFileUploads(currentStepData, currentStep);
  
    const updatedFormData = {
      ...formData,
      [`step${currentStep}`]: { ...formData[`step${currentStep}`], ...processedStepData },
    };
  
    if (!deepEqual(updatedFormData, formData)) {
      setFormData(updatedFormData);
      updateRegistrationData(`step${currentStep}`, updatedFormData[`step${currentStep}`], id);
    }
  
    if (currentStep === filteredStepsConfig.length) {
      try {
        setIsSavingDraft(true);
        
        // Get the draft response from state or localStorage
        const lastDraftResponse = draftResponse || JSON.parse(localStorage.getItem("draftResponse") || "null");
        
        if (!lastDraftResponse) {
          throw new Error("No draft data found. Please save a draft first.");
        }
  
        // Create final payload by modifying the draft response
        const finalSubmitPayload = {
          ...lastDraftResponse.data, // Use all data from draft response
          applicationDetails: {
            ...lastDraftResponse.data.applicationDetails,
            creditorBasicDetails: {
              ...lastDraftResponse.data.applicationDetails.creditorBasicDetails,
              isFinalSubmit: true, // Only change this flag
              // isActive: true
            },
            isFinalSubmit:true,
            // isActive:true
          }
        };
  
        console.log("Final Submit Payload:", finalSubmitPayload);
  
        const REQUEST = `${process.env.REACT_APP_APPLICATION_SERVICE}${URLS_ENDPOINT.APP_SERVICES.APPLICATION_FORMSUBMIT}`;
        const response = await postData(REQUEST, finalSubmitPayload);
        if (response.data.status.code === "200") {
          // Clean up draft data
          localStorage.removeItem("draftResponse");
          setDraftResponse(null);
          
          setIsComplete(true);
          toast.success("Form submitted successfully");
          navigate("/dashboard");
          
          // Navigate to tracking page with application data
          // navigate("/tracking", {
          //   state: {
          //     applicationId: response.data.applicationDetails.applicationNumber,
          //     formData: updatedFormData
          //   }
          // });
          
          // Also store in localStorage as fallback
          // localStorage.setItem("lastSubmittedApplication", response.data.applicationDetails.applicationNumber);
          // localStorage.setItem("lastSubmittedFormData", JSON.stringify(updatedFormData));
        } else {
          toast.error(`Failed to submit form: ${response.data.status.message}`);
        }
  
        // if (response.data.status.code === "200") {
        //   // Clean up draft data
        //   localStorage.removeItem("draftResponse");
        //   setDraftResponse(null);
          
        //   setIsComplete(true);
        //   toast.success("Form submitted successfully");
        //   navigate("/dashboard");
        // } else {
        //   toast.error(`Failed to submit form: ${response.data.status.message}`);
        // }
      } catch (error) {
        console.error("Error submitting form:", error);
        toast.error(error.message || "Failed to submit form");
      } finally {
        setIsSavingDraft(false);
      }
    } else {
      setCurrentStep((prevStep) => prevStep + 1);
    }
  };
  const handlePreview = async () => {
    if (!currentSubmitHandler) {
      console.error("No submit handler available.");
      return;
    }

    const currentStepData = await currentSubmitHandler();
    if (!currentStepData) {
      console.error("Form validation failed.");
      return;
    }

    if (!validateStepData(currentStep, currentStepData)) {
      return;
    }

    const processedStepData = await processFileUploads(currentStepData, currentStep);

    const updatedFormData = {
      ...formData,
      [`step${currentStep}`]: { ...formData[`step${currentStep}`], ...processedStepData },
    };

    if (!deepEqual(updatedFormData, formData)) {
      setFormData(updatedFormData);
      updateRegistrationData(`step${currentStep}`, updatedFormData[`step${currentStep}`], id);
    }
    // console.log("Updated Form Data:", updatedFormData);

    // const previewPayload = createFormPayload(updatedFormData, false, userFormData, id); // isFinalSubmit: false, use ID if editing

    // console.log("Preview Payload:", JSON.stringify(previewPayload, null, 2));
    // localStorage.setItem("formPreviewData", JSON.stringify(previewPayload));

    setCurrentStep(filteredStepsConfig.length);
    setHasPreviewed(true); // Enable preview button
  };
  // new save as draft logic

  // const handleSaveDraft = async () => {
  //   setIsSavingDraft(true);
  //   try {
  //     const currentStepData = currentSubmitHandler ? await currentSubmitHandler() : {};
  //     const processedStepData = await processFileUploads(currentStepData, currentStep);
  
  //     // const draftId = id || `draft-${Date.now()}-${Math.random().toString(36).substring(2, 9)}`;
  //     const draftId="";
  
      
  //     const creditorBasicDetailsUuid = formData.creditorBasicDetailsUuid || "";
  //     const claimDetailsUuid = formData.claimDetailsUuid || "";
  //     const signatoryDetailsUuid = formData.signatoryDetailsUuid || "";
  
  //     const draftData = {
  //       ...formData,
  //       creditorBasicDetailsUuid,
  //       claimDetailsUuid,
  //       signatoryDetailsUuid,
  //       [`step${currentStep}`]: {
  //         ...formData[`step${currentStep}`],
  //         ...processedStepData,
  //       },
  //       id: draftId,
  //       status: "draft",
  //       lastSaved: new Date().toISOString(),
  //       currentStep,
  //       formType: userFormData?.formType,
  //       userType: userFormData?.userType,
  //     };
  
  //     const draftPayload = {
  //       status:{
  //         code :"",
  //         message :""
  //       },
  //       userInfo: userFormData?.fullloginDetails || {}, 
  //       applicationDetails:createFormPayload(draftData, false, userFormData, draftId,false),
  //     }
  
  //     // const existingDraft = JSON.parse(localStorage.getItem("formDrafts") || "[]");
  //     // const updatedDrafts = [
  //     //   ...existingDraft.filter((draft) => draft.id !== draftId),
  //     //   draftData,
  //     // ];
  //     // localStorage.setItem("formDrafts", JSON.stringify(updatedDrafts));
  //     console.log(" Drafts payload:", draftPayload);
  
  //     // Save the draft data to local storage
  //     localStorage.setItem(draftId, JSON.stringify(draftPayload));

  //     // api call to save draft

  //     const REQUEST = `${process.env.REACT_APP_APPLICATION_SERVICE}${URLS_ENDPOINT.APP_SERVICES.APPLICATION_FORMSUBMIT}`;
  //    const response = await postData(REQUEST, draftPayload);
  //    console.log("response", response);


  // ;
  //    if(response.data.status.code === "200") {

  //     localStorage.setItem("draftResponse", JSON.stringify(response));
  //     setDraftResponse(response); // Store the last draft response in state
  //      setHasPreviewed(true); // Enable preview after saving draft

      
  //     // //update form data with the response data if needed
  //     // const updatedFormData = { ...formData, 
  //     //   creditorBasicDetailsUuid: response.data.applicationDetails.creditorBasicDetails.uuid,
  //     //   claimDetailsUuid: response.data.applicationDetails.claimDetails.uuid,
  //     //   signatoryDetailsUuid: response.data.applicationDetails.signatoryDetails.uuid,
  //     //   applicationNumber: response.data.applicationDetails.applicationNumber,
  //     //    };
  //     setFormData(prev=> ({ ...prev, 
  //       creditorBasicDetailsUuid: response.data.applicationDetails.creditorBasicDetails.uuid,
  //       claimDetailsUuid: response.data.applicationDetails.claimDetails.uuid,
  //       signatoryDetailsUuid: response.data.applicationDetails.signatoryDetails.uuid,
  //       applicationNumber: response.data.applicationDetails.applicationNumber
  //      }));

  //   toast.success("Draft saved successfully");
  //     } else {
  //       toast.error(`Failed to save draft: ${response.status.message}`);
  //     }
  //   } catch (error) {
  //     console.error("Error saving draft:", error);
  //     toast.error("Failed to save draft");
  //   } finally {
  //     setIsSavingDraft(false);
  //   }
  // };
  







  const handleSaveDraft = async () => {
    setIsSavingDraft(true);
    try {
      const currentStepData = currentSubmitHandler ? await currentSubmitHandler() : {};
      const processedStepData = await processFileUploads(currentStepData, currentStep);
  
      // Get the last draft response from localStorage if available
      const lastDraftResponse = JSON.parse(localStorage.getItem("draftResponse") || "null");
      const draftId = lastDraftResponse?.data?.applicationDetails?.applicationNumber || "";
  
      // Prepare the draft data with existing UUIDs or generate new ones if not available
      const creditorBasicDetailsUuid = formData.creditorBasicDetailsUuid || "";
      const claimDetailsUuid = formData.claimDetailsUuid || "";
      const signatoryDetailsUuid = formData.signatoryDetailsUuid || "";
  
      const draftData = {
        ...formData,
        creditorBasicDetailsUuid,
        claimDetailsUuid,
        signatoryDetailsUuid,
        [`step${currentStep}`]: {
          ...formData[`step${currentStep}`],
          ...processedStepData,
        },
        id: draftId,
        status: "draft",
        lastSaved: new Date().toISOString(),
        currentStep,
        formType: userFormData?.formType,
        userType: userFormData?.userType,
      };
  
      // Create the draft payload - use last draft response data if available
      const draftPayload = lastDraftResponse 
        ? {
            ...lastDraftResponse.data,
            applicationDetails: {
              ...lastDraftResponse.data.applicationDetails,
              // Update with current form data
              creditorBasicDetails: {
                ...lastDraftResponse.data.applicationDetails.creditorBasicDetails,
                ...createFormPayload(draftData, false, userFormData, draftId, false).creditorBasicDetails
              },
              claimDetails: {
                ...lastDraftResponse.data.applicationDetails.claimDetails,
                ...createFormPayload(draftData, false, userFormData, draftId, false).claimDetails
              },
              signatoryDetails: {
                ...lastDraftResponse.data.applicationDetails.signatoryDetails,
                ...createFormPayload(draftData, false, userFormData, draftId, false).signatoryDetails
              }
            }
          }
        : {
            status: {
              code: "",
              message: ""
            },
            userInfo: userFormData?.fullloginDetails || {},
            applicationDetails: createFormPayload(draftData, false, userFormData, draftId, false),
          };
  
      console.log("Draft Payload:", draftPayload);
  
      // Make API call to save draft
      const REQUEST = `${process.env.REACT_APP_APPLICATION_SERVICE}${URLS_ENDPOINT.APP_SERVICES.APPLICATION_FORMSUBMIT}`;
      const response = await postData(REQUEST, draftPayload);
  
      if (response.data.status.code === "200") {
        // Save the response to localStorage for future updates
        localStorage.setItem("draftResponse", JSON.stringify(response));
        setDraftResponse(response); // Store in state
  
        // Update form data with the response data
        setFormData(prev => ({
          ...prev,
          creditorBasicDetailsUuid: response.data.applicationDetails.creditorBasicDetails.uuid,
          claimDetailsUuid: response.data.applicationDetails.claimDetails.uuid,
          signatoryDetailsUuid: response.data.applicationDetails.signatoryDetails.uuid,
          applicationNumber: response.data.applicationDetails.applicationNumber
        }));
  
        setHasPreviewed(true); // Enable preview after saving draft
        toast.success("Draft saved successfully");
      } else {
        toast.error(`Failed to save draft: ${response.data.status.message}`);
      }
    } catch (error) {
      console.error("Error saving draft:", error);
      toast.error("Failed to save draft");
    } finally {
      setIsSavingDraft(false);
    }
  };


// previous save as draft  (old)
  // const handleSaveDraft = async () => {
  //   setIsSavingDraft(true);
  //   try {
  //     const currentStepData = currentSubmitHandler ? await currentSubmitHandler() : {};
  //     const processedStepData = await processFileUploads(currentStepData, currentStep);

  //     const draftId = id || `draft-${Date.now()}-${Math.random().toString(36).substring(2, 9)}`;

  //     // Generate uuids if they don't exist
  //     const creditorBasicDetailsUuid = formData.creditorBasicDetailsUuid || uuidv4();
  //     const claimDetailsUuid = formData.claimDetailsUuid || uuidv4();
  //     const signatoryDetailsUuid = formData.signatoryDetailsUuid || uuidv4();

  //     const draftData = {
  //       ...formData,
  //       creditorBasicDetailsUuid: creditorBasicDetailsUuid,
  //       claimDetailsUuid: claimDetailsUuid,
  //       signatoryDetailsUuid: signatoryDetailsUuid,
  //       [`step${currentStep}`]: {
  //         ...formData[`step${currentStep}`],
  //         ...processedStepData,
  //       },
  //       id: draftId,
  //       status: "draft",
  //       lastSaved: new Date().toISOString(),
  //       currentStep,
  //       formType: userFormData?.formType,
  //       userType: userFormData?.userType,
  //     };

  //     const draftPayload = createFormPayload(draftData, false, userFormData, draftId);

  //     const existingDraft = JSON.parse(localStorage.getItem("formDrafts") || "[]");
  //     const updatedDrafts = [
  //       ...existingDraft.filter((draft) => draft.id !== draftId),
  //       draftData,
  //     ];
  //     localStorage.setItem("formDrafts", JSON.stringify(updatedDrafts));
  //     console.log(" Drafts payload:", draftPayload);

  //     // Save the draft data to local storage
  //     localStorage.setItem(draftId, JSON.stringify(draftPayload)); // Save with isFinalSubmit: false

  //     toast.success("Draft saved successfully");
  //     setFormData(draftData); // Update form data.
  //     setHasPreviewed(true); // Enable preview after saving draft
  //   } catch (error) {
  //     console.error("Error saving draft:", error);
  //     toast.error("Failed to save draft");
  //   } finally {
  //     setIsSavingDraft(false);
  //   }
  // };


    const handleWorkflowAction = async (action, comments) => {
      try {
        const userFormData = JSON.parse(localStorage.getItem("userFormData"));
        const applicationNumber = JSON.parse(localStorage.getItem("applicationdatawithId"));
        console.log("handleWorkflowAction", action, comments);
        console.log("userFormData", userFormData);
        console.log("applicationDataxxxxxxxxxxxxxx", applicationNumber);
      
  
        // if (!userFormData || !userFormData.fullloginDetails) {
        //   throw new Error("User information not found");
        // }
  
        // if (!applicationData || !applicationData.applicationNumber) {
        //   throw new Error("Application data not found");
        // }
  
        const payload = {
          userInfo: userFormData.fullloginDetails,
          applicationNumber: applicationNumber,
          comments: comments,
          action: action
        };
        console.log("Workflow action payload:", payload);
           const URL =
              process.env.REACT_APP_APPLICATION_SERVICE +
              URLS_ENDPOINT.APP_SERVICES.CORPORATE_APPLICATION_SUBMIT;
  
          const response = await postData(URL, payload);
  
        return response.data;
      } catch (error) {
        console.error("Error in workflow action:", error);
        throw error;
      }
    };
  
    const handleActionSubmit = async () => {
      setIsSubmittingAction(true);
      try {
        const actionMap = {
          "Accept": "APPROVED",
          "Reject": "REJECTED",
          "Sendback To Claimants": "SEND_BACK_TO_CITIZEN",
          "Accept as Contingent": "APPROVED_AS_CONTINGENT"
        };
  
        const apiAction = actionMap[actionType];
        if (!apiAction) {
          throw new Error("Invalid action type");
        }
  
        const response = await handleWorkflowAction(apiAction, comments);
        console.log("Workflow response:", response);
        
        // if (response.status && response.status.code === "200") {
          // toast.success(`${actionType} submitted successfully`);
          toast.success(`${response.message}`);
          navigate("/dashboard");
        // } else {
        //   toast.error(response.status?.message || "Action failed");
        // }
      } catch (error) {
        console.error("Error submitting action:", error);
        toast.error(error.message || "Failed to submit action");
      } finally {
        setIsSubmittingAction(false);
        handleModalClose();
      }
    };

  const handleActionClick = (action) => {
    setActionType(action);
    setShowModal(true);
  };

  const handleModalClose = () => {
    setShowModal(false);
    setComments("");
  };

  // const handleActionSubmit = () => {
  //   console.log(`Action: ${actionType}, Comments: ${comments}`);
  //   toast.success(`${actionType} submitted successfully`);
  //   navigate("/dashboard");
  // };

  const handlePrevious = () => {
    setCurrentStep((prevStep) => Math.max(1, prevStep - 1));
    setIsConfirmed(false);
    localStorage.removeItem("isConfirmed");
  };

  const handleCancel = () => {
    navigate("/dashboard");
  };

  const calculateProgressBarWidth = () => {
    return ((currentStep - 1) / (filteredStepsConfig.length - 1)) * 100;
  };

  const ActiveComponent = filteredStepsConfig[currentStep - 1]?.Component;
  const shouldRenderReviewStep = currentStep === filteredStepsConfig.length && isDataLoaded;

  const flattenFormData = (formData) => {
    const flattened = {
      ...formData.step1,
      ...formData.step2,
      ...formData.step3,
    };

    return flattened;
  };
  useEffect(() => {
    if (currentStep === 3) {
      setHasPreviewed(false);
    }
  }, [currentStep]);
 
  return (
    <FormDataProvider formData={flattenFormData(formData)}>
      <div className="stepper">
        {filteredStepsConfig.map((step, index) => (
          <div
            key={step.name}
            ref={(el) => (stepRef.current[index] = el)}
            className={`step ${
              currentStep > index + 1 || isComplete ? "complete" : ""
            } ${currentStep === index + 1 ? "active" : ""}`}
          >
            <div className="step-number">
              {currentStep > index + 1 || isComplete ? (
                <span>✓</span>
              ) : (
                index + 1
              )}
            </div>
            <div className="step-name">{step.name}</div>
          </div>
        ))}

        <div
          className="progress-bar"
          style={{
            marginLeft: margins.marginLeft,
            marginRight: margins.marginRight,
          }}
        >
          <div
            className="progress"
            style={{ width: `${calculateProgressBarWidth()}%` }}
          ></div>
        </div>
      </div>

      {ActiveComponent && (
        <ActiveComponent
          formData={formData[`step${currentStep}`] || {}}
          provideSubmitHandler={updateSubmitHandler}
          setFormData={setFormData}
        />
      )}

      {shouldRenderReviewStep && !isCRPUser && (
        <ReviewStep />
      )}

      {currentStep === filteredStepsConfig.length && !isCRPUser && (
        <div className="confirmation-container">
          <Form.Group controlId="formConfirmation" className="mt-3 confirmation-checkbox">
            <Form.Check
              type="checkbox"
              label="I confirm that the information provided is correct."
              checked={isConfirmed}
              onChange={(e) => setIsConfirmed(e.target.checked)}
            />
          </Form.Group>
        </div>
      )}

      <div className="stepper-buttons">
        {currentStep > 1 && (
          <button className="buttonplus previous-button" onClick={handlePrevious}>
            Previous
          </button>
        )}
        {currentStep === 1 && (
          <button className="buttonplus cancel-button" onClick={handleCancel}>
            Cancel
          </button>
        )}

        {currentStep === 3 && !isCRPUser && (
          <div className="last-step-buttons">
            <button
              className="buttonplus draft-button"
              onClick={handleSaveDraft}
              disabled={isSavingDraft}
            >
              {isSavingDraft ? "Saving..." : "Save as Draft"}
            </button>
            

<button
className="buttonplus preview-button"
onClick={handlePreview}
disabled={!hasPreviewed }
>
Preview
</button>

            

           
          </div>
        )}

        {currentStep !== 3 && currentStep !== filteredStepsConfig.length && (
          <button
            className="buttonplus next-button"
            onClick={handleNext}
            disabled={isComplete}
          >
            Next
          </button>
        )}

        {currentStep === filteredStepsConfig.length && isCRPUser ? (
          <>
            <button
              className="buttonplus sendback-button"
              onClick={() => handleActionClick("Sendback To Claimants")}
            >
              Sendback To Claimants
            </button>
            <button
              className="buttonplus accept-contingent-button"
              onClick={() => handleActionClick("Accept as Contingent")}
            >
              Accept as Contingent
            </button>
            <button
              className="buttonplus accept-button"
              onClick={() => handleActionClick("Accept")}
            >
              Accept
            </button>
            <button
              className="buttonplus reject-button"
              onClick={() => handleActionClick("Reject")}
            >
              Reject
            </button>
          </>
        ) : currentStep === filteredStepsConfig.length ? (
          <button
            className="buttonplus next-button"
            onClick={handleNext}
            disabled={!isConfirmed || isComplete}
          >
            Submit
          </button>
        ) : null}
      </div>

      <Modal show={showModal} onHide={handleModalClose} centered>
        <Modal.Header closeButton>
          <Modal.Title>{actionType} Form</Modal.Title>
        </Modal.Header>
        <Modal.Body>
          <Form>
            <Form.Group controlId="comments">
              <Form.Label>Comments</Form.Label>
              <Form.Control
                as="textarea"
                rows={3}
                value={comments}
                onChange={(e) => setComments(e.target.value)}
                placeholder="Enter your comments..."
              />
            </Form.Group>
          </Form>
        </Modal.Body>
        <Modal.Footer>
          <Button className="modal-close-button" onClick={handleModalClose}>
            Close
          </Button>
          <Button
            className="modal-submit-button"
            onClick={handleActionSubmit}
            disabled={!comments.trim()}
          >
            Submit
          </Button>
        </Modal.Footer>
      </Modal>
    </FormDataProvider>
  );
};

export default Stepper;


previous logic code


this is edit form 
  const editFormData = (apiData) => {
    return {
      iwtTransactionSubsequentToRegistration: 
      apiData.step3?.transactionDataAvailable === 'yes' ?
          {
            uuid: (apiData.step3?.uuidOfTransaction && apiData.step3?.isTransDataAvail) ? String(apiData.step3.uuidOfTransaction) : "",
            iwtRegistrationBookTFk: apiData.step1?.uuidofRegistrationBook ? String(apiData.step1.uuidofRegistrationBook) : "",

            transactionNo: apiData.step3?.transactionDetails?.transactionNo || "",
            natureOfTransaction: apiData.step3?.transactionDetails?.natureOfTransaction || "",
            nameResidenceOccupation: apiData.step3?.transactionDetails?.nameResidenceOccupation || "",
            numberOfTransactionTransferee: apiData.step3?.transactionDetails?.numberOfTransactionTransferee || "",
            remarks: apiData.step3?.transactionDetails?.remarks || "",
            dateOfTransaction: apiData.step3?.transactionDetails?.dateOfTransaction || "",
            nameOfPersonTitleDerived: apiData.step3?.transactionDetails?.nameOfPersonTitleDerived || "",
            noOfSharesAffected: apiData.step3?.transactionDetails?.noOfSharesAffected || "",
            dateHourOfRegistry: apiData.step3?.transactionDetails?.dateHourOfRegistry || "",
            hourOfRegistry: apiData.step3?.transactionDetails?.dateHourOfRegistry ? new Date(apiData.step3?.transactionDetails?.dateHourOfRegistry)?.toLocaleTimeString() : "",

          } : null
      ,
      iwtVesselParticulars: {
        uuid: apiData.step2?.uuidOfVesselParticulars ? String(apiData.step2.uuidOfVesselParticulars) : "",
        build: apiData.step2?.build || null,
        builderNameAddress: apiData.step2?.builderNameAddress || "", 
        builderAddress: apiData.step2?.builderAddress || "",
        description: apiData.step2?.particulardescription || "",
        breadthExtreme: apiData.step2?.breadthExtreme || "",
        depthOfUndersideOfDeckamidShips: apiData.step2?.depthOfUndersideOfDeckamidShips || "",
        lengthOverall: apiData.step2?.lengthOverall || "",
        noOfBulkheads: apiData.step2?.noOfBulkheads || "",
        noOfDecks: apiData.step2?.noOfDecks || "",
        stren: apiData.step2?.stren || null,
        typeOfHull: apiData.step2?.typeOfHull || "",
        yearOfBuild: apiData.step2?.yearOfBuild || "",
        // iwtRegistrationBookTFk: apiData.step1?.uuidofRegistrationBook ? String(apiData.step1.uuidofRegistrationBook) : "",
       
      },
      iwtVesselDescription: {
        uuid: apiData.step1?.uuidOfVesselDescription ? String(apiData.step1.uuidOfVesselDescription) : "",
        category: apiData.step1?.category || null,
        grossTonnage: apiData.step1?.grossTonnage || "",
        registeredTonnage: apiData.step1?.registeredTonnage || null,
        vesselType: apiData.step1?.vesselType || "",
        // iwtRegistrationBookTFk: apiData.step1?.uuidofRegistrationBook ? String(apiData.step1.uuidofRegistrationBook) : "",
        
      },
      iwtRegistrationBook: {
        uuid: apiData.step1?.uuidofRegistrationBook ? String(apiData.step1.uuidofRegistrationBook) : "",
        addressOfOwner: apiData.step1?.addressOfOwner || "",
        nameOfOwner: apiData.step1?.nameOfOwner || "",
        nameOfVessel: apiData.step1?.nameOfVessel || "",
        portOfRegistry: apiData.step1?.portOfRegistry || "",
        registrationBookNo: apiData.step1?.registrationBookNo || "",
        registrationNo: apiData.step1?.registrationNo || "",
        status: null,
        yearOfRegistry: apiData.step1?.yearOfRegistry || "",
        dateOfRegistration: apiData.step2?.dateOfRegistration || "",
        iwtCarvingMakingTFk: apiData.step1?.registrationNo || "",
        noAndYear: apiData.step1?.noAndYear || "",
      },
      iwtPropulsion: {
        uuid: apiData.step2?.uuidofPropulsionsData ? String(apiData.step2.uuidofPropulsionsData) : "",
        iwtRegistrationBookFk: apiData.step1?.uuidofRegistrationBook ? String(apiData.step1.uuidofRegistrationBook) : "",
        propelerType: apiData.step2?.propelerType || "",
        rpm: apiData.step2?.propulsionsRpm || "",
        speedOfVessel: apiData.step2?.speedOfVessel || "",
        propulsionGearedOrDirectDriven: apiData.step2?.propulsionGearedOrDirectDriven || "",
        propulsionType: apiData.step2?.propulsionType || "",
        registeringAuthority: apiData.step2?.registeringAuthority || "",
        dateOfRegistration: apiData.step2?.dateOfRegistration || "",
       
      },
      iwtEngine: {
        uuid: apiData.step2?.uuidOfEngine ? String(apiData.step2.uuidOfEngine) : "",
        // iwtRegistrationBookFk: apiData.step1?.uuidofRegistrationBook ? String(apiData.step1.uuidofRegistrationBook) : "",
        typeOfEngine: apiData.step2?.typeOfEngine || "",
        engineNo: apiData.step2?.engineNo || null,
        description: apiData.step2?.enginedescription || "",
        noOfSeats: apiData.step2?.noOfSeats || 0,
        madeBy: apiData.step2?.engineMadeBy || "",
        yearOfMake: apiData.step2?.engineYearOfMake|| "",
        noOfCylindersPerSet: apiData.step2?.noOfCylindersPerSet || 0,
        diameterOfCylinderInInches: apiData.step2?.diameterOfCylinderInInches || "",
        strokesInInches: apiData.step2?.strokesInInches || "",
        surfaceJet: apiData.step2?.surfaceJet || null,
        nhp: apiData.step2?.nhp || null,
        bhp: apiData.step2?.bhp || "",
        ihp: apiData.step2?.ihp || null,
        rpm: apiData.step2?.engineRpm || null,
    
      },
      iwtBoiler: {
        uuid: apiData.step2?.uuidofBoiler ? String(apiData.step2.uuidofBoiler) : "",
        // iwtRegistrationBookTFk: apiData.step1?.uuidofRegistrationBook ? String(apiData.step1.uuidofRegistrationBook) : "",
        typeOfBoiler: apiData.step2?.typeOfBoiler || "",
        noOfBoiler: apiData.step2?.noOfBoiler || null,
        totalHeatingSurface: apiData.step2?.totalHeatingSurface || null,
        grateArea: apiData.step2?.grateArea || null,
        workingPressure: apiData.step2?.workingPressure || null,
        madeBy: apiData.step2?.boilerMadeBy || null,
        yearOfMake: apiData.step2?.boilerYearOfMake || null,
        madeOfMaterial: apiData.step2?.madeOfMaterial || null,
        
      },
      // iwtCarvingMaking: {
      //   uuid: apiData.step1?.uuidofCarving ? String(apiData.step1.uuidofCarving) : "",
      //   // officialNo: apiData.step1?.registrationNo || "",
      //   // nameOfVessel: apiData.step1?.nameOfVessel || "",
      //   // nameOfOwner: apiData.step1?.nameOfOwner || "",
      //   // addressOfOwner: apiData.step1?.addressOfOwner || "",
      //   // typeOfVessel: apiData.step1?.vesselType || "",
      //   // initialApprovalPhase: apiData.step1?.dateOfRegistration || "",
      //   // finalApprovalPhase: apiData.step1?.dateOfRegistration || "",
       
      // }
    };
  };
 const handleNext = async () => {
    if (!currentSubmitHandler) {
      console.error("No submit handler available.");
      return;
    }
  
    const currentStepData = await currentSubmitHandler();
    if (!currentStepData) {
      console.error("Form validation failed.");
      return;
    }
  
    if (!validateStepData(currentStep, currentStepData)) {
      return;
    }
  
    // Update form data
    const updatedFormData = {
      ...formData,
      [`step${currentStep}`]: { ...formData[`step${currentStep}`], ...currentStepData },
    };
  
    if (!deepEqual(updatedFormData, formData)) {
      setFormData(updatedFormData);
    }
  
    console.log("Updated Form Data:", JSON.stringify(updatedFormData, null, 2));
  
    // Transform formData to API payload structure
    const transformedPayload = transformFormData(updatedFormData);
  
    if (currentStep === stepsConfig.length) {
      setIsComplete(true);
  
      try {
        const isEdit = isEditMode;
        const endpoint = isEdit
          ? `${process.env.REACT_APP_BASE_URL}${URLS_ENDPOINT.APP_SERVICES.REGISTRATION_EDIT}`
          : `${process.env.REACT_APP_BASE_URL}${URLS_ENDPOINT.APP_SERVICES.REGISTRATION_CREATE}`;
  
        const payload = isEdit ? editFormData(updatedFormData) : transformedPayload;
        const response = await postData(endpoint, payload);
  
        // console.log("API Response:", response.data);
  
        // Check the response status and handle accordingly
        const responseStatus = response?.data?.responseStatus;
        if (responseStatus?.status === "200") {
          toast.success(responseStatus?.message );
  
          if (isEdit) {
            // Handle Update API response
            const transactionData = response?.data?.transactionResponse?.iwtTransaction?.[0];
            if (transactionData && transactionData?.nameOfPersonTitleDerived) {
              console.log("Updated Transaction Data:", transactionData);
              toast.info(`Updated Transaction: ${transactionData?.nameOfPersonTitleDerived}`);
            }
          } else {
            // Handle Registration API response
            const registrationData = response?.data?.iwtRegistrationResponses?.[0];
            if (registrationData) {
              console.log("Registration Data:", registrationData);
              toast.info(`Registration Successful for Vessel: ${registrationData?.registrationBooksData?.[0]?.nameOfVessel}`);
            }
          }
  
          // Navigate after successful submission
          navigate("/dashboard");
        } else {
          // Handle Error response (status other than 200)
          const errorMessage =
            responseStatus?.message || "Something went wrong. Please try again.";
          toast.error(errorMessage);
        }
      } catch (error) {
        console.error("API Submission Failed:", error.response?.data || error.message);
        toast.error("Registration failed. Please try again later.");
      }
    } else {
      setCurrentStep((prevStep) => prevStep + 1);
    }
  };
