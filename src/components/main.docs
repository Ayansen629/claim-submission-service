const getFullFieldName = () => {
    if (arrayFieldName && arrayIndex !== undefined) {
      return `${arrayFieldName}[${arrayIndex}].${name}`;
    }
    return name;
  };

  const fullFieldName = getFullFieldName();
  const errorMessage = errors?.[arrayFieldName]?.[arrayIndex]?.[name]?.message || "";
