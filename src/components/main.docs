import React, { useEffect, useState } from "react";
import WelcomeBox from "../../../common/WelcomeBox.js";
import StatusCard from "../../../common/StatusCard.js";
import ActionTable from "../../../common/ActionTable.tsx";
import {
  BsExclamationTriangle,
  BsClipboardData,
  BsSearch,
  BsFilter,
  BsCalendarEvent,
  BsCheckCircle,
  BsFileEarmarkCheck
} from "react-icons/bs";
import "./surveyorDashboard.css";
import { Card, Form, InputGroup, Button } from "react-bootstrap";
import { CHART_OPTIONS, CHART_TYPES } from "../../../../utils/constants.ts";
import chart_icon from "../../../../assets/icons/chart_icon.png";
import ActionModal from "../../../common/ActionModal.tsx";
import DynamicChart from "../../../common/DynamicChart";
import { 
  INBOX_COLUMNS, 
  SURVEY_STATS_CHART_DATA,
  SURVEY_STATUSES 
} from "../../../../utils/surveyor/constants";
import { 
  generateSurveyData, 
  UPCOMING_SURVEYS 
} from "../../../../utils/surveyor/mockData";

const SurveyorDashboard = ({ userDetails }) => {
  return (
    <>
      <WelcomeBox
        userName={
          userDetails
            ? `${userDetails.firstName} ${userDetails.lastName}`
            : ""
        }
        title={userDetails?.userType}
        desc="Here's your survey performance overview."
      />
      <SurveyorContent />
    </>
  );
};

const SurveyorContent = () => {
  const [surveyData, setSurveyData] = useState([]);
  const [filteredSurveyData, setFilteredSurveyData] = useState([]);
  const [currentPage, setCurrentPage] = useState(1);
  const [searchTerm, setSearchTerm] = useState("");
  const itemsPerPage = 5;
  const [isChartModalOpen, setIsChartModalOpen] = useState(false);
  const [selectedDate, setSelectedDate] = useState(new Date().toISOString().split('T')[0]);

  useEffect(() => {
    const data = generateSurveyData();
    setSurveyData(data);
    setFilteredSurveyData(data);
  }, []);

  useEffect(() => {
    const results = surveyData.filter(
      (item) =>
        item.id?.toLowerCase()?.includes(searchTerm?.toLowerCase()) ||
        item.surveyType?.toLowerCase()?.includes(searchTerm?.toLowerCase()) ||
        item.vesselName?.toLowerCase()?.includes(searchTerm?.toLowerCase()) ||
        item.status?.toLowerCase()?.includes(searchTerm?.toLowerCase())
    );
    setFilteredSurveyData(results);
    setCurrentPage(1);
  }, [searchTerm, surveyData]);

  const handleTablePageChange = (page) => {
    setCurrentPage(page);
  };

  const handleCardClick = (cardInfo, index) => {
    if (index === 4) {
      setIsChartModalOpen(true);
    }
  };

  const statData = [
    {
      title: "Total Registered Vessel Count",
      value: 125,
      icon: BsClipboardData,
      color: "#28a745",
      subtitle: "(Total Number of Registered Vessels)",
    },
    {
      title: "Pending Survey Applications",
      value: surveyData.filter(s => s.status === SURVEY_STATUSES.PENDING).length,
      icon: BsExclamationTriangle,
      color: "#ffc107",
      subtitle: "(Applications awaiting your review)",
    },
    {
      title: "Surveys In Progress",
      value: surveyData.filter(s => s.status === SURVEY_STATUSES.IN_PROGRESS).length,
      icon: BsExclamationTriangle,
      color: "#fd7e14",
      subtitle: "(Surveys currently being conducted)",
    },
    {
      title: "Pending Approvals",
      value: 7,
      icon: BsExclamationTriangle,
      color: "#dc3545",
      subtitle: "(Survey reports awaiting approval)",
    },
    {
      title: "Survey Statistics",
      imageSrc: chart_icon,
      color: "#6ec1e4",
      subtitle: "(Survey types breakdown)",
    },
    {
      title: "Survey Scheduled",
      value: surveyData.filter(s => s.status === SURVEY_STATUSES.SCHEDULED).length,
      icon: BsCalendarEvent,
      color: "#17a2b8",
      subtitle: "(Surveys scheduled for next 30 days)",
    },
    {
      title: "Survey Completed",
      value: surveyData.filter(s => s.status === SURVEY_STATUSES.COMPLETED).length,
      icon: BsCheckCircle,
      color: "#28a745",
      subtitle: "(Surveys completed this year)",
    },
    {
      title: "Survey Certificate Issued",
      value: 78,
      icon: BsFileEarmarkCheck,
      color: "#6f42c1",
      subtitle: "(Certificates issued this year)",
    },
    {
      title: "Upcoming Surveys",
      customContent: (
        <div className="upcoming-surveys-container">
          <Form.Control
            type="date"
            value={selectedDate}
            onChange={(e) => setSelectedDate(e.target.value)}
            className="mb-2"
          />
          {UPCOMING_SURVEYS.filter(survey => survey.date === selectedDate).length > 0 ? (
            <div className="upcoming-surveys-list">
              {UPCOMING_SURVEYS.filter(survey => survey.date === selectedDate).map((survey, index) => (
                <div key={index} className="survey-item">
                  <div className="survey-date">{survey.date}</div>
                  <div className="survey-title">{survey.title}</div>
                  <div className="survey-meta">
                    <span>{survey.vesselType}</span>
                    <span>{survey.location}</span>
                  </div>
                </div>
              ))}
            </div>
          ) : (
            <div className="no-surveys">No surveys scheduled for this date</div>
          )}
        </div>
      ),
      color: "#20c997",
      subtitle: "(Highlighted upcoming surveys)",
    }
  ];

  return (
    <>
      <div className="row g-3">
        {statData.map((item, index) => (
          <div className="col-md-4 col-lg-3 col-sm-12" key={index}>
            <StatusCard
              {...item}
              onClick={() => handleCardClick(item, index)}
            />
          </div>
        ))}
      </div>

      <Card className="bg-transparent mt-4 dashboard-card">
        <Card.Header className="dashboard-card-header d-flex justify-content-between align-items-center flex-wrap">
          <div className="d-flex align-items-center">
            <h5 className="mb-0 me-2">Survey Applications</h5>
            <span className="badge bg-primary rounded-pill">
              {filteredSurveyData.length}
            </span>
          </div>
          <div className="d-flex align-items-center">
            <Button variant="outline-secondary" className="filter-btn me-2">
              <BsFilter size={18} />
              <span className="ms-1 d-none d-sm-inline">Filter</span>
            </Button>
            <Form className="inbox-search">
              <InputGroup>
                <Form.Control
                  type="text"
                  placeholder="Search surveys..."
                  aria-label="Search surveys"
                  value={searchTerm}
                  onChange={(e) => setSearchTerm(e.target.value)}
                />
                <Button variant="primary">
                  <BsSearch />
                </Button>
              </InputGroup>
            </Form>
          </div>
        </Card.Header>

        <div style={{ position: "relative", bottom: "2rem" }}>
          <ActionTable
            columns={INBOX_COLUMNS}
            rowData={filteredSurveyData}
            tableHeader={null}
            hideExportBtn={true}
            hideSearchBar={true}
            pagination={true}
            currentPage={currentPage}
            itemsPerPage={itemsPerPage}
            onPageChange={handleTablePageChange}
            noDataText={
              <div className="no-results text-center py-4">
                <BsSearch size={24} className="mb-2" />
                <p>No results found</p>
                <small>
                  Try adjusting your search to find what you're looking for.
                </small>
              </div>
            }
          />
        </div>
      </Card>

      <ActionModal
        show={isChartModalOpen}
        close={setIsChartModalOpen}
        title="Survey Statistics"
        size={"md"}
        isFooterHide={true}
      >
        <DynamicChart
          type={CHART_TYPES[3]}
          data={SURVEY_STATS_CHART_DATA}
          options={CHART_OPTIONS}
        />
      </ActionModal>
    </>
  );
};

export default SurveyorDashboard;
