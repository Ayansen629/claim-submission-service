const handlePayNow = () => {
  const encodedAppNumber = encodeURIComponent(vesselRegisData.applicationNumber);
  navigate(`/payment/${encodedAppNumber}`, {
    state: {
      paymentData,
      applicationNumber: vesselRegisData.applicationNumber,
      fullPaymentData,
    },
  });
};
