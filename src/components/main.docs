// In the edit mode section of handleNext, update the supporting documents part like this:
if (updatedFormData.step4) {
  const existingDocuments = applicationDetails.supportingDocument?.documents || [];
  
  payload.applicationDetails.supportingDocument = {
    uuid: updatedFormData.supportingDocumentUuid || "",
    creditorBasicDetailsUuid: updatedFormData.creditorBasicDetailsUuid || "",
    documents: [
      ...(updatedFormData.step4?.identityProofFile ? [{
        // Use existing UUID if available, otherwise generate new one
        uuid: existingDocuments.find(d => d.docSource === "identity_proof")?.uuid || 
              updatedFormData.step4.identityProofFile.uuid || 
              uuidv4(),
        supportingDocumentUuid: updatedFormData.supportingDocumentUuid || "",
        docId: updatedFormData.step4.identityProofFile.fileId || 
              existingDocuments.find(d => d.docSource === "identity_proof")?.docId || "",
        docType: updatedFormData.step4.identityProofType || 
               existingDocuments.find(d => d.docSource === "identity_proof")?.docType || "",
        docSource: "identity_proof",
        docName: updatedFormData.step4.identityProofFile.fileName || 
               existingDocuments.find(d => d.docSource === "identity_proof")?.docName || ""
      }] : []),
      ...(updatedFormData.step4?.addressProofFile ? [{
        // Use existing UUID if available, otherwise generate new one
        uuid: existingDocuments.find(d => d.docSource === "address_proof")?.uuid || 
              updatedFormData.step4.addressProofFile.uuid || 
              uuidv4(),
        supportingDocumentUuid: updatedFormData.supportingDocumentUuid || "",
        docId: updatedFormData.step4.addressProofFile.fileId || 
              existingDocuments.find(d => d.docSource === "address_proof")?.docId || "",
        docType: updatedFormData.step4.addressProofType || 
               existingDocuments.find(d => d.docSource === "address_proof")?.docType || "",
        docSource: "address_proof",
        docName: updatedFormData.step4.addressProofFile.fileName || 
               existingDocuments.find(d => d.docSource === "address_proof")?.docName || ""
      }] : []),
      // Preserve any other existing documents that aren't identity or address proofs
      ...existingDocuments.filter(d => 
        d.docSource !== "identity_proof" && 
        d.docSource !== "address_proof"
      )
    ],
    empty: false
  };
  
  // Preserve audit details
  if (applicationDetails.supportingDocument?.auditDetails) {
    payload.applicationDetails.supportingDocument.auditDetails = 
      applicationDetails.supportingDocument.auditDetails;
  }
}




//////////////////////////////
} else if (currentStep === 4) {
  const existingDocuments = payload.applicationDetails.supportingDocument?.documents || [];
  
  const identityProofDoc = updatedFormData.step4?.identityProofFile ? {
    uuid: existingDocuments.find(d => d.docSource === "identity_proof")?.uuid || 
          updatedFormData.step4.identityProofFile.uuid || 
          uuidv4(),
    supportingDocumentUuid: payload.applicationDetails.supportingDocument?.uuid || "",
    docId: updatedFormData.step4.identityProofFile.fileId || "",
    docType: updatedFormData.step4.identityProofType || "",
    docSource: "identity_proof",
    docName: updatedFormData.step4.identityProofFile.fileName || ""
  } : existingDocuments.find(d => d.docSource === "identity_proof");

  const addressProofDoc = updatedFormData.step4?.addressProofFile ? {
    uuid: existingDocuments.find(d => d.docSource === "address_proof")?.uuid || 
          updatedFormData.step4.addressProofFile.uuid || 
          uuidv4(),
    supportingDocumentUuid: payload.applicationDetails.supportingDocument?.uuid || "",
    docId: updatedFormData.step4.addressProofFile.fileId || "",
    docType: updatedFormData.step4.addressProofType || "",
    docSource: "address_proof",
    docName: updatedFormData.step4.addressProofFile.fileName || ""
  } : existingDocuments.find(d => d.docSource === "address_proof");

  payload.applicationDetails.supportingDocument = {
    ...payload.applicationDetails.supportingDocument,
    documents: [
      ...(identityProofDoc ? [identityProofDoc] : []),
      ...(addressProofDoc ? [addressProofDoc] : []),
      ...existingDocuments.filter(d => 
        d.docSource !== "identity_proof" && 
        d.docSource !== "address_proof"
      )
    ]
  };
}



////////////////////////////////////

const handleFinalSubmit = async () => {
  if (!isConfirmed) {
    toast.error("Please confirm the declaration before submitting");
    return;
  }

  setIsSubmitting(true);
  try {
    // Get the preview response from localStorage
    const previewResponse = JSON.parse(localStorage.getItem('previewResponse'));
    if (!previewResponse) {
      throw new Error("No preview data found. Please complete the preview first.");
    }

    // Ensure all UUIDs are preserved in the final payload
    const finalPayload = {
      ...previewResponse,
      applicationDetails: {
        ...previewResponse.applicationDetails,
        isFinalSubmit: true,
        // Ensure all UUIDs are properly set
        creditorBasicDetails: {
          ...previewResponse.applicationDetails.creditorBasicDetails,
          identificationTypeDetails: {
            ...previewResponse.applicationDetails.creditorBasicDetails.identificationTypeDetails,
            identifications: previewResponse.applicationDetails.creditorBasicDetails.identificationTypeDetails.identifications.map(ident => ({
              ...ident,
              uuid: ident.uuid || uuidv4(), // Ensure UUID exists
              proofOfIdentificationDocuments: {
                ...ident.proofOfIdentificationDocuments,
                uuid: ident.proofOfIdentificationDocuments.uuid || uuidv4()
              }
            }))
          }
        },
        supportingDocument: {
          ...previewResponse.applicationDetails.supportingDocument,
          documents: previewResponse.applicationDetails.supportingDocument.documents.map(doc => ({
            ...doc,
            uuid: doc.uuid || uuidv4() // Ensure UUID exists
          }))
        }
      }
    };

    const endpoint = `${process.env.REACT_APP_APPLICATION_SERVICE}${URLS_ENDPOINT.APP_SERVICES.APPLICATION_FORMSUBMIT}`;
    const response = await postData(endpoint, finalPayload);

    if (response.data?.status?.code === "200") {
      const successData = response.data;
      // Clear local storage
      localStorage.removeItem('formData');
      localStorage.removeItem('previewResponse');
      localStorage.removeItem('apiResponse');

      toast.success("Application submitted successfully!");
      navigate("/submission-success", {
        state: {
          applicationData: successData,
          applicationNumber: successData.applicationDetails?.applicationNumber || "N/A"
        }
      });
    } else {
      throw new Error(response.data?.status?.message || 'Failed to submit application');
    }
  } catch (error) {
    console.error('Error submitting application:', error);
    toast.error(`Failed to submit application: ${error.message}`);
  } finally {
    setIsSubmitting(false);
  }
};
