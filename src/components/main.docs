import React, { useCallback, useEffect, useState } from "react";
import { useForm, FormProvider } from "react-hook-form";
import DocumentsUpload from "./DocumentsUpload";
import 'bootstrap/dist/css/bootstrap.min.css';
import { uploadDocument } from "../../../services/documentUploadService";

const StepFourth = ({ provideSubmitHandler, formData, onValidationComplete }) => {
    const [isUploading, setIsUploading] = useState(false);
    const [uploadError, setUploadError] = useState(null);

    const methods = useForm({
        mode: "onChange",
        reValidateMode: "onChange",
        defaultValues: formData || {} // Initialize with formData if available
    });
    
    const { handleSubmit, formState: { errors, isValid }, trigger, getValues, setValue } = methods;

    const handleFileUpload = async (file) => {
        try {
            setIsUploading(true);
            setUploadError(null);
            const result = await uploadDocument(file, 'document');
            return result;
        } catch (error) {
            setUploadError(error.message);
            throw error;
        } finally {
            setIsUploading(false);
        }
    };

    const onSubmit = useCallback(async () => {
        const isValid = await trigger();
        if (!isValid) {
            console.error("Validation failed");
            return false;
        }
        return getValues();
    }, [trigger, getValues]);

    useEffect(() => {
        if (onValidationComplete) {
            onValidationComplete(isValid);
        }
    }, [isValid, onValidationComplete]);

    useEffect(() => {
        if (provideSubmitHandler) {
            provideSubmitHandler(onSubmit);
        }
    }, [provideSubmitHandler, onSubmit]);

    return (
        <div className="container-fluid p-0">
            <style>
                {`
                    .step-header {
                        background-color: #269ccc;
                        color: white;
                        padding: 15px;
                        border-radius: 5px;
                        margin-bottom: 20px;
                    }
                    .custom-file-input::-webkit-file-upload-button {
                        background-color: #269ccc;
                        color: white;
                        border: none;
                        padding: 8px 15px;
                        border-radius: 4px;
                        cursor: pointer;
                    }
                    .custom-file-input::-webkit-file-upload-button:hover {
                        background-color: #1d8bb8;
                    }
                    .custom-file-label::after {
                        background-color: #269ccc;
                        color: white;
                    }
                `}
            </style>
            
            <FormProvider {...methods}>
                <form className="form-stepper">
                    <div className="step-header">
                        <h4>Document Upload</h4>
                    </div>
                    <DocumentsUpload 
                        isUploading={isUploading}
                        uploadError={uploadError}
                        setIsUploading={setIsUploading}
                        setUploadError={setUploadError}
                        onFileUpload={handleFileUpload}
                    />
                </form>
            </FormProvider>
        </div>
    );
};

export default StepFourth;
