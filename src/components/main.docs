import React, { useCallback, useEffect, useState } from "react";
import { useForm, FormProvider } from "react-hook-form";
import DynamicField from "./formCommon/DynamicField";
import Collapsible from "./formCommon/Collapsible";
import { uploadDocument } from "../../../services/documentUploadService";
import "./StepFirst.css";
import 'bootstrap/dist/css/bootstrap.min.css';
import { FaPlus, FaTimes } from "react-icons/fa";

const StepFirst = ({ provideSubmitHandler, formData, onValidationComplete }) => {
  const [isUploading, setIsUploading] = useState(false);
  const [uploadError, setUploadError] = useState(null);
  const [identificationRecords, setIdentificationRecords] = useState([{ id: 1 }]);
  
  const methods = useForm({
    mode: "onChange",
    reValidateMode: "onChange"
  });
  
  const { register, handleSubmit, formState: { errors, isValid }, trigger, setValue, control, getValues, watch } = methods;
  const watchIdentificationType = watch("identificationType", "incorporated");

  // Initialize identification records from formData if available
  useEffect(() => {
    if (formData) {
      // Find all identification records in formData
      const recordKeys = Object.keys(formData).filter(key => key.startsWith('identificationRecord_'));
      const recordIds = [...new Set(recordKeys.map(key => key.split('_')[1]))];
      
      if (recordIds.length > 0) {
        setIdentificationRecords(recordIds.map(id => ({ id: Number(id) })));
      }
      
      // Set all form values
      Object.keys(formData).forEach((key) => {
        setValue(key, formData[key]);
      });
    }
  }, [formData, setValue]);

  const handleFileUpload = async (file) => {
    try {
      setIsUploading(true);
      setUploadError(null);
      const result = await uploadDocument(file, 'identification');
      return result;
    } catch (error) {
      setUploadError(error.message);
      throw error;
    } finally {
      setIsUploading(false);
    }
  };

  const addIdentificationRecord = () => {
    const newId = Date.now();
    setIdentificationRecords([...identificationRecords, { id: newId }]);
  };

  const removeIdentificationRecord = (id) => {
    if (identificationRecords.length > 1) {
      const updatedRecords = identificationRecords.filter(record => record.id !== id);
      setIdentificationRecords(updatedRecords);
      // Also remove the form values for the removed record
      const values = getValues();
      Object.keys(values).forEach(key => {
        if (key.includes(`identificationRecord_${id}`)) {
          setValue(key, undefined);
        }
      });
    }
  };

  // ... rest of your schema definitions remain the same ...

  return (
    <FormProvider {...methods}>
      <form className="form-stepper">
        <div className="mb-4 bg-light p-4 rounded-3">
          <Collapsible title="Basic Details" defaultOpen={true}>
            <div className="container-fluid">
              <div className="row g-3">
                {basicDetailsSchema.map((fieldConfig) => (
                  <div key={fieldConfig.name} className="col-md-6">
                    <DynamicField
                      fieldConfig={fieldConfig}
                      control={control}
                      register={register}
                      errors={errors}
                      setValue={setValue}
                      trigger={trigger}
                      getValues={getValues}
                    />
                  </div>
                ))}
              </div>
              {watchIdentificationType === "incorporated" && (
                <div className="row g-3 mt-2">
                  {incorporatedBodyFields.map((fieldConfig) => (
                    <div key={fieldConfig.name} className="col-md-6">
                      <DynamicField
                        fieldConfig={{
                          ...fieldConfig,
                          isUploading,
                          uploadError,
                          setIsUploading,
                          setUploadError,
                          onFileUpload: handleFileUpload
                        }}
                        control={control}
                        register={register}
                        errors={errors}
                        setValue={setValue}
                        trigger={trigger}
                        getValues={getValues}
                      />
                      {fieldConfig.name === "incorporatedProofDocument" && (
                        <div className="form-text text-muted">
                          Max file size: 5MB (PDF, JPEG, PNG)
                        </div>
                      )}
                    </div>
                  ))}
                </div>
              )}
              {watchIdentificationType === "partnership" && (
                <div className="mt-3">
                  {identificationRecords.map((record, index) => (
                    <div key={record.id} className="mb-4 p-3 border rounded">
                      <div className="d-flex justify-content-between align-items-center mb-3">
                        <h6 className="mb-0">Identification Record {index + 1}</h6>
                        {identificationRecords.length > 1 && (
                          <button
                            type="button"
                            className="btn btn-sm btn-danger"
                            onClick={() => removeIdentificationRecord(record.id)}
                          >
                            <FaTimes /> Remove
                          </button>
                        )}
                      </div>
                      <div className="row g-3">
                        <div className="col-12">
                          <DynamicField
                            fieldConfig={{
                              name: `identificationRecord_${record.id}_details`,
                              label: <>Identification Records <span className="required-asterisk">*</span></>,
                              type: "textarea",
                              rules: {
                                required: "Identification records are required",
                                minLength: {
                                  value: 10,
                                  message: "Please provide detailed identification records"
                                }
                              },
                              placeholder: "Enter identification records",
                              tooltip: "Detailed identification records",
                              className: "mb-3"
                            }}
                            control={control}
                            register={register}
                            errors={errors}
                            setValue={setValue}
                            trigger={trigger}
                            getValues={getValues}
                          />
                        </div>
                        <div className="col-12">
                          <DynamicField
                            fieldConfig={{
                              name: `identificationRecord_${record.id}_proof`,
                              label: <>Proof of identification Records <span className="required-asterisk">*</span></>,
                              type: "file",
                              rules: {
                                required: "Identification proof is required",
                                validate: {
                                  fileSize: (files) => {
                                    if (files && files[0]) {
                                      const fileSizeInMB = files[0].size / (1024 * 1024);
                                      return fileSizeInMB <= 5 || "File size must be ≤ 5MB";
                                    }
                                    return true;
                                  }
                                }
                              },
                              tooltip: "Upload identification proof",
                              accept: "image/*,.pdf",
                              onFileUpload: handleFileUpload,
                              isUploading,
                              uploadError,
                              setIsUploading,
                              setUploadError,
                            }}
                            control={control}
                            register={register}
                            errors={errors}
                            setValue={setValue}
                            trigger={trigger}
                            getValues={getValues}
                          />
                          <div className="form-text text-muted">
                            Max file size: 5MB (PDF, JPEG, PNG)
                          </div>
                        </div>
                      </div>
                    </div>
                  ))}
                  <button
                    type="button"
                    className="btn btn-outline-primary mt-2"
                    onClick={addIdentificationRecord}
                  >
                    <FaPlus /> Add Another Identification Record
                  </button>
                </div>
              )}
            </div>
          </Collapsible>
          <Collapsible title="Address Details" defaultOpen={true}>
            <div className="container-fluid">
              <div className="row g-3">
                {addressDetailsSchema.map((fieldConfig) => (
                  <div key={fieldConfig.name} className="col-md-6">
                    <DynamicField
                      fieldConfig={fieldConfig}
                      control={control}
                      register={register}
                      errors={errors}
                      setValue={setValue}
                      trigger={trigger}
                      getValues={getValues}
                    />
                  </div>
                ))}
              </div>
            </div>
          </Collapsible>
        </div>
      </form>
    </FormProvider>
  );
};

export default StepFirst;
