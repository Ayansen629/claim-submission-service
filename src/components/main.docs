const handleFileUpload = async (file, fieldName) => {
  const maxSize = 10 * 1024 * 1024;
  if (file.size > maxSize) {
    toast.error("File size must be less than or equal to 10 MB.");
    return null;
  }

  try {
    loaderRef.current?.show();
    const formData = new FormData();
    formData.append("file", file);

    const uploadUrl = BASE_IP + URLS_ENDPOINT.FILE_STORE_SERVICE.UPLOAD_FILE;
    
    // Use axios directly without authorization headers
    const response = await axios.post(uploadUrl, formData, {
      headers: {
        'Content-Type': 'multipart/form-data',
      },
    });
    
    if (response?.data?.files?.[0]?.fileStoreId) {
      const fileStoreId = response.data.files[0].fileStoreId;
      
      setValue(fieldName, fileStoreId);
      setFormData((prev) => ({
        ...prev,
        [fieldName]: fileStoreId,
      }));

      if (errors[fieldName]) {
        clearErrors(fieldName);
      }
      
      loaderRef.current?.hide();
      return fileStoreId;
    } else {
      throw new Error("Invalid response from server");
    }
  } catch (err) {
    console.error("File upload failed", err);
    toast.error("File upload failed. Please try again.");
    loaderRef.current?.hide();
    return null;
  }
};
