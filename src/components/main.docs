import { useNavigate } from "react-router-dom";
import "react-toastify/dist/ReactToastify.css";
import { useTranslation } from "react-i18next";
import { useEffect, useState, useRef } from "react";
import ActionTable from "../../../common/ActionTable.tsx";
import { toast } from "react-toastify";
import { useForm } from "react-hook-form";
import { USER_REGIS_MASTER } from "./userManagConst.ts";
import { Card, Row, Col } from "react-bootstrap";
import InputSelect from "../../../common/InputSelect.tsx";
import InputText from "../../../common/InputText.tsx";
import NewLoader from "../../../common/NewLoader.js";
import { useMasterData } from "../../../context/MasterDataContext.js";
import { Box, Typography, Stack } from "@mui/material";
import { handleErrorMessage } from "../../../../utils/errorMessageHandler.js";
import { postData } from "../../../../services/http.service.ts";
import { URLS_ENDPOINT } from "../../../../utils/urlsConstants.ts";
import { allowOnlyInteger } from "../../../../utils/constants.ts";

const UserManagement = () => {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const [rowData, setRowData] = useState([]);
  const loaderRef = useRef();
  const { masterData } = useMasterData();

  const {
    register,
    handleSubmit,
    formState: { errors },
    reset,
    setValue,
    watch,
  } = useForm({
    defaultValues: {
      empId: "",
      userName: "",
      designation: "",
      status: "",
      phoneNumber: "",
    },
  });

  const fetchedUserDetails = useRef(false);
  useEffect(() => {
    if (fetchedUserDetails.current) return;
    getUserDetails();
    fetchedUserDetails.current = true;
  }, []);

  const columns = USER_REGIS_MASTER.USER_INFO_COLUMNS;

  const getUserDetails = async (payload) => {
    try {
      const URL =
        process.env.REACT_APP_AUTH_URL + URLS_ENDPOINT.USER_SERVICE.SEARCH_EMP;

      loaderRef.current?.show();
      const result = await postData(URL, payload);
      const rData = result?.data?.data?.vesselUsers || [];

      if (result?.status === 200 && rData?.length > 0) {
        loaderRef?.current?.hide();
        const updatedData = rData?.map(
          ({ firstName, lastName, isActive, ...rest }) => ({
            ...rest,
            firstName,
            lastName,
            isActive,
            empName: `${firstName} ${lastName}`,
            status: isActive ? "Active" : "Inactive",
            designation: rest?.professionalDetails?.[0]?.designation || ''
          })
        );
        setRowData(updatedData);
      } else {
        loaderRef?.current?.hide();
        toast.info(
          result?.data?.responseStatus?.message || "Something went wrong..",
          { position: "top-right" }
        );
      }
    } catch (err) {
      console.log(err?.response?.data);
      loaderRef?.current?.hide();
      handleErrorMessage(err?.response?.data);
    }
  };

  const handleRowClick = (e) => {};

  const handleAddEmployee = () => {
    navigate("/employee-registration");
  };

  // Pass full row data to edit page
  const handleEditRow = (row) => {
    navigate(`/employee-registration/edit/${row.id}`, { state: { employee: row } });
  };

  const onSubmitFilterForm = async (data) => {
    if (data && Object.keys(data)?.length > 0) {
      getUserDetails(data);
    }
  };

  return (
    <>
      <NewLoader ref={loaderRef} />

      <Row className="mt-2">
        <Col md={9}>
          <Stack spacing={0.5} style={{ padding: "0.5rem" }}>
            <Typography
              sx={{
                fontWeight: 550,
                color: "#2E57A7",
                fontSize: "1.2rem",
              }}
            >
              Manage Employee(s) &nbsp;
              <span style={{ color: "#666", fontSize: "0.65rem" }}>
                (Fill in the employee details to complete registration)
              </span>
            </Typography>
          </Stack>
        </Col>
        <Col md={3} className="d-flex justify-content-end">
          <button
            className="generic-btn-submit"
            type="button"
            onClick={handleAddEmployee}
          >
            <i className="fa fa-plus" aria-hidden="true"></i> New Employee
          </button>
        </Col>
      </Row>

      <div className="w-full">
        <Card>
          <form
            style={{ padding: "2rem", margin: 0 }}
            onSubmit={handleSubmit(onSubmitFilterForm)}
          >
            <Row>
              <Col md={2}>
                <InputText
                  type="text"
                  name="empId"
                  maxlength={10}
                  label="Employee Id"
                  placeholder="Employee Id.."
                  register={{
                    ...register("empId", {}),
                  }}
                  errors={errors}
                  onKeyDown={(e) => {
                    if (!allowOnlyInteger(e)) e.preventDefault();
                  }}
                />
              </Col>

              <Col md={2}>
                <InputText
                  type="text"
                  name="userName"
                  label="Employee Name"
                  maxlength={80}
                  placeholder="Employee name.."
                  register={{
                    ...register("userName", {}),
                  }}
                  errors={errors}
                />
              </Col>

              <Col md={3}>
                <InputSelect
                  label="Designation"
                  name="designation"
                  options={
                    masterData?.designations?.map((type) => ({
                      value: type.valueCode,
                      label: type.valueLabel,
                    })) || []
                  }
                  register={register}
                  errors={errors}
                  value={watch("designation")}
                  onChange={(val) => setValue("designation", val)}
                />
              </Col>

              <Col md={2}>
                <InputSelect
                  label="Status"
                  name="status"
                  placeholder="Status"
                  options={[
                    { value: "", label: "--Select--" },
                    { value: "active", label: "Active" },
                    { value: "inactive", label: "Inactive" },
                  ]}
                  register={register}
                  errors={errors}
                  value={watch("status")}
                  onChange={(val) => setValue("status", val)}
                />
              </Col>

              <Col md={3}>
                <InputText
                  type="text"
                  name="phoneNumber"
                  label="Phone Number"
                  maxlength={10}
                  placeholder="Enter Phone Number"
                  register={{
                    ...register("phoneNumber", {}),
                  }}
                  onKeyDown={(e) => {
                    if (!allowOnlyInteger(e)) e.preventDefault();
                  }}
                  errors={errors}
                />
              </Col>
            </Row>

            <Col
              md={4}
              style={{ display: "flex", gap: "10px", float: "right" }}
            >
              <div className="w48">
                <button
                  className="generic-btn-cancel"
                  type="reset"
                  onClick={() => {
                    reset();
                  }}
                >
                  {t("login.reset")}
                </button>
              </div>
              <div className="w48">
                <button className="generic-btn-submit" type="submit">
                  Filter
                </button>
              </div>
            </Col>
          </form>
        </Card>

        <ActionTable
          tableHeader="Employee List"
          columns={columns}
          rowData={rowData}
          additionalMethod={handleRowClick}
          showCustomBtn={true}
          btnName={t("Add")}
          handleBtnClick={handleAddEmployee}
          handleEditRow={handleEditRow}
          hideExportBtn={true}
          searchBarPlaceholder={t("Table.SEARCH_BY")}
        />
      </div>
    </>
  );
};

export default UserManagement;
