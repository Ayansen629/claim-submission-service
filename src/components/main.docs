<label
  htmlFor={name}
  className={`file-label btn ${hasError ? 'btn-outline-danger' : 'btn-primary'} mb-0 d-inline-flex align-items-center`}
  style={{
    borderRadius: '6px',
    padding: '8px 16px',
    cursor: disabled ? 'not-allowed' : 'pointer',
    opacity: disabled ? 0.6 : 1,
    minWidth: '120px',
    color: hasError ? '#dc3545' : 'white'
  }}
>
  <FaUpload className="file-icon me-2" />
  <span className="file-text" style={{ color: hasError ? '#dc3545' : 'white' }}>Choose File</span>
</label>
