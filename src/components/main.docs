const fetchPaymentDetails = async () => {
  // Remove API call, just set empty payment data
  setPaymentData({
    invoiceBreakupInfo: [],
    summary: {
      subTotal: 0,
      cgst: 0,
      sgst: 0,
      totalPayable: 0,
    },
  });
  setFullPaymentData({});
};
