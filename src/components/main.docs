appointmentSlots: appointmentSlots.map(slot => ({
      date: slot.date,
      timeSlot: slot.timeSlot,
      appointmentStatus: slot.appointmentStatus || "PENDING",
      isBooked: slot.isBooked || false,
      bookedBy: slot.bookedBy || null
    }))
