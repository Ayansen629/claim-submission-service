{/* Declaration Collapsible */}
<Collapsible title="Declaration" openStat={true}>
  <div className="mb-3">
    <div className="d-flex align-items-start">
      <Form.Check
        type="checkbox"
        id="termsAccepted"
        className="me-2 mt-1" // Added mt-1 for better vertical alignment
        {...register("termsAccepted", {
          required: "You must accept the terms and conditions",
        })}
        isInvalid={!!errors.termsAccepted}
        onChange={() => !viewMode && clearErrors("termsAccepted")}
        disabled={viewMode}
      />
      <div>
        <Form.Label
          htmlFor="termsAccepted"
          className="mb-0"
          style={{ fontWeight: 500, color: "#333", cursor: "pointer" }}
        >
          I have thoroughly read and understood all the terms and conditions as mentioned in the Schedule.
        </Form.Label>
        {errors.termsAccepted && (
          <div className="text-danger small mt-1" style={{ display: "block" }}>
            {errors.termsAccepted.message}
          </div>
        )}
      </div>
    </div>
  </div>
</Collapsible>
