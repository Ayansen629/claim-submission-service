import React, { useState, useEffect, useRef } from "react";
import { Form } from "react-bootstrap";
import { FaUpload, FaFile, FaFilePdf, FaFileImage, FaCheckCircle } from "react-icons/fa";

const InputFile = ({
  label,
  required,
  name,
  errors,
  onChange,
  accept = ".pdf,.jpg,.jpeg,.png",
  disabled,
  className = "",
  style = {},
  watch,
  defaultValue,
  autoFocus = false,
  register
}) => {
  const fileInputRef = useRef();
  const [fileName, setFileName] = useState("");
  const [hasFile, setHasFile] = useState(false);
  
  // Get the current value from watch
  const watchedValue = watch ? watch(name) : undefined;
  
  // Update local state when watched value changes
  useEffect(() => {
    if (watchedValue instanceof File) {
      setFileName(watchedValue.originalName || watchedValue.name);
      setHasFile(true);
    } else if (typeof watchedValue === "string" && watchedValue.trim() !== "") {
      setFileName(watchedValue);
      setHasFile(true);
    } else if (defaultValue) {
      if (defaultValue instanceof File) {
        setFileName(defaultValue.originalName || defaultValue.name);
        setHasFile(true);
      } else if (typeof defaultValue === "string" && defaultValue.trim() !== "") {
        setFileName(defaultValue);
        setHasFile(true);
      } else {
        setFileName("");
        setHasFile(false);
      }
    } else {
      setFileName("");
      setHasFile(false);
    }
  }, [watchedValue, defaultValue]);

  // Handle file change
  const handleFileChange = (e) => {
    if (e.target.files && e.target.files[0]) {
      const file = e.target.files[0];
      setFileName(file.name);
      setHasFile(true);
    } else {
      setFileName("");
      setHasFile(false);
    }
    
    if (onChange) {
      onChange(e);
    }
  };

  // Get file icon based on extension
  const getFileIcon = () => {
    if (!fileName) return <FaFile color="#0d6efd" />;
    const extension = fileName.split('.').pop()?.toLowerCase();
    if (extension === 'pdf') return <FaFilePdf color="#dc3545" />;
    if (['jpg', 'jpeg', 'png', 'gif'].includes(extension || '')) return <FaFileImage color="#28a745" />;
    return <FaFile color="#0d6efd" />;
  };

  // Only show error if there's no file and there's an error
  const showError = errors && errors[name] && !hasFile;

  return (
    <Form.Group className={`mb-4 ${className}`} controlId={name}>
      <Form.Label className="fw-bold text-dark">
        {label}
        {required && <span className="text-danger ms-1">*</span>}
      </Form.Label>
      <div
        className={`file-upload border rounded p-3 ${showError ? 'border-danger' : ''}`}
        style={{
          backgroundColor: showError ? '#fff8f8' : 'white',
          borderColor: showError ? '#dc3545' : hasFile ? '#28a745' : '#ced4da',
          borderRadius: '8px',
          transition: 'all 0.3s ease',
          ...style
        }}
      >
        <div className="d-flex align-items-center">
          <label
            htmlFor={name}
            className={`file-label btn ${showError ? 'btn-outline-danger' : 'btn-primary'} mb-0 d-inline-flex align-items-center`}
            style={{
              borderRadius: '6px',
              padding: '8px 16px',
              cursor: disabled ? 'not-allowed' : 'pointer',
              boxShadow: '0 2px 5px rgba(0,0,0,0.1)',
              transition: 'all 0.3s ease',
              minWidth: '120px',
              color: 'white!important',
              flexShrink: 0,
              opacity: disabled ? 0.65 : 1
            }}
          >
            <FaUpload className="file-icon me-2" />
            <span className="file-text">Choose File</span>
          </label>
          <div
            className={`file-name-container ms-3 flex-grow-1 border rounded px-3 py-2 ${hasFile ? 'bg-light-success' : 'bg-light'} d-flex align-items-center ${showError ? 'border-danger' : hasFile ? 'border-success' : ''}`}
            style={{
              backgroundColor: hasFile ? '#f0f9f0' : '#f8f9fa',
              overflow: 'hidden'
            }}
          >
            {hasFile ? (
              <>
                <FaCheckCircle className="text-success me-2 flex-shrink-0" />
                <div className="overflow-hidden">
                  <span className="text-success fw-bold" style={{ fontSize: '0.8rem' }}>Selected File:</span>
                  <div className="d-flex align-items-center">
                    <span className="file-icon-preview me-2 flex-shrink-0">{getFileIcon()}</span>
                    <span className="text-dark fw-semibold" style={{ overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
                      {fileName}
                    </span>
                  </div>
                </div>
              </>
            ) : (
              <span className="text-muted">No file chosen</span>
            )}
          </div>
        </div>
        <input
          ref={fileInputRef}
          type="file"
          id={name}
          onChange={handleFileChange}
          accept={accept}
          disabled={disabled}
          className="file-input d-none"
          {...register}
        />
      </div>
      <Form.Text className="text-muted small mt-1">
        Upload PDF, JPG or PNG file (Max size: 2MB)
      </Form.Text>
      {showError && (
        <Form.Text className="text-danger small mt-1 d-block">
          {errors[name]?.message}
        </Form.Text>
      )}
    </Form.Group>
  );
};

export default InputFile;
