const renderFileField = (docType, label) => {
  if (isViewMode) {
    return (
      <div>
        <label className="form-label">{t(label)}</label>
        {fileStates[docType] ? (
          <div className="d-flex align-items-center p-2 border rounded" style={{ backgroundColor: '#f8f9fa' }}>
            <span className="me-2">{fileStates[docType].name}</span>
            <button
              type="button"
              className="btn btn-sm btn-primary rounded-circle d-flex align-items-center justify-content-center"
              onClick={() => handleFileDownload(
                fileStates[docType].fileStoreId,
                fileStates[docType].name,
                fileStates[docType].originalName
              )}
              title="Download file"
              style={{ width: '24px', height: '24px', padding: '0' }}
            >
              <FaDownload size={10} />
            </button>
          </div>
        ) : (
          <div className="p-2 border rounded text-muted" style={{ backgroundColor: '#f8f9fa' }}>
            No file uploaded
          </div>
        )}
      </div>
    );
  }

  return (
    <>
      <InputFile
        name={docType}
        label={t(label)}
        id={docType}
        watch={watch}
        defaultValue={watch(docType)}
        register={register(docType, {
          required: !fileStates[docType] && t(`${label} is required`),
        })}
        errors={errors}
        required={!fileStates[docType]}
        clearErrors={clearErrors}
        onChange={(e) => handleFileChange(e, docType)}
      />
      {fileStates[docType] && (
        <div className="d-flex align-items-center mt-2">
          <small className="text-success me-2">✓ {fileStates[docType].name}</small>
          <button
            type="button"
            className="btn btn-sm btn-primary rounded-circle d-flex align-items-center justify-content-center me-2"
            onClick={() => handleFileDownload(
              fileStates[docType].fileStoreId,
              fileStates[docType].name,
              fileStates[docType].originalName
            )}
            title="Download file"
            style={{ width: '24px', height: '24px', padding: '0' }}
          >
            <FaDownload size={10} />
          </button>
          <button
            type="button"
            className="btn btn-sm btn-danger rounded-circle d-flex align-items-center justify-content-center"
            onClick={() => handleDeleteFile(
              fileStates[docType].fileStoreId,
              docType
            )}
            title="Delete file"
            style={{ width: '24px', height: '24px', padding: '0' }}
          >
            <FaTrash size={10} />
          </button>
        </div>
      )}
    </>
  );
};
