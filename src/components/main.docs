const handleCertificateSubmit = async () => {
  const formValues = methods?.getValues();
  console.log("=== CERTIFICATE SUBMIT ===", formValues);
  await handleGenerateProvisionalCertificate(formValues, "REGISTRATION_CERTIFICATE");
};
