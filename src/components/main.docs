import React, { useRef, useState, useEffect, forwardRef, useImperativeHandle } from "react";
import { useNavigate, useParams, useLocation } from "react-router-dom";
import { useForm } from "react-hook-form";
import { Container, Row, Col, Card } from "react-bootstrap";
import { toast } from "react-toastify";
import { useTranslation } from "react-i18next";
import { FaUser, FaLock, FaDownload } from "react-icons/fa";
import InputFile from "../../../../common/InputFile.jsx";
import InputText from "../../../../common/InputText.tsx";
import InputSelect from "../../../../common/InputSelect.tsx";
import Collapsible from "../../../../common/Collapsible.js";
import { CONSTANT_REGEX } from "../../../../../utils/constants.ts";
import { useMasterData } from "../../../../context/MasterDataContext.js";
import NewLoader from "../../../../common/NewLoader.js";
import { getData, postData, postFORMdata } from "../../../../../services/http.service.ts";
import { BASE_IP, SERVICE_BASE, URLS_ENDPOINT } from "../../../../../utils/urlsConstants.ts";
import { getFromLocalStorage } from "../../../../../utils/localStorageUtils.js";
import axios from "axios";
import styles from "./RegistrationApplicationForm.module.css";

const RegistrationApplicationForm = forwardRef(({ registrationData, isViewMode = false }, ref) => {
  const { t } = useTranslation();
  const { id } = useParams();
  const loaderRef = useRef();
  const { masterData } = useMasterData();
  const [isDataLoaded, setIsDataLoaded] = useState(false);
  const [suggestions, setSuggestions] = useState([]);

  const {
    register,
    formState: { errors },
    setValue,
    watch,
    clearErrors,
    trigger,
    setError,
  } = useForm({
    mode: "onChange",
    defaultValues: {
      ownerName: "", address: "", ownerType: "", builderEmail: "",
      applicantphoneNumber: "", ownerpin: "", ownerdistrict: "",
      ownerstateUt: "", occupation: "", originalPlaceOfBusiness: "",
      nameOfMaster: "", certificateNo: "", nameOfRegistry: "", registrationNo: "",
      vesselName: "", kindOfVessel: "", typeOfVessel: "", categoryOfVessel: "",
      zone: "", mortgage: "", placeOfBuild: "", detailsOfInsurance: "",
      vesselyearOfBuild: "", protectionAndIndemnityClubNo: "", protectionAndIndemnityClubName: "",
      insuranceValidUpto: "", yearOfBuild: "", typeOfEngine: "", engineMake: "",
      engineMakerAddress: "", enginepin: "", enginephoneNumber: "", engineemail: "",
      horsepower: "", speed: "", yearOfMake: "", builderName: "", builderAddress: "",
      builderemail: "", builderphoneNumber: "", pin: "", district: "",
      stateUt: "", country: "India",
    },
  });

  useImperativeHandle(ref, () => ({
    validateForm: async () => {
      if (isViewMode) return true;
      const isValid = await trigger();
      if (!isValid) {
        const firstErrorField = Object.keys(errors)[0];
        if (firstErrorField) {
          const element = document.querySelector(`[name="${firstErrorField}"]`);
          if (element) {
            element.scrollIntoView({ behavior: 'smooth', block: 'center' });
            element.focus();
          }
        }
      }
      return isValid;
    },
    getFormData: () => watch(),
  }));

  const [fileStates, setFileStates] = useState({
    thirdPartyInsurance: null,
    builderCertificate: null,
    instrumentOfSale: null,
    mortgageDetails: null,
    pniInsurance: null,
    declarationOfOwnership: null,
    challan: null,
  });

  const hasValue = (value) => {
    return value !== null && value !== undefined && value !== "" && value !== "N/A";
  };

  const isFieldDisabled = (fieldName) => {
    if (isViewMode) return true;
    if (!isDataLoaded || !registrationData?.basicDetails?.ownerDetails) return false;
    const owner = registrationData.basicDetails.ownerDetails;

    switch (fieldName) {
      case 'ownerName':
        return hasValue(owner.ownerCompanyName);
      case 'address':
        return hasValue(owner.addressLine1);
      case 'ownerType':
        return hasValue(owner.categoryOfOwner);
      case 'builderEmail':
        return hasValue(owner.emailId);
      case 'applicantphoneNumber':
        return hasValue(owner.phoneNumber);
      case 'ownerpin':
        return hasValue(owner.pin);
      case 'ownerdistrict':
        return hasValue(owner.district);
      case 'ownerstateUt':
        return hasValue(owner.state);
      default:
        return false;
    }
  };

  const isVesselFieldDisabled = (fieldName) => {
    if (isViewMode) return true;
    if (!isDataLoaded || !registrationData?.vesselDetails) return false;
    const vessel = registrationData.vesselDetails;
    const hasRegistrationNumber = hasValue(vessel.registrationNumber);
    return !hasRegistrationNumber;
  };

  const isBuilderFieldDisabled = (fieldName) => {
    if (isViewMode) return true;
    if (!isDataLoaded || !registrationData?.basicDetails?.builderDetails) return false;
    const builder = registrationData.basicDetails.builderDetails;

    switch (fieldName) {
      case 'builderName':
        return hasValue(builder.builderName);
      case 'builderAddress':
        return hasValue(builder.addressLine1);
      case 'builderemail':
        return hasValue(builder.emailId);
      case 'builderphoneNumber':
        return hasValue(builder.phone);
      case 'pin':
        return hasValue(builder.pin);
      case 'district':
        return hasValue(builder.district);
      case 'stateUt':
        return hasValue(builder.state);
      default:
        return false;
    }
  };

  const shouldShowBuilderDetails = () => {
    return registrationData?.basicDetails?.builderDetails !== null && 
           registrationData?.basicDetails?.builderDetails?.builderName !== null &&
           registrationData?.basicDetails?.constructedBy !== "1";
  };

  const hasRegistrationNumber = () => {
    return hasValue(registrationData?.vesselDetails?.registrationNumber);
  };

  const vesselKindOptions = masterData?.kindOfVessels?.map(type => ({
    value: type.valueCode,
    label: type.valueLabel
  })) || [];
  
  const vesselTypeOptions = masterData?.vesselTypes?.map(type => ({
    value: type.valueCode,
    label: type.valueLabel
  })) || [];

  const categoryOptions = masterData?.vesselCategories?.map(category => ({
    value: category.valueCode,
    label: category.valueLabel
  })) || [];

  const zoneOptions = masterData?.zones?.map(zone => ({
    value: zone.valueCode,
    label: zone.valueLabel
  })) || [];

  const hasOwnerData = isDataLoaded && registrationData?.basicDetails?.ownerDetails &&
    Object.values(registrationData.basicDetails.ownerDetails).some(value => hasValue(value));

  const hasBuilderData = isDataLoaded && registrationData?.basicDetails?.builderDetails &&
    Object.values(registrationData.basicDetails.builderDetails).some(value => hasValue(value));

  useEffect(() => {
    if (registrationData && !isDataLoaded) {
      const { basicDetails, vesselDetails, engineDetails, enclosers } = registrationData;

      if (basicDetails?.ownerDetails) {
        const owner = basicDetails.ownerDetails;
        setValue("ownerName", owner.ownerCompanyName || "");
        setValue("address", owner.addressLine1 || "");
        setValue("ownerType", owner.categoryOfOwner || "");
        setValue("builderEmail", owner.emailId || "");
        setValue("applicantphoneNumber", owner.phoneNumber || "");
        setValue("ownerpin", owner.pin?.toString() || "");
        setValue("ownerdistrict", owner.district || "");
        setValue("ownerstateUt", owner.state || "");
        setValue("occupation", basicDetails.occupation || "");
        setValue("originalPlaceOfBusiness", basicDetails.originalPlaceBusiness || "");
      }

      if (vesselDetails) {
        setValue("nameOfMaster", vesselDetails.nameOfMaster || "");
        setValue("certificateNo", vesselDetails.certficateNumber || "");
        setValue("nameOfRegistry", vesselDetails.nameOfRegistry || "");
        setValue("registrationNo", vesselDetails.registrationNumber || "");
        setValue("vesselName", vesselDetails.vesselName || "");
        setValue("kindOfVessel", vesselDetails.kindOfVessel || "");
        setValue("mortgage", vesselDetails.mortgage || "");
        setValue("placeOfBuild", vesselDetails.placeOfBuild || "");
        setValue("detailsOfInsurance", vesselDetails.insuranceDetails || "");
        setValue("vesselyearOfBuild", vesselDetails.yearOfBuild?.toString() || "");
        setValue("protectionAndIndemnityClubNo", vesselDetails.pniClubNo || "");
        setValue("protectionAndIndemnityClubName", vesselDetails.pniClubName || "");
        setValue("insuranceValidUpto", vesselDetails.insuranceValidDate || "");
      }

      if (engineDetails) {
        setValue("yearOfBuild", engineDetails.yearOfBuild || "");
        setValue("typeOfEngine", engineDetails.engineType || "");
        setValue("engineMake", engineDetails.engineMake || "");
        setValue("engineMakerAddress", engineDetails.makerAddress || "");
        setValue("enginepin", engineDetails.pin || "");
        setValue("enginephoneNumber", engineDetails.mobile || "");
        setValue("engineemail", engineDetails.email || "");
        setValue("horsepower", engineDetails.horsepower?.toString() || "");
        setValue("speed", engineDetails.speed?.toString() || "");
        setValue("yearOfMake", engineDetails.yearOfMake || "");
      }

      if (basicDetails?.builderDetails) {
        const builder = basicDetails.builderDetails;
        setValue("builderName", builder.builderName || "");
        setValue("builderAddress", builder.addressLine1 || "");
        setValue("builderemail", builder.emailId || "");
        setValue("builderphoneNumber", builder.phone || "");
        setValue("pin", builder.pin?.toString() || "");
        setValue("district", builder.district || "");
        setValue("stateUt", builder.state || "");
      }

      if (enclosers && Array.isArray(enclosers)) {
        const fileMapping = {
          "THIRD_PARTY_INSURANCE": "thirdPartyInsurance",
          "BUILDERS_CERTIFICATE": "builderCertificate",
          "INSTRUMENT_OF_SALE": "instrumentOfSale",
          "MORTGAGE_DETAILS": "mortgageDetails",
          "PREOTECTION_AND_INDEMNITY_INSURANCE_NO": "pniInsurance",
          "DECLARATION_OF_OWNERSHIP": "declarationOfOwnership",
          "CHALLAN": "challan"
        };

        enclosers.forEach(encloser => {
          const fieldName = fileMapping[encloser.documentType];
          if (fieldName && encloser.fileStoreId) {
            setFileStates(prev => ({
              ...prev,
              [fieldName]: {
                name: encloser.documentName,
                fileStoreId: encloser.fileStoreId
              }
            }));
            setValue(`${fieldName}FileStoreId`, encloser.fileStoreId);
          }
        });
      }

      setIsDataLoaded(true);
    }
  }, [registrationData, setValue, isDataLoaded]);

  useEffect(() => {
    if (registrationData?.vesselDetails && masterData && isDataLoaded) {
      const vesselDetails = registrationData.vesselDetails;

      const findValueCode = (apiValue, masterArray) => {
        if (!apiValue || !masterArray) return "";
        const found = masterArray.find(item =>
          item.valueLabel === apiValue || item.valueCode === apiValue
        );
        return found?.valueCode || "";
      };

      setValue("typeOfVessel", findValueCode(vesselDetails.typeOfVessel, masterData.vesselTypes));
      setValue("categoryOfVessel", findValueCode(vesselDetails.categoryOfVessel, masterData.vesselCategories));
      setValue("zone", findValueCode(vesselDetails.zone, masterData.zones));
      setValue("kindOfVessel", findValueCode(vesselDetails.kindOfVessel, masterData.kindOfVessels));
    }
  }, [masterData, registrationData, isDataLoaded, setValue]);

  const fetchAddressDetails = async (pin, prefix = "") => {
    if (isViewMode) return;
    try {
      loaderRef.current?.show();
      const URL = process.env.REACT_APP_PUBLIC_URL + URLS_ENDPOINT.APP_SERVICES.GET_DETAILS_BY_PINCODE + pin;
      const response = await getData(URL);

      if (response?.status === 200 && response?.data?.length > 0) {
        const data = response.data[0];
        if (prefix) {
          setValue(`${prefix}district`, data.district);
          setValue(`${prefix}stateUt`, data.stateName);
          clearErrors(`${prefix}district`);
          clearErrors(`${prefix}stateUt`);
        } else {
          setValue("district", data.district);
          setValue("stateUt", data.stateName);
          clearErrors("district");
          clearErrors("stateUt");
        }
        setValue("country", "India");
      } else {
        toast.info("No data found for this PIN code");
      }
    } catch (error) {
      toast.error("Error fetching address details");
    } finally {
      loaderRef.current?.hide();
    }
  };

  const handleDropdownChange = (name, value) => {
    if (isViewMode) return;
    setValue(name, value, { shouldValidate: true });
    clearErrors(name);
  };

  const handleFileChange = async (e, documentType) => {
    if (isViewMode) return;
    const file = e?.target?.files[0];
    if (!file) return;

    const maxSize = 10 * 1024 * 1024;
    if (file.size > maxSize) {
      toast.error("File size must be less than or equal to 10 MB.");
      e.target.value = null;
      return;
    }

    loaderRef.current?.show();

    try {
      const formData = new FormData();
      formData.append("file", file);

      const uploadUrl = BASE_IP + URLS_ENDPOINT.FILE_STORE_SERVICE.UPLOAD_FILE;
      const response = await postFORMdata(uploadUrl, formData);

      const fileStoreId = response?.data?.files[0]?.fileStoreId;

      if (fileStoreId) {
        const getStandardizedFileName = (docType) => {
          const fileNames = {
            "thirdPartyInsurance": "Third Party Insurance.pdf",
            "builderCertificate": "Builder's Certificate.pdf",
            "instrumentOfSale": "Instrument of Sale.pdf",
            "mortgageDetails": "Mortgage Details.pdf",
            "pniInsurance": "Protection & Indemnity Insurance.pdf",
            "declarationOfOwnership": "Declaration of Ownership.pdf",
            "challan": "Challan.pdf"
          };
          return fileNames[docType] || file.name;
        };

        const standardizedName = getStandardizedFileName(documentType);

        setFileStates(prev => ({
          ...prev,
          [documentType]: {
            name: standardizedName,
            fileStoreId,
            originalName: file.name
          }
        }));
        setValue(`${documentType}FileStoreId`, fileStoreId);
        toast.success("File uploaded successfully!");
      }
    } catch (err) {
      console.error("File upload failed:", err);
      toast.error("File upload failed. Please try again.");
      setFileStates(prev => ({ ...prev, [documentType]: null }));
      e.target.value = null;
    } finally {
      loaderRef.current?.hide();
    }
  };

  const handleFileDownload = async (fileStoreId, fileName, originalName) => {
    try {
      if (!fileStoreId) return;
      const url = `${BASE_IP}/filestore/v1/files/id?fileStoreId=${fileStoreId}`;

      loaderRef.current?.show();

      const accessToken = getFromLocalStorage("access_token");
      const tokenDetails = getFromLocalStorage("tokenDetails");
      const sessionState = tokenDetails
        ? JSON.parse(tokenDetails)?.session_state
        : "";

      const response = await axios({
        method: "GET",
        url: url,
        responseType: "blob",
        headers: {
          Authorization: `Bearer ${accessToken}`,
          "X-Request-ID": sessionState,
          Accept: "application/octet-stream",
        },
      });

      if (response.data) {
        let filename = fileName || "document";
        const contentDisposition = response.headers["content-disposition"];
        if (contentDisposition) {
          const filenameMatch = contentDisposition.match(
            /filename[^;=\n]*=((['"]).*?\2|[^;\n]*)/
          );
          if (filenameMatch && filenameMatch[1]) {
            filename = filenameMatch[1].replace(/['"]/g, "");
          }
        }

        const blob = new Blob([response.data], {
          type: response.headers["content-type"] || "application/octet-stream",
        });
        const blobUrl = window.URL.createObjectURL(blob);

        const link = document.createElement("a");
        link.href = blobUrl;
        link.setAttribute("download", filename);
        document.body.appendChild(link);
        link.click();

        setTimeout(() => {
          document.body.removeChild(link);
          window.URL.revokeObjectURL(blobUrl);
        }, 100);
      }
    } catch (error) {
      console.error("Error downloading file:", error);
      toast.error("Failed to download file. Please try again.");
    } finally {
      loaderRef.current?.hide();
    }
  };

  const getVesselNameSuggestions = async (value) => {
    if (isViewMode) return;
    try {
      const vesselName = value.trim();
      if (!vesselName) return;
      const applicationNo = registrationData?.vesselDetails?.applicationNo;
      const regId = registrationData?.vesselDetails?.id;
      if (!applicationNo || !regId) return;

      loaderRef.current?.show();

      const apiUrl = SERVICE_BASE.IWT_REGISTRATION + URLS_ENDPOINT.VESSEL_SERVICE.VESSELNAME_SEARCH + `?vesselName=${encodeURIComponent(vesselName)}`;

      const response = await postData(apiUrl, {
        vesselRegistrationId: regId,
        applicationNumber: applicationNo,
        vesselName: vesselName
      });

      setSuggestions([]);
      clearErrors("vesselName");
      loaderRef.current?.hide();

      return true;

    } catch (error) {
      const suggestions = error?.response?.data?.data?.suggestions || [];
      setSuggestions(suggestions);
      toast.error(error?.response?.data?.responseStatus?.message || "The Vessel is not available please try with another name");
      setError("vesselName", {
        type: "manual",
        message: t("The Vessel is not available please try with another name"),
      });
      loaderRef.current?.hide();

      return false;
    }
  };

  const checkVesselNameError = () => {
    if (errors?.vesselName) {
      return false;
    }
    return true;
  }

  const values = watch();

  useEffect(() => {
    if (isViewMode) return;
    const timer = setTimeout(() => {
      getVesselNameSuggestions(values.vesselName);
    }, 1000);

    return () => clearTimeout(timer);
  }, [values.vesselName, isViewMode]);

  const renderFileSection = (documentType, label) => {
    if (isViewMode) {
      return fileStates[documentType] ? (
        <div className="d-flex align-items-center justify-content-between p-3 border rounded bg-light">
          <span className="text-dark fw-medium">{fileStates[documentType].name}</span>
          <button
            type="button"
            className="btn btn-sm btn-primary rounded-circle d-flex align-items-center justify-content-center"
            onClick={() => handleFileDownload(
              fileStates[documentType].fileStoreId,
              fileStates[documentType].name,
              fileStates[documentType].originalName
            )}
            title="Download file"
            style={{ width: '32px', height: '32px', padding: '0' }}
          >
            <FaDownload size={12} />
          </button>
        </div>
      ) : (
        <div className="p-3 border rounded bg-light text-muted">
          No file uploaded
        </div>
      );
    }

    return (
      <div className="position-relative">
        <InputFile
          name={documentType}
          label={label}
          id={documentType}
          register={register(documentType, {
            required: !fileStates[documentType] && `${label} is required`,
          })}
          errors={errors}
          required={!fileStates[documentType]}
          clearErrors={clearErrors}
          onChange={(e) => handleFileChange(e, documentType)}
        />
        {fileStates[documentType] && (
          <div className="d-flex align-items-center mt-2">
            <small className="text-success me-2">✓ {fileStates[documentType].name}</small>
            <button
              type="button"
              className="btn btn-sm btn-primary rounded-circle d-flex align-items-center justify-content-center"
              onClick={() => handleFileDownload(
                fileStates[documentType].fileStoreId,
                fileStates[documentType].name,
                fileStates[documentType].originalName
              )}
              title="Download file"
              style={{ width: '24px', height: '24px', padding: '0' }}
            >
              <FaDownload size={10} />
            </button>
          </div>
        )}
      </div>
    );
  };

  return (
    <>
      <NewLoader ref={loaderRef} />
      <Container fluid className="p-0">
        <Card className="mb-1">
          <Card.Body>
            <form className="employee-form">
              <div className="d-flex align-items-center">
                <h6 style={{ color: "#2466BF" }}>{t("FORM-2")}</h6>
              </div>

              {/* APPLICANT DETAILS */}
              <Collapsible
                title={
                  <div className="d-flex align-items-center">
                    <FaUser className="me-2" style={{ color: "#2466BF" }} />
                    <span style={{ color: "#2466BF" }}>{t("APPLICANT DETAILS")}</span>
                    {(hasOwnerData || isViewMode) && (
                      <FaLock className="ms-2" style={{ color: "#6c757d" }} title="Fields are disabled in view mode or auto-populated" />
                    )}
                  </div>
                }
                defaultOpen={true}
              >
                <Card className="form-section-card">
                  <Card.Body>
                    <Row className="g-3">
                      <Col md={4}>
                        <InputText
                          name="ownerName"
                          label={t("Owner Name")}
                          placeholder={t("Enter owner name")}
                          required={!isViewMode}
                          register={{
                            ...register("ownerName", !isViewMode ? {
                              required: t("Owner Name is required"),
                              minLength: { value: 3, message: t("Minimum 3 characters required") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isFieldDisabled('ownerName')}
                          disabled={isFieldDisabled('ownerName')}
                          style={{
                            backgroundColor: isFieldDisabled('ownerName') ? '#e9ecef' : '#ffffff',
                            cursor: isFieldDisabled('ownerName') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="address"
                          label={t("Address")}
                          placeholder={t("Enter address")}
                          required={!isViewMode}
                          register={{
                            ...register("address", !isViewMode ? {
                              required: t("Address is required"),
                              minLength: { value: 5, message: t("Minimum 5 characters required") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isFieldDisabled('address')}
                          disabled={isFieldDisabled('address')}
                          style={{
                            backgroundColor: isFieldDisabled('address') ? '#e9ecef' : '#ffffff',
                            cursor: isFieldDisabled('address') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="ownerType"
                          label={t("Owner Type")}
                          placeholder={t("Enter owner type")}
                          required={!isViewMode}
                          register={{
                            ...register("ownerType", !isViewMode ? {
                              required: t("Owner Type is required"),
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isFieldDisabled('ownerType')}
                          disabled={isFieldDisabled('ownerType')}
                          style={{
                            backgroundColor: isFieldDisabled('ownerType') ? '#e9ecef' : '#ffffff',
                            cursor: isFieldDisabled('ownerType') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="builderEmail"
                          label={t("Email ID")}
                          type="email"
                          placeholder={t("Enter email address")}
                          required={!isViewMode}
                          register={{
                            ...register("builderEmail", !isViewMode ? {
                              required: t("Email is required"),
                              pattern: { value: CONSTANT_REGEX.EMAIL, message: t("Invalid email address") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isFieldDisabled('builderEmail')}
                          disabled={isFieldDisabled('builderEmail')}
                          style={{
                            backgroundColor: isFieldDisabled('builderEmail') ? '#e9ecef' : '#ffffff',
                            cursor: isFieldDisabled('builderEmail') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="applicantphoneNumber"
                          label={t("Phone Number")}
                          placeholder={t("Enter 10-digit phone number")}
                          required={!isViewMode}
                          maxlength={10}
                          register={{
                            ...register("applicantphoneNumber", !isViewMode ? {
                              required: t("Phone Number is required"),
                              pattern: { value: CONSTANT_REGEX.MOBILE, message: t("Phone number must be 10 digits") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isFieldDisabled('applicantphoneNumber')}
                          disabled={isFieldDisabled('applicantphoneNumber')}
                          style={{
                            backgroundColor: isFieldDisabled('applicantphoneNumber') ? '#e9ecef' : '#ffffff',
                            cursor: isFieldDisabled('applicantphoneNumber') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="ownerpin"
                          label={t("PIN")}
                          placeholder={t("Enter 6-digit PIN code")}
                          required={!isViewMode}
                          register={{
                            ...register("ownerpin", !isViewMode ? {
                              required: t("PIN Code is required"),
                              pattern: { value: /^[0-9]{6}$/, message: t("PIN must be 6 digits") },
                            } : {}),
                          }}
                          onChange={async (e) => {
                            const pinValue = e.target.value;
                            if (pinValue.length === 6 && !isFieldDisabled('ownerpin')) {
                              await fetchAddressDetails(pinValue, "owner");
                            }
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isFieldDisabled('ownerpin')}
                          disabled={isFieldDisabled('ownerpin')}
                          style={{
                            backgroundColor: isFieldDisabled('ownerpin') ? '#e9ecef' : '#ffffff',
                            cursor: isFieldDisabled('ownerpin') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="ownerdistrict"
                          label={t("District")}
                          required={!isViewMode}
                          placeholder={t("Auto-populated")}
                          register={{
                            ...register("ownerdistrict", !isViewMode ? { required: t("District is required") } : {}),
                          }}
                          errors={errors}
                          readOnly={true}
                          clearErrors={clearErrors}
                          style={{ backgroundColor: '#e9ecef', cursor: 'not-allowed' }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="ownerstateUt"
                          label={t("State/UT")}
                          required={!isViewMode}
                          placeholder={t("Auto-populated")}
                          register={{
                            ...register("ownerstateUt", !isViewMode ? { required: t("State/UT is required") } : {}),
                          }}
                          errors={errors}
                          readOnly={true}
                          clearErrors={clearErrors}
                          style={{ backgroundColor: '#e9ecef', cursor: 'not-allowed' }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="occupation"
                          label={t("Occupation")}
                          placeholder={t("Occupation")}
                          register={{ ...register("occupation") }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="originalPlaceOfBusiness"
                          label={t("Original Place of Business")}
                          placeholder={t("Original Place of Business")}
                          register={{ ...register("originalPlaceOfBusiness") }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                    </Row>
                  </Card.Body>
                </Card>
              </Collapsible>

              {/* VESSEL DETAILS */}
              <Collapsible
                title={
                  <div className="d-flex align-items-center">
                    <FaUser className="me-2" style={{ color: "#2466BF" }} />
                    <span style={{ color: "#2466BF" }}>{t("VESSEL DETAILS")}</span>
                    {(isViewMode || (isDataLoaded && !hasValue(registrationData?.vesselDetails?.registrationNumber))) && (
                      <FaLock className="ms-2" style={{ color: "#6c757d" }} title="Fields are disabled in view mode or until registration number is available" />
                    )}
                  </div>
                }
                defaultOpen={true}
              >
                <Card className="form-section-card">
                  <Card.Body>
                    <Row className="g-3">
                      <Col md={4}>
                        <InputText
                          name="nameOfMaster"
                          label={t("Name of Master")}
                          placeholder={t("Enter name of master")}
                          required={!isViewMode}
                          register={{
                            ...register("nameOfMaster", !isViewMode ? { required: t("Name of Master is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('nameOfMaster')}
                          disabled={isVesselFieldDisabled('nameOfMaster')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('nameOfMaster') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('nameOfMaster') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="certificateNo"
                          label={t("Certificate No.")}
                          placeholder={t("Enter certificate number")}
                          required={!isViewMode}
                          register={{
                            ...register("certificateNo", !isViewMode ? { required: t("Certificate No. is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('certificateNo')}
                          disabled={isVesselFieldDisabled('certificateNo')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('certificateNo') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('certificateNo') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="nameOfRegistry"
                          label={t("Name of Registry")}
                          placeholder={t("Enter name of registry")}
                          required={!isViewMode}
                          register={{
                            ...register("nameOfRegistry", !isViewMode ? { required: t("Name of Registry is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('nameOfRegistry')}
                          disabled={isVesselFieldDisabled('nameOfRegistry')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('nameOfRegistry') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('nameOfRegistry') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="registrationNo"
                          label={t("Registration No.")}
                          placeholder={t("Enter registration number")}
                          required={!isViewMode && hasRegistrationNumber()}
                          register={{
                            ...register("registrationNo", 
                              (!isViewMode && hasRegistrationNumber()) ? { required: t("Registration No. is required") } : {}
                            ),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('registrationNo')}
                          disabled={isVesselFieldDisabled('registrationNo')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('registrationNo') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('registrationNo') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4} className="position-relative">
                        <InputText
                          name="vesselName"
                          label={t("Vessel Name")}
                          placeholder={t("Enter vessel name")}
                          required={!isViewMode}
                          register={{
                            ...register("vesselName", !isViewMode ? {
                              required: t("Vessel Name is required"),
                              validate: async (value) => {
                                const isValid = checkVesselNameError();
                                if (!isValid) {
                                  return t("The Vessel is not available please try with another name");
                                }
                              },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('vesselName')}
                          disabled={isVesselFieldDisabled('vesselName')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('vesselName') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('vesselName') ? 'not-allowed' : 'text'
                          }}
                        />
                        {suggestions.length > 0 && !isViewMode && (
                          <div
                            className={`${styles.suggestionCard} border rounded mt-1 bg-white w-100`}
                            style={{
                              top: `${errors?.vesselName ? (errors?.vesselName?.type === "validate" ? "90%" : "60%") : '60%'}`,
                            }}
                          >
                            <div className="px-3 py-2 border-bottom bg-light" style={{ fontWeight: "600", fontSize: "0.875rem", color: "#545D69" }}>
                              {t("Suggestions")}
                            </div>
                            {suggestions.map((suggestion, index) => (
                              <div
                                key={suggestion + index}
                                className={`${styles.suggestionItem} px-3 py-2 suggestion-item`}
                                onClick={() => {
                                  setValue("vesselName", suggestion);
                                  setSuggestions([]);
                                  clearErrors("vesselName");
                                }}
                              >
                                {suggestion}
                              </div>
                            ))}
                          </div>
                        )}
                      </Col>

                      <Col md={4}>
                        <InputSelect
                          name="kindOfVessel"
                          label={t("Kind of Vessel")}
                          required={!isViewMode}
                          placeholder={t("Select kind of vessel")}
                          options={vesselKindOptions}
                          register={register("kindOfVessel", !isViewMode ? { required: t("Kind of Vessel is required") } : {})}
                          errors={errors}
                          value={watch("kindOfVessel") || ""}
                          onChange={(val) => handleDropdownChange("kindOfVessel", val)}
                          clearErrors={clearErrors}
                          disabled={isVesselFieldDisabled('kindOfVessel')}
                        />
                      </Col>
                      <Col md={4}>
                        <InputSelect
                          name="typeOfVessel"
                          label={t("Type of Vessel")}
                          required={!isViewMode}
                          placeholder={t("Select type of vessel")}
                          options={vesselTypeOptions}
                          register={register("typeOfVessel", !isViewMode ? { required: t("Type of Vessel is required") } : {})}
                          errors={errors}
                          value={watch("typeOfVessel") || ""}
                          onChange={(val) => handleDropdownChange("typeOfVessel", val)}
                          clearErrors={clearErrors}
                          disabled={isVesselFieldDisabled('typeOfVessel')}
                        />
                      </Col>
                      <Col md={4}>
                        <InputSelect
                          name="categoryOfVessel"
                          label={t("Category of Vessel")}
                          required={!isViewMode}
                          placeholder={t("Select category of vessel")}
                          options={categoryOptions}
                          register={register("categoryOfVessel", !isViewMode ? { required: t("Category of Vessel is required") } : {})}
                          errors={errors}
                          value={watch("categoryOfVessel") || ""}
                          onChange={(val) => handleDropdownChange("categoryOfVessel", val)}
                          clearErrors={clearErrors}
                          disabled={isVesselFieldDisabled('categoryOfVessel')}
                        />
                      </Col>
                      <Col md={4}>
                        <InputSelect
                          name="zone"
                          label={t("Zone")}
                          required={!isViewMode}
                          placeholder={t("Select zone")}
                          options={zoneOptions}
                          register={register("zone", !isViewMode ? { required: t("Zone is required") } : {})}
                          errors={errors}
                          value={watch("zone") || ""}
                          onChange={(val) => handleDropdownChange("zone", val)}
                          clearErrors={clearErrors}
                          disabled={isVesselFieldDisabled('zone')}
                        />
                      </Col>
                      <Col md={4}>
                        <InputSelect
                          name="mortgage"
                          label={t("Mortgage")}
                          required={!isViewMode}
                          placeholder={t("Select Mortgage")}
                          options={[
                            { value: "yes", label: "Yes" },
                            { value: "no", label: "No" },
                          ]}
                          register={register("mortgage", !isViewMode ? { required: t("Mortgage is required") } : {})}
                          errors={errors}
                          value={watch("mortgage") || ""}
                          onChange={(val) => handleDropdownChange("mortgage", val)}
                          clearErrors={clearErrors}
                          disabled={isVesselFieldDisabled('mortgage')}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="placeOfBuild"
                          label={t("Place of Build")}
                          placeholder={t("Enter place of build")}
                          required={!isViewMode}
                          register={{
                            ...register("placeOfBuild", !isViewMode ? { required: t("Place of Build is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('placeOfBuild')}
                          disabled={isVesselFieldDisabled('placeOfBuild')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('placeOfBuild') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('placeOfBuild') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="detailsOfInsurance"
                          label={t("Details of Insurance")}
                          placeholder={t("Enter details of insurance")}
                          required={!isViewMode}
                          register={{
                            ...register("detailsOfInsurance", !isViewMode ? { required: t("Details of Insurance is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('detailsOfInsurance')}
                          disabled={isVesselFieldDisabled('detailsOfInsurance')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('detailsOfInsurance') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('detailsOfInsurance') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="vesselyearOfBuild"
                          label={t("Year of Build")}
                          placeholder={t("Enter year of build")}
                          required={!isViewMode}
                          register={{
                            ...register("vesselyearOfBuild", !isViewMode ? {
                              required: t("Year of Build is required"),
                              pattern: { value: /^[0-9]{4}$/, message: t("Enter a valid year (YYYY)") },
                              min: { value: 1900, message: t("Year must be after 1900") },
                              max: { value: new Date().getFullYear(), message: t("Year cannot be in the future") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('vesselyearOfBuild')}
                          disabled={isVesselFieldDisabled('vesselyearOfBuild')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('vesselyearOfBuild') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('vesselyearOfBuild') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="protectionAndIndemnityClubNo"
                          label={t("Protection & Indemnity Club No.")}
                          placeholder={t("Enter PNI Club No.")}
                          required={!isViewMode}
                          register={{
                            ...register("protectionAndIndemnityClubNo", !isViewMode ? {
                              required: t("PNI Club No. is required"),
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('protectionAndIndemnityClubNo')}
                          disabled={isVesselFieldDisabled('protectionAndIndemnityClubNo')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('protectionAndIndemnityClubNo') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('protectionAndIndemnityClubNo') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="protectionAndIndemnityClubName"
                          label={t("Protection & Indemnity Club Name")}
                          placeholder={t("Enter PNI Club Name")}
                          required={!isViewMode}
                          register={{
                            ...register("protectionAndIndemnityClubName", !isViewMode ? {
                              required: t("PNI Club Name is required"),
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isVesselFieldDisabled('protectionAndIndemnityClubName')}
                          disabled={isVesselFieldDisabled('protectionAndIndemnityClubName')}
                          style={{
                            backgroundColor: isVesselFieldDisabled('protectionAndIndemnityClubName') ? '#e9ecef' : '#ffffff',
                            cursor: isVesselFieldDisabled('protectionAndIndemnityClubName') ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                    </Row>
                  </Card.Body>
                </Card>
              </Collapsible>

              {/* ENGINE DETAILS */}
              <Collapsible
                title={
                  <div className="d-flex align-items-center">
                    <FaUser className="me-2" style={{ color: "#2466BF" }} />
                    <span style={{ color: "#2466BF" }}>{t("Engine Details")}</span>
                    {isViewMode && (
                      <FaLock className="ms-2" style={{ color: "#6c757d" }} title="Fields are disabled in view mode" />
                    )}
                  </div>
                }
                defaultOpen={true}
              >
                <Card className="form-section-card">
                  <Card.Body>
                    <Row className="g-3">
                      <Col md={4}>
                        <InputText
                          name="insuranceValidUpto"
                          label={t("Insurance Valid Upto")}
                          placeholder={t("Enter insurance valid upto")}
                          required={!isViewMode}
                          register={{
                            ...register("insuranceValidUpto", !isViewMode ? { required: t("Insurance Valid Upto is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="yearOfBuild"
                          label={t("Year of Build")}
                          placeholder={t("Enter year of build")}
                          required={!isViewMode}
                          register={{
                            ...register("yearOfBuild", !isViewMode ? {
                              required: t("Year of Build is required"),
                              pattern: { value: /^[0-9]{4}$/, message: t("Enter a valid year (YYYY)") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="typeOfEngine"
                          label={t("Type of Engine")}
                          placeholder={t("Enter type of engine")}
                          required={!isViewMode}
                          register={{
                            ...register("typeOfEngine", !isViewMode ? { required: t("Type of Engine is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="engineMake"
                          label={t("Engine Make")}
                          placeholder={t("Enter engine make")}
                          required={!isViewMode}
                          register={{
                            ...register("engineMake", !isViewMode ? { required: t("Engine Make is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="engineMakerAddress"
                          label={t("Engine Maker Address")}
                          placeholder={t("Enter engine maker address")}
                          required={!isViewMode}
                          register={{
                            ...register("engineMakerAddress", !isViewMode ? { required: t("Engine Maker Address is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="enginepin"
                          label={t("PIN")}
                          placeholder={t("Enter 6-digit PIN code")}
                          required={!isViewMode}
                          register={{
                            ...register("enginepin", !isViewMode ? {
                              required: t("PIN Code is required"),
                              pattern: { value: /^[0-9]{6}$/, message: t("PIN must be 6 digits") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="enginephoneNumber"
                          label={t("Phone Number")}
                          placeholder={t("Enter 10-digit phone number")}
                          required={!isViewMode}
                          maxlength={10}
                          register={{
                            ...register("enginephoneNumber", !isViewMode ? {
                              required: t("Phone Number is required"),
                              pattern: { value: CONSTANT_REGEX.MOBILE, message: t("Phone number must be 10 digits") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="engineemail"
                          label={t("Email ID")}
                          type="email"
                          placeholder={t("Enter email address")}
                          required={!isViewMode}
                          register={{
                            ...register("engineemail", !isViewMode ? {
                              required: t("Email is required"),
                              pattern: { value: CONSTANT_REGEX.EMAIL, message: t("Invalid email address") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="horsepower"
                          label={t("Horsepower")}
                          type="number"
                          placeholder={t("Enter horsepower")}
                          required={!isViewMode}
                          register={{
                            ...register("horsepower", !isViewMode ? {
                              required: t("Horsepower is required"),
                              min: { value: 1, message: t("Horsepower must be positive") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="speed"
                          label={t("Speed")}
                          placeholder={t("Enter speed")}
                          required={!isViewMode}
                          register={{
                            ...register("speed", !isViewMode ? { required: t("Speed is required") } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="yearOfMake"
                          label={t("Year of Make")}
                          placeholder={t("Enter year of make")}
                          required={!isViewMode}
                          register={{
                            ...register("yearOfMake", !isViewMode ? {
                              required: t("Year of Make is required"),
                              pattern: { value: /^[0-9]{4}$/, message: t("Enter a valid year (YYYY)") },
                            } : {}),
                          }}
                          errors={errors}
                          clearErrors={clearErrors}
                          readOnly={isViewMode}
                          disabled={isViewMode}
                          style={{
                            backgroundColor: isViewMode ? '#e9ecef' : '#ffffff',
                            cursor: isViewMode ? 'not-allowed' : 'text'
                          }}
                        />
                      </Col>
                    </Row>
                  </Card.Body>
                </Card>
              </Collapsible>

              {/* BUILDER DETAILS */}
              {shouldShowBuilderDetails() && (
                <Collapsible
                  title={
                    <div className="d-flex align-items-center">
                      <FaUser className="me-2" style={{ color: "#2466BF" }} />
                      <span style={{ color: "#2466BF" }}>{t("Builder Details")}</span>
                      {(hasBuilderData || isViewMode) && (
                        <FaLock className="ms-2" style={{ color: "#6c757d" }} title="Fields are disabled in view mode or auto-populated" />
                      )}
                    </div>
                  }
                  defaultOpen={true}
                >
                  <Card className="form-section-card">
                    <Card.Body>
                      <Row className="g-3">
                        <Col md={4}>
                          <InputText
                            name="builderName"
                            label={t("Name of Builder")}
                            placeholder={t("Enter name of builder")}
                            required={!isViewMode}
                            register={{
                              ...register("builderName", !isViewMode ? { required: t("Name of Builder is required") } : {}),
                            }}
                            errors={errors}
                            clearErrors={clearErrors}
                            readOnly={isBuilderFieldDisabled('builderName')}
                            disabled={isBuilderFieldDisabled('builderName')}
                            style={{
                              backgroundColor: isBuilderFieldDisabled('builderName') ? '#e9ecef' : '#ffffff',
                              cursor: isBuilderFieldDisabled('builderName') ? 'not-allowed' : 'text'
                            }}
                          />
                        </Col>
                        <Col md={4}>
                          <InputText
                            name="builderAddress"
                            label={t("Address of Builder")}
                            placeholder={t("Enter address of builder")}
                            required={!isViewMode}
                            register={{
                              ...register("builderAddress", !isViewMode ? { required: t("Address of Builder is required") } : {}),
                            }}
                            errors={errors}
                            clearErrors={clearErrors}
                            readOnly={isBuilderFieldDisabled('builderAddress')}
                            disabled={isBuilderFieldDisabled('builderAddress')}
                            style={{
                              backgroundColor: isBuilderFieldDisabled('builderAddress') ? '#e9ecef' : '#ffffff',
                              cursor: isBuilderFieldDisabled('builderAddress') ? 'not-allowed' : 'text'
                            }}
                          />
                        </Col>
                        <Col md={4}>
                          <InputText
                            name="builderemail"
                            label={t("Email ID")}
                            type="email"
                            placeholder={t("Enter email address")}
                            required={!isViewMode}
                            register={{
                              ...register("builderemail", !isViewMode ? {
                                required: t("Email is required"),
                                pattern: { value: CONSTANT_REGEX.EMAIL, message: t("Invalid email address") },
                              } : {}),
                            }}
                            errors={errors}
                            clearErrors={clearErrors}
                            readOnly={isBuilderFieldDisabled('builderemail')}
                            disabled={isBuilderFieldDisabled('builderemail')}
                            style={{
                              backgroundColor: isBuilderFieldDisabled('builderemail') ? '#e9ecef' : '#ffffff',
                              cursor: isBuilderFieldDisabled('builderemail') ? 'not-allowed' : 'text'
                            }}
                          />
                        </Col>
                        <Col md={4}>
                          <InputText
                            name="builderphoneNumber"
                            label={t("Phone Number")}
                            placeholder={t("Enter 10-digit phone number")}
                            required={!isViewMode}
                            maxlength={10}
                            register={{
                              ...register("builderphoneNumber", !isViewMode ? {
                                required: t("Phone Number is required"),
                                pattern: { value: CONSTANT_REGEX.MOBILE, message: t("Phone number must be 10 digits") },
                              } : {}),
                            }}
                            errors={errors}
                            clearErrors={clearErrors}
                            readOnly={isBuilderFieldDisabled('builderphoneNumber')}
                            disabled={isBuilderFieldDisabled('builderphoneNumber')}
                            style={{
                              backgroundColor: isBuilderFieldDisabled('builderphoneNumber') ? '#e9ecef' : '#ffffff',
                              cursor: isBuilderFieldDisabled('builderphoneNumber') ? 'not-allowed' : 'text'
                            }}
                          />
                        </Col>
                        <Col md={4}>
                          <InputText
                            name="pin"
                            label={t("PIN")}
                            placeholder={t("Enter 6-digit PIN code")}
                            required={!isViewMode}
                          
                            register={{
                              ...register("pin", !isViewMode ? {
                                required: t("PIN Code is required"),
                                pattern: { value: /^[0-9]{6}$/, message: t("PIN must be 6 digits") },
                              } : {}),
                            }}
                            onChange={(e) => {
                              const pinValue = e.target.value;
                              if (pinValue.length === 6 && !isBuilderFieldDisabled('pin')) {
                                fetchAddressDetails(pinValue);
                              }
                            }}
                            errors={errors}
                            clearErrors={clearErrors}
                            readOnly={isBuilderFieldDisabled('pin')}
                            disabled={isBuilderFieldDisabled('pin')}
                            style={{
                              backgroundColor: isBuilderFieldDisabled('pin') ? '#e9ecef' : '#ffffff',
                              cursor: isBuilderFieldDisabled('pin') ? 'not-allowed' : 'text'
                            }}
                          />
                        </Col>
                        <Col md={4}>
                          <InputText
                            name="district"
                            label={t("District")}
                            required={!isViewMode}
                            placeholder={t("Auto-populated")}
                            register={{
                              ...register("district", !isViewMode ? { required: t("District is required") } : {}),
                            }}
                            errors={errors}
                            readOnly={true}
                            clearErrors={clearErrors}
                            style={{ backgroundColor: '#e9ecef', cursor: 'not-allowed' }}
                          />
                        </Col>
                        <Col md={4}>
                          <InputText
                            name="stateUt"
                            label={t("State/UT")}
                            required={!isViewMode}
                            placeholder={t("Auto-populated")}
                            register={{
                              ...register("stateUt", !isViewMode ? { required: t("State/UT is required") } : {}),
                            }}
                            errors={errors}
                            readOnly={true}
                            clearErrors={clearErrors}
                            style={{ backgroundColor: '#e9ecef', cursor: 'not-allowed' }}
                          />
                        </Col>
                      </Row>
                    </Card.Body>
                  </Card>
                </Collapsible>
              )}

              {/* FORM-3 - DECLARATION OF OWNERSHIP */}
              <div className="d-flex align-items-center mb-3 mt-4">
                <h6 style={{ color: "#2466BF" }}>{t("FORM-3")}</h6>
              </div>
              <Collapsible
                title={
                  <div className="d-flex align-items-center">
                    <FaUser className="me-2" style={{ color: "#2466BF" }} />
                    <span style={{ color: "#2466BF" }}>{t("DECLARATION OF OWNERSHIP")}</span>
                  </div>
                }
                defaultOpen={true}
              >
                <Card className="form-section-card">
                  <Card.Body>
                    <Row className="g-3">
                      <Col md={6}>
                        <div className="mb-3">
                          <label className="form-label fw-semibold">{t("Third Party Insurance")}</label>
                          {renderFileSection("thirdPartyInsurance", t("Third Party Insurance"))}
                        </div>
                      </Col>
                      <Col md={6}>
                        <div className="mb-3">
                          <label className="form-label fw-semibold">{t("Builder's Certificate")}</label>
                          {renderFileSection("builderCertificate", t("Builder's Certificate"))}
                        </div>
                      </Col>
                      <Col md={6}>
                        <div className="mb-3">
                          <label className="form-label fw-semibold">{t("Instrument of Sale")}</label>
                          {renderFileSection("instrumentOfSale", t("Instrument of Sale"))}
                        </div>
                      </Col>
                      <Col md={6}>
                        <div className="mb-3">
                          <label className="form-label fw-semibold">{t("Mortgage Details")}</label>
                          {renderFileSection("mortgageDetails", t("Mortgage Details"))}
                        </div>
                      </Col>
                      <Col md={6}>
                        <div className="mb-3">
                          <label className="form-label fw-semibold">{t("Protection & Indemnity Insurance No.")}</label>
                          {renderFileSection("pniInsurance", t("Protection & Indemnity Insurance No."))}
                        </div>
                      </Col>
                      <Col md={6}>
                        <div className="mb-3">
                          <label className="form-label fw-semibold">{t("Declaration of Ownership")}</label>
                          {renderFileSection("declarationOfOwnership", t("Declaration of Ownership"))}
                        </div>
                      </Col>
                      <Col md={6}>
                        <div className="mb-3">
                          <label className="form-label fw-semibold">{t("Challan")}</label>
                          {renderFileSection("challan", t("Challan"))}
                        </div>
                      </Col>
                    </Row>
                  </Card.Body>
                </Card>
              </Collapsible>
            </form>
          </Card.Body>
        </Card>
      </Container>
    </>
  );
});

export default RegistrationApplicationForm;
