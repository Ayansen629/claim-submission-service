// Update the error handling in InputText
const errorMessage = errors?.[name]?.message 
  || (name.includes('.') && name.split('.').reduce((obj, key) => (obj && obj[key]) ? obj[key]?.message : null, errors);

console.log("errorMessagetext", errorMessage);
