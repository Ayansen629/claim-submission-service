import React, { useRef } from "react";
import { Form } from "react-bootstrap";
import { FaUpload, FaFile, FaFilePdf, FaFileImage, FaCheckCircle, FaTimesCircle } from "react-icons/fa";
import { useFormContext } from "react-hook-form";

const InputFile = ({
  label,
  required = false,
  name,
  accept = ".pdf,.jpg,.jpeg,.png",
  disabled,
  className = "",
  style = {},
  defaultValue,
  autoFocus = false
}) => {
  const { register, watch, setValue, formState: { errors } } = useFormContext();
  const fileInputRef = useRef(null);
  const hasError = errors && errors[name];
  const watchedValue = watch(name);

  let fileName = "";
  let fileExtension = "";

  if (watchedValue instanceof File) {
    fileName = watchedValue.originalName || watchedValue.name;
    if (!fileName.includes('.')) {
      fileExtension = watchedValue.name.substring(watchedValue.name.lastIndexOf('.'));
      fileName = fileName + fileExtension;
    }
  } else if (typeof watchedValue === "string" && watchedValue.trim() !== "") {
    fileName = watchedValue;
  } else if (defaultValue) {
    if (defaultValue instanceof File) {
      fileName = defaultValue.originalName || defaultValue.name;
      if (!fileName.includes('.')) {
        fileExtension = defaultValue.name.substring(defaultValue.name.lastIndexOf('.'));
        fileName = fileName + fileExtension;
      }
    } else if (typeof defaultValue === "string") {
      fileName = defaultValue;
    }
  }

  const handleFileChange = (e) => {
    if (e.target.files && e.target.files[0]) {
      const file = e.target.files[0];
      const extension = file.name.substring(file.name.lastIndexOf('.'));
      const newFileName = `${label.replace(/\s+/g, '_')}_${Date.now()}${extension}`;
      file.originalName = newFileName;
      setValue(name, file, { shouldValidate: true });
    }
  };

  const handleRemoveFile = () => {
    setValue(name, null, { shouldValidate: true });
    if (fileInputRef.current) {
      fileInputRef.current.value = '';
    }
  };

  const getFileIcon = () => {
    if (!fileName) return <FaFile color="#0d6efd" />;
    const extension = fileName.split('.').pop()?.toLowerCase();
    if (extension === 'pdf') return <FaFilePdf color="#dc3545" />;
    if (['jpg', 'jpeg', 'png', 'gif'].includes(extension || '')) return <FaFileImage color="#28a745" />;
    return <FaFile color="#0d6efd" />;
  };

  return (
    <Form.Group className={`mb-4 ${className}`} controlId={name}>
      <Form.Label className="fw-bold text-dark">
        {label}
        {required && <span className="text-danger ms-1">*</span>}
      </Form.Label>
      <div
        className={`file-upload border rounded p-3 ${hasError ? 'border-danger' : fileName ? 'border-success' : ''}`}
        style={{
          ...style,
          backgroundColor: hasError ? '#fff8f8' : 'white',
          borderColor: hasError ? '#dc3545' : fileName ? '#28a745' : '#ced4da',
          borderRadius: '8px',
          transition: 'all 0.3s ease'
        }}
      >
        <div className="d-flex align-items-center">
          <label
            htmlFor={name}
            className={`file-label btn ${hasError ? 'btn-outline-danger' : 'btn-primary'} mb-0 d-inline-flex align-items-center`}
            style={{
              borderRadius: '6px',
              padding: '8px 16px',
              cursor: disabled ? 'not-allowed' : 'pointer',
              boxShadow: '0 2px 5px rgba(0,0,0,0.1)',
              transition: 'all 0.3s ease',
              minWidth: '120px',
              color: 'white',
              flexShrink: 0,
              opacity: disabled ? 0.65 : 1
            }}
          >
            <FaUpload className="file-icon me-2" />
            <span className="file-text">Choose File</span>
          </label>
          <div
            className={`file-name-container ms-3 flex-grow-1 border rounded px-3 py-2 ${fileName ? 'bg-light-success' : 'bg-light'} d-flex align-items-center justify-content-between ${hasError ? 'border-danger' : fileName ? 'border-success' : ''}`}
            style={{
              backgroundColor: fileName ? '#f0f9f0' : '#f8f9fa',
              overflow: 'hidden',
              minHeight: '50px'
            }}
          >
            {fileName ? (
              <div className="d-flex align-items-center flex-grow-1 overflow-hidden">
                <div className="d-flex align-items-center flex-grow-1 overflow-hidden">
                  <FaCheckCircle className="text-success me-2 flex-shrink-0" />
                  <div className="overflow-hidden">
                    <span className="text-success fw-bold" style={{ fontSize: '0.8rem' }}>Selected File:</span>
                    <div className="d-flex align-items-center">
                      <span className="file-icon-preview me-2 flex-shrink-0">{getFileIcon()}</span>
                      <span 
                        className="text-dark fw-semibold" 
                        style={{ 
                          overflow: 'hidden', 
                          textOverflow: 'ellipsis', 
                          whiteSpace: 'nowrap' 
                        }}
                      >
                        {fileName}
                      </span>
                    </div>
                  </div>
                </div>
                {!disabled && (
                  <button 
                    type="button" 
                    className="btn btn-sm btn-outline-danger ms-2"
                    onClick={handleRemoveFile}
                    style={{
                      flexShrink: 0,
                      padding: '0.25rem 0.5rem'
                    }}
                  >
                    <FaTimesCircle />
                  </button>
                )}
              </div>
            ) : (
              <span className="text-muted">No file chosen</span>
            )}
          </div>
        </div>
        <input
          ref={(e) => {
            fileInputRef.current = e;
            if (e) {
              register(name, {
                required: required ? `${label} is required` : false,
                validate: {
                  lessThan2MB: files => 
                    !files[0] || files[0].size < 2 * 1024 * 1024 || 'File size should be less than 2MB',
                  acceptedFormats: files => 
                    !files[0] || 
                    ['application/pdf', 'image/jpeg', 'image/png', 'image/jpg'].includes(files[0].type) || 
                    'Only PDF, JPG or PNG files are allowed'
                }
              })(e);
            }
          }}
          type="file"
          id={name}
          onChange={handleFileChange}
          accept={accept}
          disabled={disabled}
          className="file-input d-none"
        />
      </div>
      <Form.Text className="text-muted small mt-1">
        Upload PDF, JPG or PNG file (Max size: 2MB)
      </Form.Text>
      {hasError && (
        <Form.Text className="text-danger small mt-1 d-block">
          {errors[name]?.message}
        </Form.Text>
      )}
    </Form.Group>
  );
};

export default InputFile;
