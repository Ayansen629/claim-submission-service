import { useEffect } from "react";
import { Form, useFieldArray, useFormContext } from "react-hook-form";
import { Card, Row, Col, Button } from "react-bootstrap";
import InputSelect from "../../../common/InputSelect.tsx";
import InputText from "../../../common/InputText.tsx";
import Collapsible from "../../../common/Collapsible.js";
import { FaPlus, FaTrash, FaUser, FaMapMarkerAlt, FaGraduationCap, FaBriefcase, FaFileUpload } from "react-icons/fa";
import { CONSTANT_REGEX } from "../../../../utils/constants.ts";
import { useMasterData } from "../../../context/MasterDataContext.js";
import { URLS_ENDPOINT } from "../../../../utils/urlsConstants.ts";
import { getData } from "../../../../services/http.service.ts";

const EmployeeRegistrationForm = ({ onSubmit, onCancel }) => {
  const { 
    control, 
    register, 
    setValue, 
    watch, 
    handleSubmit, 
    formState: { errors, isSubmitted } 
  } = useFormContext();
  
  const { fields, append, remove } = useFieldArray({
    control,
    name: "educationDetails"
  });

  const watchPin = watch("vesselUserDetails.pin");

  const { masterData } = useMasterData();

  useEffect(() => {
    const fetchAddressDetails = async (pin) => {
      if (pin?.length === 6) {
        try {
          const URL = process.env.REACT_APP_PUBLIC_URL + URLS_ENDPOINT.APP_SERVICES.GET_DETAILS_BY_PINCODE + pin;
          const response = await getData(URL);
          if (response?.status === 200 && response?.data?.length > 0) {
            setValue('vesselUserDetails.district', response?.data[0]?.district);
            setValue('vesselUserDetails.stateUt', response?.data[0]?.stateName);
            setValue("vesselUserDetails.country", "India");
          } else {
            setValue("vesselUserDetails.district", "");
            setValue("vesselUserDetails.stateUt", "");
            setValue("vesselUserDetails.country", "");
          }
        } catch (error) {
          setValue("vesselUserDetails.district", "");
          setValue("vesselUserDetails.stateUt", "");
          setValue("vesselUserDetails.country", "");
        }
      }
    };

    if (watchPin) {
      fetchAddressDetails(watchPin);
    }
  }, [watchPin, setValue]);

  // Helper function to check if field has error
  const hasError = (fieldName) => {
    const fieldNames = fieldName.split('.');
    let errorObj = errors;
    
    for (const name of fieldNames) {
      if (errorObj && errorObj[name]) {
        errorObj = errorObj[name];
      } else {
        return false;
      }
    }
    
    return !!errorObj;
  };

  return (
    <Form onSubmit={handleSubmit(onSubmit)}>
      <div className="employee-registration-form">
        {/* Personal Details Section */}
        <Collapsible
          title={
            <div className="d-flex align-items-center">
              <FaUser className="me-2" style={{ color: "#2466BF" }} />
              <span style={{ color: "#2466BF" }}>Personal Details</span>
            </div>
          }
          defaultOpen={true}
        >
          <Card className="form-section-card">
            <Card.Body>
              <Row className="g-3">
                <Col md={4}>
                  <InputText
                    name="vesselUserDetails.firstName"
                    label="First Name"
                    placeholder="Enter first name"
                    register={register("vesselUserDetails.firstName", { 
                      required: "First Name is required" 
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("vesselUserDetails.firstName") ? '#dc3545' : undefined,
                      backgroundColor: hasError("vesselUserDetails.firstName") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={4}>
                  <InputText
                    name="vesselUserDetails.lastName"
                    label="Last Name"
                    placeholder="Enter last name"
                    register={register("vesselUserDetails.lastName", { 
                      required: "Last Name is required" 
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("vesselUserDetails.lastName") ? '#dc3545' : undefined,
                      backgroundColor: hasError("vesselUserDetails.lastName") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={4}>
                  <InputText
                    name="vesselUserDetails.email"
                    label="Email ID"
                    type="email"
                    placeholder="Enter email address"
                    register={register("vesselUserDetails.email", {
                      required: "Email is required",
                      pattern: {
                        value: CONSTANT_REGEX.EMAIL,
                        message: "Invalid email address"
                      }
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("vesselUserDetails.email") ? '#dc3545' : undefined,
                      backgroundColor: hasError("vesselUserDetails.email") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={4}>
                  <InputText
                    name="vesselUserDetails.phoneNumber"
                    label="Phone Number"
                    placeholder="Enter 10-digit phone number"
                    register={register("vesselUserDetails.phoneNumber", {
                      required: "Phone Number is required",
                      pattern: {
                        value: CONSTANT_REGEX.MOBILE,
                        message: "Invalid phone number"
                      }
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("vesselUserDetails.phoneNumber") ? '#dc3545' : undefined,
                      backgroundColor: hasError("vesselUserDetails.phoneNumber") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={4}>
                  <InputSelect
                    name="vesselUserDetails.gender"
                    label="Gender"
                    placeholder="-- Select gender --"
                    options={masterData?.genders?.map(type => ({
                      value: type.valueCode,
                      label: type.valueLabel
                    })) || []}
                    register={register}
                    errors={errors}
                    control={control}
                    required
                    rules={{ required: "Gender is required" }}
                    value={watch("vesselUserDetails.gender") || ""}
                    onChange={val => setValue("vesselUserDetails.gender", val, { shouldValidate: true })}
                    style={{
                      borderColor: hasError("vesselUserDetails.gender") ? '#dc3545' : undefined,
                      backgroundColor: hasError("vesselUserDetails.gender") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={4}>
                  <InputText
                    name="vesselUserDetails.dateOfBirth"
                    label="Date of Birth"
                    type="date"
                    placeholder="Select date of birth"
                    register={register("vesselUserDetails.dateOfBirth", { 
                      required: "Date of Birth is required" 
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("vesselUserDetails.dateOfBirth") ? '#dc3545' : undefined,
                      backgroundColor: hasError("vesselUserDetails.dateOfBirth") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
              </Row>
            </Card.Body>
          </Card>
        </Collapsible>

        {/* Address Details Section */}
        <Collapsible
          title={
            <div className="d-flex align-items-center">
              <FaMapMarkerAlt className="me-2" style={{ color: "#2466BF" }} />
              <span style={{ color: "#2466BF" }}>Address Details</span>
            </div>
          }
        >
          <Card className="form-section-card">
            <Card.Body>
              <Row className="g-3">
                <Col md={6}>
                  <InputText
                    name="vesselUserDetails.addressLine1"
                    label="Address Line-1"
                    placeholder="Enter address line 1"
                    register={register("vesselUserDetails.addressLine1", { 
                      required: "Address Line-1 is required" 
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("vesselUserDetails.addressLine1") ? '#dc3545' : undefined,
                      backgroundColor: hasError("vesselUserDetails.addressLine1") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={6}>
                  <InputText
                    name="vesselUserDetails.addressLine2"
                    label="Address Line-2"
                    placeholder="Enter address line 2 (optional)"
                    register={register("vesselUserDetails.addressLine2")}
                    errors={errors}
                  />
                </Col>
                <Col md={3}>
                  <InputText
                    name="vesselUserDetails.pin"
                    label="PIN"
                    placeholder="Enter 6-digit PIN code"
                    register={register("vesselUserDetails.pin", {
                      required: "PIN Code is required",
                      pattern: {
                        value: /^\d{6}$/,
                        message: "Invalid PIN code (must be 6 digits)"
                      }
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("vesselUserDetails.pin") ? '#dc3545' : undefined,
                      backgroundColor: hasError("vesselUserDetails.pin") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={3}>
                  <InputText
                    name="vesselUserDetails.district"
                    label="District"
                    placeholder="Auto-populated"
                    register={register("vesselUserDetails.district")}
                    errors={errors}
                    readOnly
                  />
                </Col>
                <Col md={3}>
                  <InputText
                    name="vesselUserDetails.stateUt"
                    label="State/UT"
                    placeholder="Auto-populated"
                    register={register("vesselUserDetails.stateUt")}
                    errors={errors}
                    readOnly
                  />
                </Col>
                <Col md={3}>
                  <InputText
                    name="vesselUserDetails.country"
                    label="Country"
                    placeholder="Enter country"
                    register={register("vesselUserDetails.country")}
                    errors={errors}
                    readOnly
                  />
                </Col>
              </Row>
            </Card.Body>
          </Card>
        </Collapsible>

        {/* Education Details Section */}
        <Collapsible
          title={
            <div className="d-flex align-items-center">
              <FaGraduationCap className="me-2" style={{ color: "#2466BF" }} />
              <span style={{ color: "#2466BF" }}>Education Details</span>
            </div>
          }
        >
          <Card className="form-section-card">
            <Card.Body>
              {fields.map((field, index) => (
                <div key={field.id} className="education-entry mb-4">
                  {index > 0 && <div className="entry-divider mb-3"></div>}
                  <Row className="g-3">
                    <Col md={4}>
                      <InputText
                        name={`educationDetails.${index}.qualification`}
                        label={`Qualification ${index === 0 ? '*' : ''}`}
                        placeholder="Enter qualification"
                        register={register(`educationDetails.${index}.qualification`,
                          index === 0 ? { required: "Qualification is required" } : {}
                        )}
                        errors={errors}
                        required={index === 0}
                        style={{
                          borderColor: hasError(`educationDetails.${index}.qualification`) ? '#dc3545' : undefined,
                          backgroundColor: hasError(`educationDetails.${index}.qualification`) ? '#fff8f8' : undefined
                        }}
                      />
                    </Col>
                    <Col md={4}>
                      <InputText
                        name={`educationDetails.${index}.stream`}
                        label={`Stream ${index === 0 ? '*' : ''}`}
                        placeholder="Enter stream"
                        register={register(`educationDetails.${index}.stream`,
                          index === 0 ? { required: "Stream is required" } : {}
                        )}
                        errors={errors}
                        required={index === 0}
                        style={{
                          borderColor: hasError(`educationDetails.${index}.stream`) ? '#dc3545' : undefined,
                          backgroundColor: hasError(`educationDetails.${index}.stream`) ? '#fff8f8' : undefined
                        }}
                      />
                    </Col>
                    <Col md={3}>
                      <InputText
                        name={`educationDetails.${index}.yearOfPassing`}
                        label={`Year of Passing ${index === 0 ? '*' : ''}`}
                        placeholder="Enter year"
                        type="number"
                        register={register(`educationDetails.${index}.yearOfPassing`, {
                          ...(index === 0 ? { required: "Year of Passing is required" } : {}),
                          min: { value: 1900, message: "Invalid year" },
                          max: { value: new Date().getFullYear(), message: "Invalid year" }
                        })}
                        errors={errors}
                        required={index === 0}
                        style={{
                          borderColor: hasError(`educationDetails.${index}.yearOfPassing`) ? '#dc3545' : undefined,
                          backgroundColor: hasError(`educationDetails.${index}.yearOfPassing`) ? '#fff8f8' : undefined
                        }}
                      />
                    </Col>
                    <Col md={1} className="d-flex align-items-end">
                      {index > 0 && (
                        <Button
                          variant="danger"
                          size="sm"
                          onClick={() => remove(index)}
                          className="mt-1"
                        >
                          <FaTrash />
                        </Button>
                      )}
                    </Col>
                  </Row>
                </div>
              ))}
              <Button
                variant="outline-primary"
                onClick={() => append({ qualification: "", stream: "", yearOfPassing: "" })}
                className="add-education-btn"
              >
                <FaPlus className="me-2" /> Add Education Details
              </Button>
            </Card.Body>
          </Card>
        </Collapsible>

        {/* Professional Details Section */}
        <Collapsible
          title={
            <div className="d-flex align-items-center">
              <FaBriefcase className="me-2" style={{ color: "#2466BF" }} />
              <span style={{ color: "#2466BF" }}>Professional Details</span>
            </div>
          }
        >
          <Card className="form-section-card">
            <Card.Body>
              <Row className="g-3">
                <Col md={4}>
                  <InputText
                    name="professional.appointmentDate"
                    label="Appointment Date"
                    type="date"
                    placeholder="Select appointment date"
                    register={register("professional.appointmentDate", { 
                      required: "Appointment Date is required" 
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("professional.appointmentDate") ? '#dc3545' : undefined,
                      backgroundColor: hasError("professional.appointmentDate") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={4}>
                  <InputSelect
                    name="professional.employeeType"
                    label="Employee Type"
                    placeholder="Select employee type"
                    options={masterData?.employeeTypes?.map(type => ({
                      value: type.valueCode,
                      label: type.valueLabel
                    })) || []}
                    register={register}
                    errors={errors}
                    control={control}
                    required
                    rules={{ required: "Employee Type is required" }}
                    value={watch("professional.employeeType") || ""}
                    onChange={val => setValue("professional.employeeType", val, { shouldValidate: true })}
                    style={{
                      borderColor: hasError("professional.employeeType") ? '#dc3545' : undefined,
                      backgroundColor: hasError("professional.employeeType") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={4}>
                  <InputSelect
                    name="professional.employeeStatus"
                    label="Employee Status"
                    placeholder="Select employee status"
                    options={masterData?.employeeStatuses?.map(type => ({
                      value: type.valueCode,
                      label: type.valueLabel
                    })) || []}
                    register={register}
                    errors={errors}
                    control={control}
                    required
                    rules={{ required: "Employee Status is required" }}
                    value={watch("professional.employeeStatus") || ""}
                    onChange={val => setValue("professional.employeeStatus", val, { shouldValidate: true })}
                    style={{
                      borderColor: hasError("professional.employeeStatus") ? '#dc3545' : undefined,
                      backgroundColor: hasError("professional.employeeStatus") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={4}>
                  <InputSelect
                    name="professional.designation"
                    label="Designation"
                    placeholder="Select designation"
                    options={masterData?.designations?.map(type => ({
                      value: type.valueCode,
                      label: type.valueLabel
                    })) || []}
                    register={register}
                    errors={errors}
                    control={control}
                    required
                    rules={{ required: "Designation is required" }}
                    value={watch("professional.designation") || ""}
                    onChange={val => setValue("professional.designation", val, { shouldValidate: true })}
                    style={{
                      borderColor: hasError("professional.designation") ? '#dc3545' : undefined,
                      backgroundColor: hasError("professional.designation") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
                <Col md={4}>
                  <InputText
                    name="professional.department"
                    label="Department"
                    placeholder="Auto-populated"
                    register={register("professional.department")}
                    errors={errors}
                    readOnly
                  />
                </Col>
                <Col md={4}>
                  <InputText
                    name="professional.section"
                    label="Section"
                    placeholder="Enter section"
                    register={register("professional.section")}
                    errors={errors}
                  />
                </Col>
                <Col md={4}>
                  <InputSelect
                    name="professional.roles"
                    label="Roles"
                    placeholder="Select role"
                    options={masterData?.roles?.map(type => ({
                      value: type.valueCode,
                      label: type.valueLabel
                    })) || []}
                    register={register}
                    errors={errors}
                    control={control}
                    required
                    rules={{ required: "Role is required" }}
                    value={watch("professional.roles") || ""}
                    onChange={val => setValue("professional.roles", val, { shouldValidate: true })}
                    style={{
                      borderColor: hasError("professional.roles") ? '#dc3545' : undefined,
                      backgroundColor: hasError("professional.roles") ? '#fff8f8' : undefined
                    }}
                  />
                </Col>
              </Row>
            </Card.Body>
          </Card>
        </Collapsible>

        {/* Documents Upload Section */}
        <Collapsible
          title={
            <div className="d-flex align-items-center">
              <FaFileUpload className="me-2" style={{ color: "#2466BF" }} />
              <span style={{ color: "#2466BF" }}>Documents Upload</span>
            </div>
          }
        >
          <Card className="form-section-card">
            <Card.Body>
              <Row className="g-3">
                <Col md={4}>
                  <InputText
                    name="documents.addressProof"
                    label="Address Proof"
                    type="file"
                    register={register("documents.addressProof", { 
                      required: "Address Proof is required" 
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("documents.addressProof") ? '#dc3545' : undefined,
                      backgroundColor: hasError("documents.addressProof") ? '#fff8f8' : undefined
                    }}
                  />
                  <small className="text-muted">Upload PDF, JPG or PNG (Max: 2MB)</small>
                </Col>
                <Col md={4}>
                  <InputText
                    name="documents.identityProof"
                    label="Identity Proof"
                    type="file"
                    register={register("documents.identityProof", { 
                      required: "Identity Proof is required" 
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("documents.identityProof") ? '#dc3545' : undefined,
                      backgroundColor: hasError("documents.identityProof") ? '#fff8f8' : undefined
                    }}
                  />
                  <small className="text-muted">Upload PDF, JPG or PNG (Max: 2MB)</small>
                </Col>
                <Col md={4}>
                  <InputText
                    name="documents.appointmentLetter"
                    label="Appointment Letter"
                    type="file"
                    register={register("documents.appointmentLetter", { 
                      required: "Appointment Letter is required" 
                    })}
                    errors={errors}
                    required
                    style={{
                      borderColor: hasError("documents.appointmentLetter") ? '#dc3545' : undefined,
                      backgroundColor: hasError("documents.appointmentLetter") ? '#fff8f8' : undefined
                    }}
                  />
                  <small className="text-muted">Upload PDF, JPG or PNG (Max: 2MB)</small>
                </Col>
              </Row>
            </Card.Body>
          </Card>
        </Collapsible>
        
        <div className="d-flex justify-content-end mt-4 gap-2">
          <Button
            variant="secondary"
            onClick={onCancel}
            className="cancel-btn"
          >
            Cancel
          </Button>
          <Button
            variant="primary"
            type="submit"
            className="submit-btn"
            style={{
              background: "linear-gradient(135deg, #0077b6, #023e8a)",
              border: "none",
              boxShadow: "0 4px 6px rgba(0, 0, 0, 0.1)",
              padding: "8px 20px",
            }}
          >
            Submit
          </Button>
        </div>
      </div>
    </Form>
  );
};

export default EmployeeRegistrationForm;
