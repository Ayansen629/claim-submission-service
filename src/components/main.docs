import React, { useState } from 'react';
import "./Register.css";
import { useForm } from "react-hook-form";
import { Link, useNavigate } from 'react-router-dom';
import { ToastContainer, toast } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';
import axios from 'axios';

const Register = () => {
  const navigate = useNavigate();
  const [otpSent, setOtpSent] = useState(false);
  const [otp, setOtp] = useState("");
  const [loading, setLoading] = useState(false);
  const [registerLoading, setRegisterLoading] = useState(false);
  const [selectedRadio, setSelectedRadio] = useState("individual");

  const {
    register,
    handleSubmit,
    watch,
    reset,
    formState: { errors },
  } = useForm({
    defaultValues: {
      name: "",
      corporateName: "",
      email: "",
      mobilenumber: "",
      aadhar: "",
      pancard: "",
      cinTaNumber: "",
      userType: "individual", 
    },
  });

  const formValues = watch();

  const isFormComplete = () => {
    if (selectedRadio === "individual") {
      return (
        formValues.name &&
        formValues.email &&
        formValues.mobilenumber &&
        formValues.aadhar &&
        formValues.pancard
      );
    } else {
      return (
        formValues.corporateName &&
        formValues.email &&
        formValues.mobilenumber &&
        formValues.cinTaNumber &&
        formValues.pancard
      );
    }
  };

  const handleSendOTP = () => {
    if (!isFormComplete()) {
      toast.error("Please fill all required fields");
      return;
    }
    setOtpSent(true);
    toast.success("OTP will be sent when you click Register");
  };

  const onSubmit = async (data) => {
    if (!otp) {
      toast.error("Please enter the OTP");
      return;
    }

    setRegisterLoading(true);

    // Prepare payload based on user type
    const payload = {
      claimant: {
        uuid: "",
        claimantName: selectedRadio === "individual" ? data.name : null,
        corporateName: selectedRadio === "corporate" ? data.corporateName : null,
        emailId: data.email,
        claimantEntityType: selectedRadio === "individual" ? "INDV" : "CORP",
        mobileNumber: data.mobilenumber,
        aadharNumber: selectedRadio === "individual" ? data.aadhar : null,
        panNumber: data.pancard,
        cinOrTaNumber: selectedRadio === "corporate" ? data.cinTaNumber : null,
        roleCode: "CLAIMANT",
        active: true,
        auditDetails: {
          createdBy: "system_user",
          createdDate: new Date().toISOString(),
          lastModifiedBy: "system_user",
          lastModifiedDate: new Date().toISOString()
        }
      },
      corporateDebtor: null,
      otp: otp // Include OTP in the payload
    };

    try {
      const response = await axios.post('/claim-user-service/_user/_registration', payload);
      
      if (response.data.status.code === "201") {
        toast.success("Registration successful!", { position: 'top-right' });
        
        // Reset form
        reset();
        setOtp("");
        setOtpSent(false);

        // Navigate to login after a delay
        setTimeout(() => {
          navigate("/login");
        }, 1000);
      } else {
        toast.error(response.data.status.message || "Registration failed");
      }
    } catch (error) {
      console.error("Registration error:", error);
      if (error.response) {
        // Handle specific error messages from backend
        toast.error(error.response.data?.status?.message || "Registration failed");
      } else {
        toast.error("Network error. Please try again.");
      }
    } finally {
      setRegisterLoading(false);
    }
  };

  return (
    <div className='register-page'>
      <div className="register-container">
        <div className="register-header">
          <div className="register-textmain"> Claimants Register</div>
        </div>
        <form className="register-form" onSubmit={handleSubmit(onSubmit)}>
          {/* ... (rest of your form JSX remains the same until the OTP section) */}

          {/* OTP Section */}
          {!otpSent ? (
            <button
              type="button"
              className="send-otp-btn"
              onClick={handleSendOTP}
              disabled={!isFormComplete() || loading}
            >
              Send OTP
            </button>
          ) : (
            <div className="otp-verification">
              <input
                type="text"
                placeholder="Enter OTP"
                value={otp}
                onChange={(e) => setOtp(e.target.value)}
                className="user-input"
              />
              <button
                type="submit"
                className={`register-btn ${!otp || registerLoading ? 'disabled' : ''}`}
                disabled={!otp || registerLoading}
              >
                {registerLoading ? "Registering..." : "Register"}
              </button>
            </div>
          )}

          {/* ... (rest of your JSX remains the same) */}
        </form>

        <p className="login-link">
          Already have an account? <Link to="/login">Login here</Link>
        </p>
      </div>
      <ToastContainer />
    </div>
  );
};

export default Register;
