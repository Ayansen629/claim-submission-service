import React, { useState, useEffect, useRef, useCallback } from "react";
import style from "./ActionTable.module.css";
import _ from "lodash";
import editIcon from "../../../assests/icons/edit.svg";
import viewIcon from "../../../assests/icons/view.svg";
import deleteIcon from "../../../assests/icons/delete.svg";
import upload from "../../../assests/icons/upload.svg";
import download from "../../../assests/icons/download.svg";
import { BsGeoAltFill } from "react-icons/bs";
import Tooltip from "@mui/material/Tooltip";
import { Dropdown } from "react-bootstrap";

const ActionTable = (props) => {
  console.log(props.rowData, "originaldata");
  
  const [searchedData, setSearchedData] = useState(props.rowData || []);
  const [currentPage, setCurrentPage] = useState(1);
  const [selected, setSelected] = useState([]);
  const [sortedColumn, setSortedColumn] = useState(null);
  const [sortOrder, setSortOrder] = useState("");
  const hideColumns = props.hideColumns || [];
  const [searchQuery, setSearchQuery] = useState("");
  const [statusFilter, setStatusFilter] = useState("all");
  const [dateSort, setDateSort] = useState("none");

  useEffect(() => {
    if (props.rowData) {
      setSearchedData(props.rowData);
      setCurrentPage(1);
      const selectedRows = props.rowData
        .filter((item) => item.isChecked)
        .map((item) => item.id);
      setSelected(selectedRows);
    }
  }, [props.rowData]);

  const [itemsPerPage, setItemPerPage] = useState(10);
  const indexOfLastItem = currentPage * itemsPerPage;
  const indexOfFirstItem = indexOfLastItem - itemsPerPage;
  const currentItems = searchedData.slice(indexOfFirstItem, indexOfLastItem);
  const totalPages = Math.ceil((props.rowData?.length || 0) / itemsPerPage);

  // Apply filters and sorting
  useEffect(() => {
    let filteredData = [...props.rowData];
    
    // Apply status filter
    if (statusFilter !== "all") {
      filteredData = filteredData.filter(item => item.status === statusFilter);
    }
    
    // Apply search query
    if (searchQuery) {
      const searchValue = searchQuery.toLowerCase();
      filteredData = filteredData.filter(item => 
        Object.values(item).some(value => 
          value?.toString().toLowerCase().includes(searchValue)
      );
    }
    
    // Apply date sorting
    if (dateSort !== "none") {
      filteredData.sort((a, b) => {
        const dateA = new Date(a.dateSubmitted);
        const dateB = new Date(b.dateSubmitted);
        return dateSort === "asc" ? dateA - dateB : dateB - dateA;
      });
    }
    
    setSearchedData(filteredData);
    setCurrentPage(1);
  }, [props.rowData, statusFilter, searchQuery, dateSort]);

  const handleNextPage = () => {
    setCurrentPage((prevPage) => Math.min(prevPage + 1, totalPages));
  };

  const handlePrevPage = () => {
    setCurrentPage((prevPage) => Math.max(prevPage - 1, 1));
  };

  const handleOnSearch = (e) => {
    setSearchQuery(e.target.value);
  };

  const sortByColumn = (column) => {
    if (sortedColumn === column) {
      setSortOrder(sortOrder === "asc" ? "desc" : "asc");
    } else {
      setSortedColumn(column);
      setSortOrder("asc");
    }
  };

  const showSortedColumnIcon = (columnName) => {
    return sortedColumn === columnName ? (
      sortOrder === "asc" ? (
        <i className="fa fa-long-arrow-up table-arrow" aria-hidden="true" />
      ) : (
        <i className="fa fa-long-arrow-down table-arrow" aria-hidden="true" />
      )
    ) : (
      <></>
    );
  };

  const handleDeleteRow = (rowData) => {
    props.onDeleteRow?.(rowData);
  };

  const handleTrack = (rowData) => {
    props.onTrackRow?.(rowData);
  };

  const handleView = (rowData) => {
    props.onViewRow?.(rowData);
  };

  const handleEdit = (rowData) => {
    props.handleEditRow(rowData);
  };

  const handleDownload = (rowData) => {
    props.download?.(rowData);
  };

  // Select all rows in current page
  const handleSelectAll = (e) => {
    if (e.target.checked) {
      const newSelecteds = currentItems.map((n) => n);
      setSelected(newSelecteds);
      props.onCheckHandler?.(newSelecteds);
    } else {
      setSelected([]);
      props.onCheckHandler?.([]);
    }
  };

  // Select individual row
  const handleRowSelect = (e, row) => {
    if (e.target.checked) {
      setSelected([...selected, row]);
      props.onCheckHandler?.([...selected, row]);
    } else {
      setSelected(selected.filter(item => item.id !== row.id));
      props.onCheckHandler?.(selected.filter(item => item.id !== row.id));
    }
  };

  const isSelected = (id) => {
    return selected.some(item => item.id === id);
  };

  // Get unique status values for filter dropdown
  const getStatusOptions = () => {
    const statuses = [...new Set(props.rowData?.map(item => item.status))];
    return ["all", ...statuses].filter(Boolean);
  };

  const StatusFilterDropdown = () => (
    <Dropdown className="ms-2">
      <Dropdown.Toggle variant="secondary" id="status-filter-dropdown">
        Status: {statusFilter === "all" ? "All" : statusFilter}
      </Dropdown.Toggle>
      <Dropdown.Menu>
        {getStatusOptions().map((status, index) => (
          <Dropdown.Item 
            key={index} 
            onClick={() => setStatusFilter(status)}
            active={status === statusFilter}
          >
            {status === "all" ? "All Statuses" : status}
          </Dropdown.Item>
        ))}
      </Dropdown.Menu>
    </Dropdown>
  );

  const DateSortDropdown = () => (
    <Dropdown className="ms-2">
      <Dropdown.Toggle variant="secondary" id="date-sort-dropdown">
        Date: {dateSort === "none" ? "Default" : dateSort === "asc" ? "Oldest First" : "Newest First"}
      </Dropdown.Toggle>
      <Dropdown.Menu>
        <Dropdown.Item onClick={() => setDateSort("none")} active={dateSort === "none"}>
          Default Order
        </Dropdown.Item>
        <Dropdown.Item onClick={() => setDateSort("asc")} active={dateSort === "asc"}>
          Oldest First
        </Dropdown.Item>
        <Dropdown.Item onClick={() => setDateSort("desc")} active={dateSort === "desc"}>
          Newest First
        </Dropdown.Item>
      </Dropdown.Menu>
    </Dropdown>
  );

  const PaginationOptions = () => {
    const [isShow, setIsShow] = useState(false);
    const exportRef = useRef(null);

    useEffect(() => {
      const handleOutsideClick = (e) => {
        if (exportRef.current && !exportRef.current.contains(e.target)) {
          setIsShow(false);
        }
      };
      document.addEventListener("click", handleOutsideClick);
      return () => {
        document.removeEventListener("click", handleOutsideClick);
      };
    }, []);

    return (
      <div
        id="export_button"
        ref={exportRef}
        className="d-inline-flex flex-row align-items-center stickyCard"
      >
        <div className="ps-3" style={{ marginRight: "10px" }}>
          Rows per page
        </div>
        <Dropdown>
          <Dropdown.Toggle
            id="dropdownMenuButton1"
            style={{
              color: "white",
              backgroundColor: "#269ccc",
              borderColor: "#000000",
            }}
          >
            <span className="font-14">{itemsPerPage}</span>
          </Dropdown.Toggle>

          <Dropdown.Menu>
            {[10, 25, 50, 100].map((item, i) => (
              <Dropdown.Item
                key={i}
                onClick={() => {
                  setItemPerPage(item);
                  setCurrentPage(1);
                }}
                onMouseEnter={(e) => {
                  e.currentTarget.style.backgroundColor = "#269ccc";
                  e.currentTarget.style.color = "#000";
                }}
                onMouseLeave={(e) => {
                  e.currentTarget.style.backgroundColor = "";
                  e.currentTarget.style.color = "";
                }}
                style={{
                  cursor: "pointer",
                  transition: "background-color 0.3s ease, color 0.3s ease",
                }}
              >
                {item}
              </Dropdown.Item>
            ))}
          </Dropdown.Menu>
        </Dropdown>
      </div>
    );
  };

  const userData = JSON.parse(localStorage.getItem("userFormData"));

  return (
    <div className="mb-5 px-0 mx-0 generic-action-table pt-3">
      {/* Search Bar and Filters */}
      <div
        className={`row ${style.application_search} ${
          props.hideSearchBar ? "justify-content-end mb-2" : "justify-content-center"
        }`}
      >
        {props.filters && (
          <div className="col-sm-6 my-2 fw-600" style={{ width: "fit-content" }}>
            {props.filters()}
          </div>
        )}
        
        <div className="col-sm-12 my-2 d-flex flex-wrap align-items-center">
          {!props.hideSearchBar && (
            <div className="me-2 mb-2" style={{ minWidth: "200px" }}>
              <input
                type="search"
                value={searchQuery}
                onChange={handleOnSearch}
                placeholder="Search..."
                className="form-control"
              />
            </div>
          )}
          
          <div className="d-flex mb-2">
            <StatusFilterDropdown />
            <DateSortDropdown />
          </div>
          
          {props.showCustomBtn && (
            <div
              id="table_custom_button_div"
              className="ms-auto mb-2"
              style={props.btnStyle ? props.btnStyle : { width: "12%" }}
            >
              <button
                onClick={props.handleBtnClick}
                type="button"
                disabled={props.isCustomBtnDisabled}
                className="btn btn-primary"
                style={{ width: "100%" }}
              >
                <span className="font-14">{props.btnName}</span>
              </button>
            </div>
          )}
        </div>
      </div>

      {/* Table */}
      <div className="table-container">
        <table className={`table ${style.table}`} style={{ width: props.tableWidth }}>
          <thead>
            <tr className="table_header">
              {/* Checkbox column */}
              <th className="px-3 py-2" style={{ minWidth: "3rem" }}>
                <input
                  type="checkbox"
                  checked={selected.length > 0 && selected.length === currentItems.length}
                  onChange={handleSelectAll}
                  ref={input => {
                    if (input) {
                      input.indeterminate = selected.length > 0 && selected.length < currentItems.length;
                    }
                  }}
                />
              </th>
              
              {props.columns?.map((data, colIndx) => {
                return !hideColumns.includes(data.fieldName) ? (
                  <th
                    key={colIndx}
                    className={`px-3 py-2 ${data.clsName}`}
                    style={{ minWidth: data.minWidth || "6rem" }}
                    onClick={() => sortByColumn(data.fieldName)}
                  >
                    {data.titleCode ? data.titleCode : data.colName}{" "}
                    {showSortedColumnIcon(data.fieldName)}
                  </th>
                ) : null;
              })}
            </tr>
          </thead>
          <tbody>
            {currentItems.length !== 0 ? (
              currentItems.map((row, index) => {
                const isItemSelected = isSelected(row.id);
                return (
                  <tr key={index} className="table_datarow">
                    {/* Checkbox cell */}
                    <td className="px-3 py-2">
                      <input
                        type="checkbox"
                        checked={isItemSelected}
                        onChange={(e) => handleRowSelect(e, row)}
                      />
                    </td>
                    
                    {props.columns.map((o, colIndex) =>
                      o.isClickable ? (
                        <td
                          key={colIndex}
                          className={`px-3 py-2 ${o.clsName} ${
                            colIndex === props.columns.length - 1
                              ? "last-column"
                              : colIndex === 0
                              ? "first-column"
                              : ""
                          }`}
                          style={{
                            color: "#3370D4",
                            cursor: "pointer",
                            fontWeight: row.isRead === false ? "bolder" : "normal",
                          }}
                          onClick={(e) => props.additionalMethod?.(row)}
                        >
                          <span>{_.get(row, o.fieldName)}</span>
                        </td>
                      ) : !hideColumns.includes(o.fieldName) && o.fieldName !== "action" ? (
                        <td
                          key={colIndex}
                          className={`px-3 py-2 ${o.clsName} ${
                            colIndex === props.columns.length - 1
                              ? "last-column"
                              : colIndex === 0
                              ? "first-column"
                              : ""
                          }`}
                          style={{
                            fontWeight: row.isRead === false ? "bolder" : "normal",
                          }}
                        >
                          {o.colName === "Status" ? (
                            <span className={`status-column ${_.get(row, o.fieldName)}-status`}>
                              {_.get(row, o.fieldName) || (
                                <i className="fa fa-exclamation-circle me-1">No Status found</i>
                              )}
                              {o.fieldName2 ? _.get(row, o.fieldName2) : ""}
                            </span>
                          ) : (
                            <span>
                              {_.get(row, o.fieldName)}{" "}
                              {o.fieldName2 ? _.get(row, o.fieldName2) : ""}
                            </span>
                          )}
                        </td>
                      ) : !hideColumns.includes(o.fieldName) && o.fieldName === "action" ? (
                        <td key={colIndex} className={`px-3 py-2 ${o.clsName} action-column`}>
                          <div className="d-flex" style={{ gap: "12px", justifyContent: "center" }}>
                            {o.actions.includes("edit") && userData?.userType !== "CRP" && (
                              <Tooltip title="Edit this item" placement="top" arrow>
                                <button
                                  className="btn btn-light border-secondary btn-sm"
                                  onClick={() => handleEdit(row)}
                                  type="button"
                                  disabled={
                                    row.applicationStatus !== "Sent back" && row.applicationStatus !== "Draft"
                                  }
                                  style={
                                    row.applicationStatus !== "Sent back" && row.applicationStatus !== "Draft"
                                      ? { opacity: 0.5, cursor: "not-allowed" }
                                      : {}
                                  }
                                >
                                  <img src={editIcon} alt="edit" className="table-action-icon" />
                                </button>
                              </Tooltip>
                            )}
                            
                            {o.actions.includes("view") && (
                              <Tooltip title="View details" placement="top" arrow>
                                <button
                                  className="btn btn-info btn-sm"
                                  onClick={() => handleView(row)}
                                  type="button"
                                >
                                  <img src={viewIcon} alt="view" className="table-action-icon" />
                                </button>
                              </Tooltip>
                            )}
                            
                            {o.actions.includes("track") && userData?.userType !== "CRP" && (
                              <Tooltip title="Track this item" placement="top" arrow>
                                <button
                                  className="btn btn-info btn-sm"
                                  onClick={() => handleTrack(row)}
                                  type="button"
                                >
                                  <BsGeoAltFill />
                                </button>
                              </Tooltip>
                            )}
                          </div>
                        </td>
                      ) : null
                    )}
                  </tr>
                );
              })
            ) : (
              <tr>
                <td colSpan={props.columns.length + 1} className="text-center fw-bold py-4">
                  <h5>No records found matching your criteria</h5>
                </td>
              </tr>
            )}
          </tbody>
        </table>
      </div>

      {/* Table Footer */}
      <div
        className={`col-sm-12 py-3 position-relative d-flex justify-content-between align-items-center ${style.table_footer}`}
      >
        <div>
          Showing {indexOfFirstItem + 1}-{Math.min(indexOfLastItem, searchedData.length)} of {searchedData.length} entries
          {selected.length > 0 && (
            <span className="ms-3">
              ({selected.length} selected)
            </span>
          )}
        </div>
        <div
          className="d-flex align-items-center"
          style={{ marginRight: 30 }}
        >
          <PaginationOptions />
          <div>
            <button
              onClick={handlePrevPage}
              disabled={currentPage === 1}
              className="btn btn-light"
            >
              <i className="fa fa-arrow-left" aria-hidden="true"></i>
            </button>
            <span className={`mx-1 ${style.pagenumber}`}>{currentPage}</span>
            <button
              onClick={handleNextPage}
              disabled={currentPage === totalPages}
              className="btn btn-light"
            >
              <i className="fa fa-arrow-right" aria-hidden="true"></i>
            </button>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ActionTable;
