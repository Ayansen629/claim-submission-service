import React, { useState, useEffect } from 'react';
import { useForm, FormProvider } from 'react-hook-form';
import { Container, Row, Col, Button, Card } from 'react-bootstrap';
import { FaFileAlt, FaFileUpload, FaPrint, FaArrowLeft, FaArrowRight } from 'react-icons/fa';
import DynamicFormFields from '../../common/DynamicFormFields';
import createFormFieldsConfig from '../../common/FormFieldsConfig';
import { useLocation, useNavigate } from 'react-router-dom';

const OfficerViewPage = () => {
  const [currentStep, setCurrentStep] = useState(1);
  const [formData, setFormData] = useState({});
  const [isLoading, setIsLoading] = useState(true);
  const navigate = useNavigate();
  const totalSteps = 2;

  // Mock data - in real app this would come from API
  const mockApplicationData = {
    firstName: "John",
    lastName: "Doe",
    emailId: "john.doe@example.com",
    phoneNumber: "9876543210",
    panNumber: "ABCDE1234F",
    gstNumber: "22ABCDE1234F1Z5",
    addressLine1: "123 Main Street",
    addressLine2: "Building A",
    pin: "700001",
    district: "Kolkata",
    stateUt: "West Bengal",
    country: "India",
    addressProof: "address_proof.pdf",
    panCardUpload: "pan_card.pdf",
    gstCertificate: "gst_certificate.pdf"
  };

  useEffect(() => {
    // Simulate API call
    setTimeout(() => {
      setFormData(mockApplicationData);
      setIsLoading(false);
    }, 500);
  }, []);

  const methods = useForm({ defaultValues: { country: 'India' } });
  const { setValue } = methods;

  useEffect(() => {
    if (!isLoading) {
      Object.keys(formData).forEach(key => {
        if (formData[key]) {
          setValue(key, formData[key]);
        }
      });
    }
  }, [isLoading, formData, setValue]);

  const getDisabledFormFieldsConfig = () => {
    const originalConfig = createFormFieldsConfig(() => {});
    
    const disabledStep1 = originalConfig.step1.map(field => ({
      ...field,
      disabled: true
    }));
    
    const disabledStep2 = originalConfig.step2.map(field => ({
      ...field,
      disabled: true,
      render: (value) => (
        <div className="file-display">
          <span>{value || 'No file uploaded'}</span>
          {value && (
            <Button variant="link" size="sm" onClick={() => handleDownload(value)}>
              Download
            </Button>
          )}
        </div>
      )
    }));
    
    return {
      step1: disabledStep1,
      step2: disabledStep2
    };
  };

  const formFields = getDisabledFormFieldsConfig();

  const getCurrentStepFields = () => {
    switch (currentStep) {
      case 1: return formFields.step1;
      case 2: return formFields.step2;
      default: return [];
    }
  };

  const handleNext = () => {
    if (currentStep < totalSteps) {
      setCurrentStep(currentStep + 1);
      window.scrollTo(0, 0);
    }
  };

  const handlePrevious = () => {
    setCurrentStep(Math.max(1, currentStep - 1));
    window.scrollTo(0, 0);
  };

  const handleDownload = (filename) => {
    console.log(`Downloading ${filename}`);
    // Actual download implementation would go here
  };

  const handlePrint = () => {
    window.print();
  };

  const handleBackToList = () => {
    navigate('/officerRoleDashboard');
  };

  if (isLoading) {
    return <div>Loading application details...</div>;
  }

  return (
    <Container fluid className="py-4">
      <Row className="justify-content-center">
        <Col md={10} xl={9}>
          <Card>
            <Card.Header className="bg-light">
              <div className="d-flex justify-content-between align-items-center">
                <h5 className="mb-0">Application Details (View Mode)</h5>
                <div>
                  <Button variant="outline-secondary" size="sm" onClick={handlePrint} className="me-2">
                    <FaPrint className="me-1" /> Print
                  </Button>
                  <Button variant="outline-primary" size="sm" onClick={handleBackToList}>
                    Back to List
                  </Button>
                </div>
              </div>
              <div className="mt-2">
                <small className="text-muted">Application No: APP-2025-001</small>
              </div>
            </Card.Header>

            <Card.Body className="p-4">
              <FormProvider {...methods}>
                <form>
                  {/* Stepper */}
                  <div className="stepper-wrapper mb-4">
                    <div className={`stepper-item ${currentStep >= 1 ? 'active' : ''}`}>
                      <div className="step-counter">
                        {currentStep > 1 ? <FaFileAlt /> : '1'}
                      </div>
                      <div className="step-name">Basic Details</div>
                    </div>
                    <div className={`stepper-item ${currentStep >= 2 ? 'active' : ''}`}>
                      <div className="step-counter">
                        {currentStep > 2 ? <FaFileUpload /> : '2'}
                      </div>
                      <div className="step-name">Documents</div>
                    </div>
                  </div>

                  {/* Form Content */}
                  <div className="form-content">
                    <DynamicFormFields 
                      fields={getCurrentStepFields()} 
                      setFormData={setFormData} 
                    />
                  </div>

                  {/* Navigation Buttons */}
                  <div className="d-flex justify-content-between mt-4">
                    <Button
                      variant="outline-secondary"
                      onClick={handlePrevious}
                      disabled={currentStep === 1}
                    >
                      <FaArrowLeft className="me-1" /> Previous
                    </Button>
                    
                    {currentStep < totalSteps && (
                      <Button
                        variant="primary"
                        onClick={handleNext}
                      >
                        Next <FaArrowRight className="ms-1" />
                      </Button>
                    )}
                  </div>
                </form>
              </FormProvider>
            </Card.Body>
          </Card>
        </Col>
      </Row>
    </Container>
  );
};

export default OfficerViewPage;
