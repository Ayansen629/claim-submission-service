const validateDocuments = async () => {
  if (isViewMode) return true;

  const requiredFields = [];
  const termsAccepted = getValues("termsAccepted");

  // In edit mode, only require files if GRT was modified
  if (isEditMode) {
    const initialGrt = location.state?.initialData?.grt || getValues("grt");
    const currentGrt = getValues("grt");
    const grtModified = currentGrt !== initialGrt;

    if (grtModified) {
      const tonnageBooklets = getValues("tonnageBooklets");
      const stabilityBooklets = getValues("stabilityBooklets");

      if (!tonnageBooklets || !tonnageBooklets.name) {
        requiredFields.push("tonnageBooklets");
      }

      if (!stabilityBooklets || !stabilityBooklets.name) {
        requiredFields.push("stabilityBooklets");
      }
    }
  }
  // In normal mode, these files are not required
  // else {
  //   // No files required in normal mode
  // }

  if (!termsAccepted) {
    requiredFields.push("termsAccepted");
  }

  let hasErrors = false;

  requiredFields.forEach((fieldName) => {
    const fieldValue = getValues(fieldName);

    if (
      !fieldValue ||
      (fieldValue instanceof FileList && fieldValue.length === 0)
    ) {
      setError(fieldName, {
        type: "required",
        message: `${fieldName} is required`,
      });
      hasErrors = true;
    }
  });

  return !hasErrors;
};
