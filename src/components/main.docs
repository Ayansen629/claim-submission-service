\// DynamicField component for VesselOwnerRegistrationPage
const DynamicField = ({ field }) => {
  if (field.showIf && !field.showIf()) return null;

  const handleChange = (e) => {
    if (field.onChange) field.onChange(e);
    
    // Clear error when user starts typing
    if (e.target.value && errors[field.name]) {
      clearErrors(field.name);
      setValidationMessage("");
    }
  };

  switch(field.type) {
    case 'select':
      return (
        <InputSelect
          label={field.label}
          required={field.required}
          name={field.name}
          options={field.options}
          register={register(field.name, {
            ...field.validation,
            onChange: handleChange
          })}
          errors={errors}
          icon={field.icon}
          className="mb-4"
        />
      );
      
    case 'file':
      return (
        <InputFile
          label={field.label}
          required={field.required}
          name={field.name}
          register={register(field.name)}
          errors={errors}
          onChange={(e) => {
            if (e.target.files && e.target.files[0]) {
              const file = e.target.files[0];
              
              // Store file name in form data
              setValue(field.name, file.name);
              setFormData(prev => {
                const updated = { ...prev, [field.name]: file.name };
                console.log(`Updated formData with file ${field.name}:`, updated);
                return updated;
              });
              
              // Clear any errors
              if (errors[field.name]) {
                clearErrors(field.name);
              }
              
              // File type validation
              const validTypes = ['application/pdf', 'image/jpeg', 'image/png', 'image/jpg'];
              if (!validTypes.includes(file.type)) {
                setError(field.name, {
                  type: 'manual',
                  message: 'Only PDF, JPG or PNG files are allowed'
                });
              }
              
              // File size validation (max 2MB)
              if (file.size > 2 * 1024 * 1024) {
                setError(field.name, {
                  type: 'manual',
                  message: 'File size should not exceed 2MB'
                });
              }
            }
          }}
          className="mb-4"
          watch={watch}
          accept=".pdf,.jpg,.jpeg,.png"
          defaultValue={formData[field.name]}
        />
      );
      
    default:
      return (
        <InputText
          label={field.label}
          required={field.required}
          name={field.name}
          type={field.type}
          placeholder={field.placeholder}
          register={register(field.name, {
            ...field.validation,
            onChange: handleChange
          })}
          errors={errors}
          readOnly={field.readOnly}
          icon={field.icon}
          className="mb-4"
          style={{
            borderColor: errors[field.name] ? '#dc3545' : '#ced4da',
            backgroundColor: errors[field.name] ? '#fff8f8' : 'white'
          }}
        />
      );
  }
};
