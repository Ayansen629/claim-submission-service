// In NewVesselRegistration.jsx
const NewVesselRegistration = ({ isEdit = false, id }) => {
  const { t } = useTranslation();
  const { changeLanguage } = useLanguageContext();
  const methods = useForm({ mode: "onTouched" });
  const { trigger, getValues, setError, reset } = methods;

  // Payment modal state
  const [showModal, setShowModal] = useState(false);
  const [paymentMode, setPaymentMode] = useState("offline");
  const [challanFile, setChallanFile] = useState(null);
  const [showPreview, setShowPreview] = useState(false);
  const [isLoading, setIsLoading] = useState(false);

  // Load static data when in edit mode
  useEffect(() => {
    if (isEdit) {
      // This is where we would normally fetch data from an API
      // For now, we'll use static mock data
      const mockVesselData = {
        ownerName: "John Doe",
        ownerAddress: "123 Marine Drive, Mumbai",
        mobile: "9876543210",
        email: "john.doe@example.com",
        pan: "ABCDE1234F",
        aadhar: "123456789012",
        tradeLicence: "TRADE12345",
        gst: "GSTIN123456789",
        grt: "500",
        workOrderValue: "1000000",
        vesselType: "new",
        category: "A",
        purchaseType: "new",
        constructedBy: "1",
        // Add other fields as needed
      };
      
      reset(mockVesselData);
    }
  }, [isEdit, id, reset]);

  // ... rest of the component remains the same
