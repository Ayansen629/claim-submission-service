const renderField = (field) => {
  if (field.showIf && !field.showIf()) return null;

  const handleChange = (e) => {
    if (field?.onChange) field.onChange(e, setValue, watch);
    
    // For file fields, validate immediately on change
    if (field.type === 'file' && e.target.files?.[0]) {
      const file = e.target.files[0];
      const validTypes = ['application/pdf', 'image/jpeg', 'image/png', 'image/jpg'];
      
      // Clear any existing errors
      clearErrors(field.name);
      
      // Validate file type
      if (!validTypes.includes(file.type)) {
        setError(field.name, {
          type: 'fileType',
          message: 'Only PDF, JPG or PNG files are allowed'
        });
        return;
      }
      
      // Validate file size
      if (file.size > 2 * 1024 * 1024) {
        setError(field.name, {
          type: 'fileSize',
          message: 'File size should not exceed 2MB'
        });
        return;
      }
      
      // If valid, set the value
      const extension = file?.name?.substring(file?.name?.lastIndexOf('.'));
      const fileName = `${field.label}${extension}`;
      file.originalName = fileName;
      
      setValue(field.name, renameFile(file, fileName), { shouldValidate: true });
      setFormData(prev => ({
        ...prev,
        [field.name]: renameFile(file, fileName)
      }));
    }
  };

  // ... rest of the renderField function
};
