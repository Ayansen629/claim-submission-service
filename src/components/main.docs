import React, { useState } from 'react';
import "./Register.css";
import { useForm } from "react-hook-form";
import { Link, useNavigate } from 'react-router-dom';
import { ToastContainer, toast } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';

const Register = () => {
  const navigate = useNavigate();
  const [otpSent, setOtpSent] = useState(false);
  const [otp, setOtp] = useState("");
  const [loading, setLoading] = useState(false);
  const [selectedRadio, setSelectedRadio] = useState("individual");

  const {
    register,
    handleSubmit,
    watch,
    reset,
    formState: { errors },
  } = useForm({
    defaultValues: {
      firstName: "",
      lastName: "",
      email: "",
      mobilenumber: "",
      aadhar: "",
      pancard: "",
      cinNumber: "",
      tanNumber: "",
      userType: "individual", 
    },
  });

  const formValues = watch();

  const isFormComplete = () => {
    if (selectedRadio === "individual") {
      return (
        formValues.firstName &&
        formValues.lastName &&
        formValues.email &&
        formValues.mobilenumber &&
        formValues.aadhar &&
        formValues.pancard
      );
    } else {
      return (
        formValues.firstName &&
        formValues.lastName &&
        formValues.email &&
        formValues.mobilenumber &&
        formValues.cinNumber &&
        formValues.tanNumber &&
        formValues.pancard
      );
    }
  };

  const handleSendOTP = async () => {
    setLoading(true);

    // Simulate OTP sending
    setTimeout(() => {
      toast.success("OTP Sent", { position: 'top-right' });
      setOtpSent(true); // Show OTP input box
      setLoading(false);
    }, 1000);
  };

  const onSubmit = async (data) => {
    if (!otp) {
      toast.error("Please enter the OTP");
      return;
    }

    if (otp !== "123456") {
      toast.error("Invalid OTP. Please enter the correct OTP.");
      return;
    }

    // Prepare payload
    const payload = {
      firstName: data.firstName,
      lastName: data.lastName,
      email: data.email,
      mobilenumber: data.mobilenumber,
      pancard: data.pancard,
      userType: selectedRadio === "individual" ? "INDV" : "CORP",
      ...(selectedRadio === "individual" 
        ? { aadhar: data.aadhar }
        : { 
            cinNumber: data.cinNumber,
            tanNumber: data.tanNumber 
          }
      )
    };

    console.log("Registration Payload:", payload);
    toast.success("Registration successful!", { position: 'top-right' });

    // Reset form and OTP state
    reset();
    setOtp("");
    setOtpSent(false);

    // Navigate to login after a delay
    setTimeout(() => {
      navigate("/login");
    }, 1000);
  };

  return (
    <div className='register-page'>
      <div className="register-container">
        <div className="register-header">
          <div className="register-textmain"> Claimants Register</div>
        </div>
        <form className="register-form" onSubmit={handleSubmit(onSubmit)}>
          <div className="radio-options">
            <div className={`radio-op ${selectedRadio === "individual" ? "active" : ""}`}>
              <input
                type="radio"
                id="individual"
                name="userType"
                value="individual"
                checked={selectedRadio === "individual"}
                onChange={() => setSelectedRadio("individual")}
              />
              <label htmlFor="individual">Individual</label>
            </div>
            <div className={`radio-op ${selectedRadio === "corporate" ? "active" : ""}`}>
              <input
                type="radio"
                id="corporate"
                name="userType"
                value="corporate"
                checked={selectedRadio === "corporate"}
                onChange={() => setSelectedRadio("corporate")}
              />
              <label htmlFor="corporate">Corporate</label>
            </div>
          </div>

          <div className="form-grid">
            <div className="form-column">
              
              <input
                type="text"
                {...register("CorporateName", { required: true })}
                placeholder="Corporate Name"
                className="user-input"
              />
              {errors.CorporateName && <span className="error">Corporate Name is required</span>}
              
        

              <input
                type="email"
                {...register("email", { 
                  required: "Email is required",
                  pattern: {
                    value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
                    message: "Invalid email address"
                  }
                })}
                placeholder="Email Address"
                className="user-input"
              />
              {errors.email && <span className="error">{errors.email.message}</span>}

              {selectedRadio === "individual" ? (
                <input
                  type="text"
                  {...register("aadhar", { 
                    required: "Aadhar number is required",
                    pattern: {
                      value: /^[2-9]{1}[0-9]{11}$/,
                      message: "Invalid Aadhar number"
                    }
                  })}
                  placeholder="Aadhar Number"
                  className="user-input"
                />
              ) : (
                <>
                  <input
                    type="text"
                    {...register("cinTaNumber", { 
                      required: "CIN/TA is required",
                      // pattern: {
                      //   value: /^[A-Z]{1}[0-9]{5}[A-Z]{2}[0-9]{4}[A-Z]{3}[0-9]{6}$/,
                      //   message: "Invalid CIN format (e.g., U12345AB6789CDE12345)"
                      // }
                    })}
                    placeholder="CIN/TA Number"
                    className="user-input"
                  />
                  {errors.cinTaNumber && <span className="error">{errors.cinTaNumber.message}</span>}
                  
                  {/* <input
                    type="text"
                    {...register("tanNumber", { 
                      required: "TAN is required",
                      pattern: {
                        value: /^[A-Z]{4}[0-9]{5}[A-Z]{1}$/,
                        message: "Invalid TAN format (e.g., ABCD12345E)"
                      }
                    })}
                    placeholder="TAN Number"
                    className="user-input"
                  />
                  {errors.tanNumber && <span className="error">{errors.tanNumber.message}</span>} */}
                </>
              )}
              {selectedRadio === "individual" && errors.aadhar && (
                <span className="error">{errors.aadhar.message}</span>
              )}
            </div>
            
            <div className="form-column">
              <input
                type="text"
                {...register("pancard", { 
                  required: "PAN is required",
                  pattern: {
                    value: /^[A-Z]{5}[0-9]{4}[A-Z]{1}$/,
                    message: "Invalid PAN format (e.g., ABCDE1234F)"
                  }
                })}
                placeholder="PAN Number"
                className="user-input"
              />
              {errors.pancard && <span className="error">{errors.pancard.message}</span>}

              <input
                type="text"
                {...register("mobilenumber", { 
                  required: "Mobile number is required",
                  pattern: {
                    value: /^[6-9][0-9]{9}$/,
                    message: "Invalid Indian mobile number"
                  }
                })}
                placeholder="Mobile Number"
                className="user-input"
              />
              {errors.mobilenumber && <span className="error">{errors.mobilenumber.message}</span>}
            </div>
          </div>

          {/* OTP Section */}
          {!otpSent && (
            <button
              type="button"
              className="send-otp-btn"
              onClick={handleSendOTP}
              disabled={!isFormComplete() || loading}
            >
              {loading ? "Sending OTP..." : "Send OTP"}
            </button>
          )}

          {otpSent && (
            <div className="otp-verification">
              <input
                type="text"
                placeholder="Enter OTP"
                value={otp}
                onChange={(e) => setOtp(e.target.value)}
                className="user-input"
              />
            </div>
          )}

          {/* Register Button */}
          <button
            type="submit"
            className={`register-btn ${!otpSent || otp !== "123456" || loading ? 'disabled' : ''}`}
            disabled={!otpSent || otp !== "123456" || loading}
          >
            {loading ? "Registering..." : "Register"}
          </button>
        </form>

        {/* Login Link */}
        <p className="login-link">
          Already have an account? <Link to="/login">Login here</Link>
        </p>
      </div>
      <ToastContainer />
    </div>
  );
};

export default Register;
