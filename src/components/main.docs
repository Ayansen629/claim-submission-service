const handleFileUpload = async (file) => {
    if (!file) return;

    setIsUploading(true);
    setUploadError(null);

    try {
      const result = await uploadDocument(file, 'identification');
      console.log("File upload result:", result);
      if (result.success) {
        // Directly set the form value with the upload result
        setValue("identificationDocument", {
          fileId: result.fileId,
          docName: result.docName,
          docType: result.docType,
          docSource: result.docSource,
          fileUrl: result.fileUrl,
          name: file.name // Keep the original file name for display
        }, { shouldValidate: true });
      } else {
        throw new Error(result.message || "File upload failed");
      }
    } catch (error) {
      console.error("Upload error:", error);
      setUploadError(error.message || "Failed to upload document");
      // Optionally clear the file input on error
      setValue("identificationDocument", null);
    } finally {
      setIsUploading(false);
    }
  };
