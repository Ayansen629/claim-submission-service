import React, { useState } from 'react';
import { useForm } from 'react-hook-form';
import { Link, useNavigate } from 'react-router-dom';
import { toast } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';
import './Register.css';

const CorporateDebtorRegister = () => {
  const navigate = useNavigate();
  const [loading, setLoading] = useState(false);
  
  const {
    register,
    handleSubmit,
    watch,
    formState: { errors },
  } = useForm();

  const processTypeOptions = [
    { value: 'CIRP', label: 'Corporate Insolvency Resolution Process (CIRP)' },
    { value: 'Liquidation', label: 'Liquidation' },
    { value: 'VoluntaryLiquidation', label: 'Voluntary Liquidation' },
    { value: 'PPIRP', label: 'Pre-Packaged Insolvency Resolution Process (PPIRP)' },
  ];

  const onSubmit = async (data) => {
    setLoading(true);
    setTimeout(() => {
      console.log('Registration Data:', {
        name: data.name,
        email: data.email,
        mobile: data.mobile,
        cin: data.cin,
        processType: data.processType
      });
      toast.success('Registration successful!');
      setLoading(false);
      navigate('/login');
    }, 1500);
  };

  const password = watch("password");

  return (
    <div className='register-page'>
      <div className="register-container">
        <div className="register-header">
          <div className="register-textmain">Corporate Debtor Registration</div>
        </div>

        <form className="register-form" onSubmit={handleSubmit(onSubmit)}>
          {/* Name */}
          <div className="form-group">
            <input
              type="text"
              {...register("name", { required: "Company name is required" })}
              placeholder="Company Name"
              className="user-input"
            />
            {errors.name && <span className="error">{errors.name.message}</span>}
          </div>

          {/* Email */}
          <div className="form-group">
            <input
              type="email"
              {...register("email", { 
                required: "Email is required",
                pattern: {
                  value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
                  message: "Invalid email address"
                }
              })}
              placeholder="Email ID"
              className="user-input"
            />
            {errors.email && <span className="error">{errors.email.message}</span>}
          </div>

          {/* Mobile Number */}
          <div className="form-group">
            <input
              type="tel"
              {...register("mobile", { 
                required: "Mobile number is required",
                pattern: {
                  value: /^[6-9]\d{9}$/,
                  message: "Invalid Indian mobile number"
                }
              })}
              placeholder="Mobile Number"
              className="user-input"
            />
            {errors.mobile && <span className="error">{errors.mobile.message}</span>}
          </div>

          {/* CIN Number */}
          <div className="form-group">
            <input
              type="text"
              {...register("cin", { 
                required: "CIN is required",
                pattern: {
                  value: /^[A-Z]{1}[0-9]{5}[A-Z]{2}[0-9]{4}[A-Z]{3}[0-9]{6}$/,
                  message: "Invalid CIN format (e.g., U12345AB6789CDE12345)"
                }
              })}
              placeholder="CIN Number"
              className="user-input"
            />
            {errors.cin && <span className="error">{errors.cin.message}</span>}
          </div>

          {/* Process Type */}
          <div className="dropdown-container">
            <select
              className="dropdown-select"
              {...register("processType", { required: "Process type is required" })}
            >
              <option value="">Process Type</option>
              {processTypeOptions.map(opt => (
                <option key={opt.value} value={opt.value}>{opt.label}</option>
              ))}
            </select>
            {errors.processType && <span className="error">{errors.processType.message}</span>}
          </div>

          {/* Password */}
          <div className="form-group">
            <input
              type="password"
              {...register("password", { 
                required: "Password is required",
                minLength: {
                  value: 8,
                  message: "Password must be at least 8 characters"
                }
              })}
              placeholder="Password"
              className="user-input"
            />
            {errors.password && <span className="error">{errors.password.message}</span>}
          </div>

          {/* Confirm Password */}
          <div className="form-group">
            <input
              type="password"
              {...register("confirmPassword", { 
                required: "Please confirm your password",
                validate: value => 
                  value === password || "Passwords do not match"
              })}
              placeholder="Confirm Password"
              className="user-input"
            />
            {errors.confirmPassword && <span className="error">{errors.confirmPassword.message}</span>}
          </div>

          {/* Register Button */}
          <button
            type="submit"
            className={`register-btn ${loading ? 'disabled' : ''}`}
            disabled={loading}
          >
            {loading ? "Registering..." : "Register"}
          </button>
        </form>

        <div className="login-link">
          Already have an account? <Link to="/login">Login here</Link>
        </div>
      </div>
    </div>
  );
};

export default CorporateDebtorRegister;
