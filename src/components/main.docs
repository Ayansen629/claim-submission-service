import React from 'react';
import DatePicker from 'react-datepicker';
import { format, isToday, isSameDay, parseISO } from 'date-fns';
import 'react-datepicker/dist/react-datepicker.css';
import './surveyTrackerCard.css';

const SurveyTrackerCard = ({ events }) => {
  const today = new Date();
  
  // Custom day component to highlight events and today
  const renderDayContents = (day, date) => {
    const dayEvents = events.filter(event => 
      isSameDay(parseISO(event.start), date)
    );
    const isCurrentDay = isToday(date);
    
    return (
      <div className={`day-container ${isCurrentDay ? 'today' : ''}`}>
        <div className="day-number">{format(date, 'd')}</div>
        {dayEvents.length > 0 && (
          <div className="event-indicators">
            {dayEvents.map((event, i) => (
              <div 
                key={i} 
                className="event-indicator" 
                style={{ backgroundColor: event.color || '#2466BF' }}
              />
            ))}
          </div>
        )}
      </div>
    );
  };

  return (
    <div className="survey-tracker-card">
      <div className="calendar-header">
        <h5>Survey Tracker</h5>
        <p>{events.length} upcoming surveys</p>
      </div>
      <DatePicker
        inline
        selected={today}
        renderDayContents={renderDayContents}
        calendarClassName="survey-calendar"
        showMonthDropdown
        showYearDropdown
        dropdownMode="select"
      />
    </div>
  );
};

export default SurveyTrackerCard;
