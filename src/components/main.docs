import React, { useState, useEffect, useRef } from "react";
import style from "./ActionTable.module.css";
import _ from "lodash";
import editIcon from "../../../assests/icons/edit.svg";
import viewIcon from "../../../assests/icons/view.svg";
import deleteIcon from "../../../assests/icons/delete.svg";
import { BsGeoAltFill } from "react-icons/bs";
import Tooltip from "@mui/material/Tooltip";
import { Dropdown } from "react-bootstrap";

const ActionTable = (props) => {
  const [searchedData, setSearchedData] = useState(props.rowData || []);
  const [currentPage, setCurrentPage] = useState(1);
  const [selected, setSelected] = useState([]);
  const [sortedColumn, setSortedColumn] = useState(null);
  const [sortOrder, setSortOrder] = useState("");
  const hideColumns = props.hideColumns || [];
  const [searchQuery, setSearchQuery] = useState("");
  const [statusFilter, setStatusFilter] = useState("all");
  const [dateSort, setDateSort] = useState("none");
  const [itemsPerPage, setItemPerPage] = useState(10);

  useEffect(() => {
    if (props.rowData) {
      setSearchedData(props.rowData);
      setCurrentPage(1);
      const selectedRows = props.rowData
        .filter((item) => item.isChecked)
        .map((item) => item.id);
      setSelected(selectedRows);
    }
  }, [props.rowData]);

  // Apply filters and sorting
  useEffect(() => {
    let filteredData = [...(props.rowData || [])];
    
    // Apply status filter
    if (statusFilter !== "all") {
      filteredData = filteredData.filter(item => item.status === statusFilter);
    }
    
    // Apply search query
    if (searchQuery) {
      const searchValue = searchQuery.toLowerCase();
      filteredData = filteredData.filter(item => 
        Object.values(item).some(value => 
          value?.toString().toLowerCase().includes(searchValue)
        )
      );
    }
    
    // Apply date sorting
    if (dateSort !== "none") {
      filteredData.sort((a, b) => {
        // Try multiple possible date fields with fallbacks
        const getDate = (obj) => {
          const dateStr = obj.dateSubmitted || obj.createdAt || obj.date || obj.submissionDate;
          if (!dateStr) return 0;
          const date = new Date(dateStr);
          return isNaN(date.getTime()) ? 0 : date.getTime();
        };
        
        const dateA = getDate(a);
        const dateB = getDate(b);
        
        return dateSort === "asc" ? dateA - dateB : dateB - dateA;
      });
    }
    
    setSearchedData(filteredData);
    setCurrentPage(1);
  }, [props.rowData, statusFilter, searchQuery, dateSort]);

  // ... keep all your existing functions unchanged ...

  return (
    <div className="mb-5 px-0 mx-0 generic-action-table pt-3">
      {/* Search and Filter Controls (unchanged) */}

      {/* Table */}
      <div className={`${style.table_container} mb-3`}>
        <table className={`table ${style.table}`}>
          <thead>
            {/* Table header (unchanged) */}
          </thead>
          <tbody>
            {currentItems.length > 0 ? (
              currentItems.map((row, index) => {
                const isItemSelected = isSelected(row.id);
                return (
                  <tr 
                    key={index} 
                    className={`${style.table_datarow} ${isItemSelected ? style.selected_row : ''}`}
                  >
                    <td className="px-3 py-2">
                      <input
                        type="checkbox"
                        checked={isItemSelected}
                        onChange={(e) => handleRowSelect(e, row)}
                      />
                    </td>
                    
                    {props.columns.map((o, colIndex) => {
                      if (o.isClickable) {
                        return (
                          <td
                            key={colIndex}
                            className={`px-3 py-2 ${o.clsName} ${style.clickable_cell}`}
                            onClick={(e) => props.additionalMethod?.(row)}
                          >
                            <span className={row.isRead === false ? style.unread : ''}>
                              {_.get(row, o.fieldName)}
                            </span>
                          </td>
                        );
                      } else if (!hideColumns.includes(o.fieldName) && o.fieldName !== "action") {
                        const cellValue = _.get(row, o.fieldName) || '';
                        const showTooltip = ['claimnumber', 'status', 'comments', 'comment'].includes(o.fieldName.toLowerCase());
                        
                        return (
                          <td
                            key={colIndex}
                            className={`px-3 py-2 ${o.clsName} ${showTooltip ? style.tooltip_cell : ''}`}
                          >
                            {o.colName === "Status" ? (
                              <div className={style.tooltip_cell}>
                                <span className={`${style.status_badge} ${style[_.get(row, o.fieldName) + '_status'] || ''} ${style.truncate_text}`}>
                                  {cellValue || "No Status"}
                                </span>
                                {showTooltip && (
                                  <span className={style.tooltip_text}>
                                    {cellValue}
                                  </span>
                                )}
                              </div>
                            ) : (
                              <div className={showTooltip ? style.tooltip_cell : ''}>
                                <span className={`${row.isRead === false ? style.unread : ''} ${showTooltip ? style.truncate_text : ''}`}>
                                  {cellValue}
                                  {o.fieldName2 ? _.get(row, o.fieldName2) : ""}
                                </span>
                                {showTooltip && (
                                  <span className={style.tooltip_text}>
                                    {cellValue}
                                  </span>
                                )}
                              </div>
                            )}
                          </td>
                        );
                      } else if (!hideColumns.includes(o.fieldName) && o.fieldName === "action") {
                        // ... keep your existing action cell code
                      }
                      return null;
                    })}
                  </tr>
                );
              })
            ) : (
              <tr>
                <td colSpan={props.columns.length + 1} className="text-center py-5">
                  <div className={style.no_records}>
                    <i className="fa fa-database me-2" />
                    No records found matching your criteria
                  </div>
                </td>
              </tr>
            )}
          </tbody>
        </table>
      </div>

      {/* Table Footer (unchanged) */}
    </div>
  );
};

export default ActionTable;
/////////////////
/* Add these to your ActionTable.module.css */
.truncate-text {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 150px; /* Adjust as needed */
  display: inline-block;
  vertical-align: middle;
}

.tooltip-cell {
  position: relative;
}

.tooltip-cell .tooltip-text {
  visibility: hidden;
  width: auto;
  min-width: 200px;
  max-width: 400px;
  background-color: #555;
  color: #fff;
  text-align: center;
  border-radius: 6px;
  padding: 8px;
  position: absolute;
  z-index: 100;
  bottom: 125%;
  left: 50%;
  transform: translateX(-50%);
  opacity: 0;
  transition: opacity 0.3s;
  white-space: normal;
  word-wrap: break-word;
}

.tooltip-cell:hover .tooltip-text {
  visibility: visible;
  opacity: 1;
}
/////////////
