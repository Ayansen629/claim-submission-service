import React, { useState } from "react";
import { Button, Modal, Form } from "react-bootstrap";
import InputFile from "./InputFile";

const PaymentModal = ({
  show,
  onHide,
  paymentMode,
  setPaymentMode,
  challanFile,
  onChallanUpload,
  onOnlinePay,
  isLoading,
  formMethods, // From react-hook-form
  supervisionAuthority, // From Invoicing
}) => {
  const [submitting, setSubmitting] = useState(false);

  const handleOfflineSubmit = (e) => {
    e.preventDefault();
    setSubmitting(true);

    // Get all form data from react-hook-form
    const formData = formMethods.getValues();

    // Log all data in a structured way
    console.log("===== VESSEL REGISTRATION FORM DATA =====");
    console.log("1. Vessel Details:", {
      basicInfo: {
        ownerName: formData.ownerName,
        ownerAddress: formData.ownerAddress,
        mobile: formData.mobile,
        email: formData.email,
        pan: formData.pan,
        aadhar: formData.aadhar,
        tradeLicence: formData.tradeLicence,
        gst: formData.gst,
        grt: formData.grt,
      },
      vesselSpecs: {
        vesselType: formData.vesselType,
        category: formData.category,
        zone: formData.zone,
        supervisionAuthority: formData.supervisionAuthority,
        purchaseType: formData.purchaseType,
        classificationSociety: formData.classificationSociety,
      },
      builderDetails: formData.constructedBy === "2" ? {
        builderName: formData.builderName,
        builderAddress: formData.builderAddress,
        builderEmail: formData.builderEmail,
        builderMobile: formData.builderMobile,
        builderTradeLicence: formData.builderTradeLicence,
        builderGST: formData.builderGST,
        builderAuth: formData.builderAuth,
        authLetter: formData.authLetter,
        workOrderValue: formData.workOrderValue,
      } : null,
      documents: {
        workOrderFiles: formData.workOrderFiles, // If using MultipleFileUpload
        drawingFiles: formData.drawingFiles, // If using MultipleFileUpload
        tonnageBooklets: formData.tonnageBooklets,
        stabilityBooklets: formData.stabilityBooklets,
      },
      declaration: {
        termsAccepted: formData.termsAccepted,
      },
    });

    console.log("2. Invoicing Details:", {
      supervisionAuthority: supervisionAuthority,
      paymentMode: paymentMode,
      challanFile: challanFile?.name || null,
    });

    console.log("===== END OF FORM DATA =====");

    setTimeout(() => {
      setSubmitting(false);
      onHide();
      onOnlinePay();
    }, 800);
  };

  return (
    <Modal show={show} onHide={onHide} centered>
      <Modal.Header closeButton>
        <Modal.Title>Payment Option</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <Form onSubmit={handleOfflineSubmit}>
          <Form.Group>
            <Form.Label className="fw-semibold mb-2">Select Payment Mode</Form.Label>
            <div className="d-flex gap-3 mb-3">
              <Form.Check
                type="radio"
                id="offline"
                label="Offline"
                checked={paymentMode === "offline"}
                onChange={() => setPaymentMode("offline")}
                name="paymentMode"
              />
              <Form.Check
                type="radio"
                id="online"
                label="Online"
                checked={paymentMode === "online"}
                onChange={() => setPaymentMode("online")}
                name="paymentMode"
              />
            </div>
          </Form.Group>
          {paymentMode === "offline" && (
            <div className="mb-3">
              <InputFile
                label="Upload Challan"
                name="challan"
                required
                value={challanFile}
                onChange={onChallanUpload}
                errors={{}}
              />
            </div>
          )}
          <div className="d-flex justify-content-end gap-2 mt-4">
            <Button
              variant="secondary"
              onClick={onHide}
              style={{
                minWidth: 100,
                fontWeight: 500,
                borderRadius: 6,
                fontSize: "1rem",
              }}
              disabled={isLoading}
            >
              Close
            </Button>
            {paymentMode === "offline" ? (
              <Button
                type="submit"
                variant="primary"
                disabled={submitting || isLoading}
                style={{
                  minWidth: 120,
                  fontWeight: 600,
                  borderRadius: 6,
                  fontSize: "1rem",
                }}
              >
                {submitting ? "Submitting..." : "Submit"}
              </Button>
            ) : (
              <Button
                variant="success"
                onClick={onOnlinePay}
                disabled={isLoading}
                style={{
                  minWidth: 160,
                  fontWeight: 600,
                  borderRadius: 6,
                  fontSize: "1rem",
                }}
              >
                Proceed to Payment Gateway
              </Button>
            )}
          </div>
        </Form>
      </Modal.Body>
    </Modal>
  );
};

export default PaymentModal;
