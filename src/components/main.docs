const handleConditionChange = useCallback((fieldName, selectedOption) => {
  // react-select passes the full option object, we need just the value
  const value = selectedOption ? selectedOption.value : "";
  setValue(fieldName, value, { shouldValidate: true, shouldDirty: true });
}, [setValue]);
