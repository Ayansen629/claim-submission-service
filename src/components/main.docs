const validateDocuments = async () => {
  if (isViewMode) return true;

  const requiredFields = [];
  const grossTonnage = getValues("grt");

  // Check tonnage booklets requirement
  const tonnageBooklets = getValues("tonnageBooklets");
  let isTonnageRequired = false;
  
  if (isEditMode) {
    const hasFileId = tonnageBooklets?.id;
    const hasGrtValue = grossTonnage && grossTonnage.trim() !== "";
    isTonnageRequired = !!(hasFileId || hasGrtValue);
  } else {
    // Normal mode - only required if GRT has value
    isTonnageRequired = !!(grossTonnage && grossTonnage.trim() !== "");
  }

  if (isTonnageRequired && !tonnageBooklets) {
    setError("tonnageBooklets", {
      type: "required",
      message: t("Tonnage Booklets is required"),
    });
    requiredFields.push("tonnageBooklets");
  }

  // Check stability booklets requirement
  const stabilityBooklets = getValues("stabilityBooklets");
  let isStabilityRequired = false;
  
  if (isEditMode) {
    const hasFileId = stabilityBooklets?.id;
    const hasGrtValue = grossTonnage && grossTonnage.trim() !== "";
    isStabilityRequired = !!(hasFileId || hasGrtValue);
  } else {
    // Normal mode - only required if GRT has value
    isStabilityRequired = !!(grossTonnage && grossTonnage.trim() !== "");
  }

  if (isStabilityRequired && !stabilityBooklets) {
    setError("stabilityBooklets", {
      type: "required",
      message: t("Stability Booklets is required"),
    });
    requiredFields.push("stabilityBooklets");
  }

  // Terms acceptance is always required
  const termsAccepted = getValues("termsAccepted");
  if (!termsAccepted) {
    setError("termsAccepted", {
      type: "required",
      message: t("You must accept the terms and conditions"),
    });
    requiredFields.push("termsAccepted");
  }

  // Trigger validation for these fields
  await trigger(["tonnageBooklets", "stabilityBooklets", "termsAccepted"]);

  return requiredFields.length === 0;
};
