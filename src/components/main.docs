import React, { useCallback, useEffect } from "react";
import { useForm, FormProvider } from "react-hook-form";
import DynamicField from "./formCommon/DynamicField";
import Collapsible from "./formCommon/Collapsible";
import 'bootstrap/dist/css/bootstrap.min.css';

const StepThird = ({ provideSubmitHandler, formData }) => {
    const methods = useForm({
        mode: "onChange", // Validate on every change
        reValidateMode: "onChange" // Re-validate on every change
    });
    
    const { 
        register, 
        handleSubmit, 
        formState: { errors, isValid }, 
        trigger, 
        setValue, 
        control, 
        getValues,
        watch
    } = methods;

    // Watch all fields to trigger re-render on changes
    const watchAllFields = watch();

    const financialCreditorDetailsSchema = [
        { 
            name: "creditorName", 
            label: <>Name of financial creditor or person authorised to act on its behalf (in BLOCK LETTERS) <span className="required-asterisk">*</span></>, 
            type: "text", 
            rules: { 
                required: "Creditor name is required",
                pattern: {
                    value: /^[A-Z\s]+$/,
                    message: "Please enter in BLOCK LETTERS only"
                },
                minLength: {
                    value: 3,
                    message: "Name must be at least 3 characters"
                }
            },
            placeholder: "Enter name in BLOCK LETTERS",
            className: "mb-3",
            onChange: async (e) => {
                await trigger("creditorName");
            }
        },
        { 
            name: "creditorPosition", 
            label: <>Position with or in relation to creditor <span className="required-asterisk">*</span></>, 
            type: "text", 
            rules: { 
                required: "Position is required",
                minLength: {
                    value: 3,
                    message: "Position must be at least 3 characters"
                }
            },
            placeholder: "Enter position/relation to creditor",
            className: "mb-3",
            onChange: async (e) => {
                await trigger("creditorPosition");
            }
        },
        { 
            name: "creditorAddress", 
            label: <>Address of person signing <span className="required-asterisk">*</span></>, 
            type: "textarea",
            rules: { 
                required: "Address is required",
                minLength: {
                    value: 10,
                    message: "Address must be at least 10 characters"
                }
            },
            placeholder: "Enter complete address",
            className: "mb-3",
            rows: 3,
            onChange: async (e) => {
                await trigger("creditorAddress");
            }
        },
    ];

    const onSubmit = useCallback(async () => {
        // Trigger validation for all fields
        const isValid = await trigger();
        
        if (!isValid) {
            // Scroll to the first error
            const errorField = Object.keys(errors)[0];
            if (errorField) {
                document.querySelector(`[name="${errorField}"]`)?.scrollIntoView({ 
                    behavior: 'smooth', 
                    block: 'center' 
                });
            }
            return false;
        }
        
        return getValues();
    }, [trigger, getValues, errors]);

    useEffect(() => {
        if (formData) {
            Object.keys(formData).forEach((key) => {
                setValue(key, formData[key], { shouldValidate: true });
            });
        }
    }, [formData, setValue]);

    useEffect(() => {
        if (provideSubmitHandler) {
            provideSubmitHandler(onSubmit);
        }
    }, [provideSubmitHandler, onSubmit]);

    return (
        <FormProvider {...methods}>
            <form className="form-stepper">
                <div className="mb-4 bg-light p-4 rounded">
                    <Collapsible title="Financial Creditor Details" defaultOpen={true}>
                        <div className="row g-3">
                            {financialCreditorDetailsSchema.map((fieldConfig) => (
                                <div key={fieldConfig.name} className="col-md-12">
                                    <DynamicField
                                        fieldConfig={fieldConfig}
                                        control={control}
                                        register={register}
                                        errors={errors}
                                        setValue={setValue}
                                        trigger={trigger}
                                        getValues={getValues}
                                    />
                                    {errors[fieldConfig.name] && (
                                        <div className="invalid-feedback d-block">
                                            {errors[fieldConfig.name].message}
                                        </div>
                                    )}
                                </div>
                            ))}
                        </div>
                    </Collapsible>
                </div>
            </form>
        </FormProvider>
    );
};

export default StepThird;
