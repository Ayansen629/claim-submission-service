const onSubmit = async (data) => {
  console.log("Form Data:", data);
  try {
    loaderRef.current?.show();

    const uInfo = {
      userType: "EMPLOYEE",
      userBasicDetails: {},
      employeeDetails: {
        id: isEditMode ? employeeData?.id : "",
        firstName: data.firstName,
        lastName: data.lastName,
        emailId: data.email,
        phoneNumber: data.phoneNumber,
        addressLine1: data.addressLine1,
        addressLine2: data.addressLine2,
        pin: data.pin,
        district: data.district,
        state: data.stateUt,
        country: data.country,
        isActive: data?.isActive,
        dateOfBirth: data.dateOfBirth,
        gender: data.gender,
        appointmentDate: data.appointmentDate,
        employeeType: data.employeeType,
        employeeStatus: data.employeeStatus,
        designation: data.designation,
        department: data.department,
        section: data.section,
        roles: data.roles,
        educationDetails: data?.educationDetails?.map((ed) => ({
          uuid: isEditMode ? ed.uuid : "",
          qualification: ed.qualification,
          stream: ed.stream,
          yearOfPassing: ed.yearOfPassing,
        })),
      },
    };

    // If in edit mode, include existing file references
    if (isEditMode && employeeData?.files) {
      uInfo.employeeDetails.files = employeeData.files.map(file => ({
        fileStoreId: file.fileStoreId,
        alfrescoId: file.alfrescoId,
        registrationNumber: file.registrationNumber,
        fileName: file.fileName,
        module: file.module,
        tenantId: file.tenantId,
        tag: file.tag,
        fieldName: file.fieldName,
        downLoadFile: file.downLoadFile
      }));
    }

    const formData = new FormData();
    formData.append(
      "request",
      new Blob([JSON.stringify(uInfo)], {
        type: "application/json",
      })
    );

    // Add files to formData only if they exist
    if (addressProofFile) {
      formData.append("files", addressProofFile);
    }
    
    if (identityProofFile) {
      formData.append("files", identityProofFile);
    }
    
    if (appointmentLetterFile) {
      formData.append("files", appointmentLetterFile);
    }

    await submitEmpDetails(formData);
  } catch (error) {
    loaderRef.current?.hide();
    toast.error(
      error?.response?.data?.message ||
        "Error submitting form. Please try again."
    );
  }
};
