// In VesselOwnerRegistrationPage component

// Update the handleNext and handlePrevious functions to properly handle file objects
const handleNext = async () => {
  const isStepValid = await validateStep();
  
  if (isStepValid) {
    setValidationMessage("");
    
    // Save all form values including file objects
    const currentValues = getValues();
    setFormData(prev => ({ ...prev, ...currentValues }));
    
    if (currentStep < totalSteps) {
      setCurrentStep(currentStep + 1);
      window.scrollTo(0, 0);
    } else {
      handleSubmit(onSubmit)();
    }
  }
};

const handlePrevious = () => {
  // Save current values before navigating
  const currentValues = getValues();
  setFormData(prev => ({ ...prev, ...currentValues }));
  
  // Navigate to previous step
  setCurrentStep(Math.max(1, currentStep - 1));
  window.scrollTo(0, 0);
  setValidationMessage("");
};

// Update the onSubmit function to log all form data including files
const onSubmit = (data) => {
  const finalData = { ...formData, ...data };
  
  // Create FormData object for file uploads
  const formDataToSend = new FormData();
  
  // Append all form data
  Object.keys(finalData).forEach(key => {
    if (finalData[key] instanceof File) {
      formDataToSend.append(key, finalData[key]);
    } else if (finalData[key] !== undefined && finalData[key] !== null) {
      formDataToSend.append(key, finalData[key]);
    }
  });
  
  console.log('Form submitted with data:', finalData);
  console.log('FormData object:', formDataToSend);
  
  setIsSubmitted(true);
  window.scrollTo(0, 0);
};

/////////////////
// In InputFile component

const InputFile: React.FC<InputFileProps> = ({
  label,
  required,
  name,
  register,
  errors,
  onChange,
  accept = ".pdf,.jpg,.jpeg,.png",
  disabled,
  className = "",
  style = {},
  watch
}) => {
  const hasError = errors && errors[name];
  const [fileName, setFileName] = useState<string>("");
  const [fileObject, setFileObject] = useState<File | null>(null);
  const inputRef = React.useRef<HTMLInputElement>(null);
  
  // Handle both string values (from form state) and File objects
  useEffect(() => {
    if (watch) {
      const watchedValue = watch(name);
      
      if (watchedValue instanceof File) {
        setFileName(watchedValue.name);
        setFileObject(watchedValue);
      } else if (typeof watchedValue === 'string' && watchedValue) {
        setFileName(watchedValue);
      } else {
        setFileName("");
        setFileObject(null);
      }
    }
  }, [watch, name]);
  
  // Handle file change
  const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    if (e.target.files && e.target.files[0]) {
      const file = e.target.files[0];
      setFileName(file.name);
      setFileObject(file);
      
      // Update form value with the File object
      if (onChange) {
        onChange({
          ...e,
          target: {
            ...e.target,
            value: file  // Send the File object instead of the event
          }
        });
      }
    }
  };
  
  // Get file icon based on extension
  const getFileIcon = () => {
    if (!fileName) return <FaFile color="#0d6efd" />;
    
    const extension = fileName.split('.').pop()?.toLowerCase();
    
    if (extension === 'pdf') return <FaFilePdf color="#dc3545" />;
    if (['jpg', 'jpeg', 'png', 'gif'].includes(extension || '')) return <FaFileImage color="#28a745" />;
    return <FaFile color="#0d6efd" />;
  };
  
  return (
    <Form.Group className={`mb-4 ${className}`} controlId={name}>
      <Form.Label className="fw-bold text-dark">
        {label}
        {required && <span className="text-danger ms-1">*</span>}
      </Form.Label>
      
      <div 
        className={`file-upload border rounded p-3 ${hasError ? 'border-danger' : ''}`} 
        style={{
          ...style,
          backgroundColor: hasError ? '#fff8f8' : 'white',
          borderColor: hasError ? '#dc3545' : '#ced4da',
          borderRadius: '8px',
          transition: 'all 0.3s ease'
        }}
      >
        <div className="d-flex align-items-center justify-content-between">
          <label 
            htmlFor={name} 
            className={`file-label btn ${hasError ? 'btn-outline-danger' : 'btn-primary'} mb-0 d-inline-flex align-items-center`}
            style={{ 
              borderRadius: '6px',
              padding: '8px 16px',
              cursor: 'pointer',
              boxShadow: '0 2px 5px rgba(0,0,0,0.1)',
              transition: 'all 0.3s ease',
              minWidth: '120px'
            }}
          >
            <FaUpload className="file-icon me-2" />
            <span className="file-text">Choose File</span>
          </label>
          
          <div 
            className={`file-name-container ms-3 flex-grow-1 border rounded px-3 py-2 ${fileName ? 'bg-light-success' : 'bg-light'} d-flex align-items-center ${hasError ? 'border-danger' : fileName ? 'border-success' : ''}`}
            style={{
              backgroundColor: fileName ? '#f0f9f0' : '#f8f9fa'
            }}
          >
            {fileName ? (
              <>
                <FaCheckCircle className="text-success me-2" />
                <div>
                  <span className="text-success fw-bold" style={{ fontSize: '0.8rem' }}>Selected File:</span>
                  <div className="d-flex align-items-center">
                    <span className="file-icon-preview me-2">{getFileIcon()}</span>
                    <span className="text-dark fw-semibold" style={{ overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
                      {fileName}
                    </span>
                  </div>
                </div>
              </>
            ) : (
              <span className="text-muted">No file chosen</span>
            )}
          </div>
        </div>
        
        <input 
          type="file" 
          id={name}
          ref={inputRef}
          {...register(name)}
          onChange={handleFileChange}
          accept={accept}
          disabled={disabled}
          className="file-input d-none"
        />
      </div>
      
      <Form.Text className="text-muted small mt-1">
        Upload PDF, JPG or PNG file (Max size: 2MB)
      </Form.Text>
      
      {hasError && (
        <Form.Text className="text-danger small mt-1 d-block">
          {errors[name]?.message}
        </Form.Text>
      )}
    </Form.Group>
  );
};
