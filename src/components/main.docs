const handlePayNow = () => {
  // Get all form data before showing payment modal
  const currentStepInfo = {
    index: activeStep,
    label: currentStep?.label,
    isLastStep: true,
    getFormData: currentStep?.getFormData,
  };
  
  console.log("=== STEPPER PAY NOW ===");
  console.log("Final Step Info:", currentStepInfo);
  console.log("All Steps Completed:", completed);
  console.log("======================");
  
  onPayNow?.() || currentStep?.component?.props?.onPayNow?.();
};
