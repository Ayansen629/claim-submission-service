import React, { useState, useEffect, useRef } from "react";
import { BsGeoAltFill } from "react-icons/bs";
import Tooltip from "@mui/material/Tooltip";
import { Dropdown } from "react-bootstrap";
import { cn } from "@/lib/utils" // If you have a utility for combining class names

// Mock styles (or import your actual CSS/SCSS module)
const style = {
  action_table_wrapper: 'w-full', // Added for full width
  table_controls: 'flex flex-wrap items-center justify-between gap-4',
  search_input_group: 'flex items-center gap-2',
  search_input: 'w-full sm:w-[200px] md:w-[250px] px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500',
  custom_button: 'px-4 py-2 rounded-md',
  table_container: 'w-full overflow-x-auto',
  table: 'min-w-full', // Ensure table takes full width
  table_header: 'bg-gray-100',
  th: 'px-4 py-3 text-left font-semibold',
  checkbox_column: 'w-12', // Consistent width for checkbox column
  sortable_header: 'cursor-pointer select-none',
  table_datarow: 'hover:bg-gray-50',
  selected_row: 'bg-blue-100',
  td: 'px-4 py-3',
  action_cell: 'flex justify-center gap-2',
  action_button: 'p-1 rounded-md',
  edit_button: 'text-blue-500 hover:text-blue-700',
  view_button: 'text-green-500 hover:text-green-700',
  track_button: 'text-purple-500 hover:text-purple-700',
  delete_button: 'text-red-500 hover:text-red-700',
  status_badge: 'px-2 py-1 rounded-full text-xs font-semibold',
  unread: 'font-semibold',
  no_records: 'py-8 text-center text-gray-500',
  table_footer: 'flex flex-wrap items-center justify-between gap-4 py-3',
  selection_info: 'text-sm text-gray-600',
  pagination_button: 'px-3 py-1 rounded-md',
  'approve-status': 'bg-green-100 text-green-800',
  'initiate-status': 'bg-blue-100 text-blue-800',
  'pending-status': 'bg-yellow-100 text-yellow-800',
  'sent back-status': 'bg-orange-100 text-orange-800',
  'draft-status': 'bg-gray-200 text-gray-700',
};

const ActionTable = (props) => {
  const [searchedData, setSearchedData] = useState(props.rowData || []);
  const [currentPage, setCurrentPage] = useState(1);
  const [selected, setSelected] = useState([]);
  const [sortedColumn, setSortedColumn] = useState(null);
  const [sortOrder, setSortOrder] = useState("");
  const hideColumns = props.hideColumns || [];
  const [searchQuery, setSearchQuery] = useState("");
  const [statusFilter, setStatusFilter] = useState("all");
  const [dateSort, setDateSort] = useState("none");
  const [itemsPerPage, setItemsPerPage] = useState(10);

  useEffect(() => {
    if (props.rowData) {
      setSearchedData(props.rowData);
      setCurrentPage(1);
      const selectedRows = props.rowData
        .filter((item) => item.isChecked)
        .map((item) => item.id);
      setSelected(selectedRows);
    }
  }, [props.rowData]);

  // Apply filters and sorting
  useEffect(() => {
    let filteredData = [...(props.rowData || [])];

    // Apply status filter
    if (statusFilter !== "all") {
      filteredData = filteredData.filter(item => item.status === statusFilter);
    }

    // Apply search query
    if (searchQuery) {
      const searchValue = searchQuery.toLowerCase();
      filteredData = filteredData.filter(item =>
        Object.values(item).some(value =>
          value?.toString().toLowerCase().includes(searchValue)
        )
      );
    }

    // Apply date sorting
    if (dateSort !== "none") {
      filteredData.sort((a, b) => {
        const dateA = new Date(a.dateSubmitted || a.createdAt || a.date);
        const dateB = new Date(b.dateSubmitted || b.createdAt || b.date);
        return dateSort === "asc" ? dateA - dateB : dateB - dateA;
      });
    }

    // Apply column sorting
    if (sortedColumn) {
      filteredData.sort((a, b) => {
        const valueA = _.get(a, sortedColumn);
        const valueB = _.get(b, sortedColumn);

        if (typeof valueA === 'string' && typeof valueB === 'string') {
          return sortOrder === "asc" ? valueA.localeCompare(valueB) : valueB.localeCompare(valueA);
        } else if (typeof valueA === 'number' && typeof valueB === 'number') {
          return sortOrder === "asc" ? valueA - valueB : valueB - valueA;
        } else if (valueA instanceof Date && valueB instanceof Date) {
          return sortOrder === "asc" ? valueA.getTime() - valueB.getTime() : valueB.getTime() - valueA.getTime();
        } else {
          // Handle cases where data types are mixed or not directly comparable
          return sortOrder === "asc" ? String(valueA).localeCompare(String(valueB)) : String(valueB).localeCompare(String(valueA));
        }
      });
    }

    setSearchedData(filteredData);
    setCurrentPage(1);
  }, [props.rowData, statusFilter, searchQuery, dateSort, sortedColumn, sortOrder]);

  const indexOfLastItem = currentPage * itemsPerPage;
  const indexOfFirstItem = indexOfLastItem - itemsPerPage;
  const currentItems = searchedData.slice(indexOfFirstItem, indexOfLastItem);
  const totalPages = Math.ceil((searchedData?.length || 0) / itemsPerPage);

  const handleNextPage = () => {
    setCurrentPage((prevPage) => Math.min(prevPage + 1, totalPages));
  };

  const handlePrevPage = () => {
    setCurrentPage((prevPage) => Math.max(prevPage - 1, 1));
  };

  const handleOnSearch = (e) => {
    setSearchQuery(e.target.value);
  };

  const sortByColumn = (column) => {
    if (sortedColumn === column) {
      setSortOrder(sortOrder === "asc" ? "desc" : "asc");
    } else {
      setSortedColumn(column);
      setSortOrder("asc");
    }
  };

  const showSortedColumnIcon = (columnName) => {
    return sortedColumn === columnName ? (
      sortOrder === "asc" ? (
        <i className="fa fa-long-arrow-up table-arrow" aria-hidden="true" />
      ) : (
        <i className="fa fa-long-arrow-down table-arrow" aria-hidden="true" />
      )
    ) : null;
  };

  const handleDeleteRow = (rowData) => {
    props.onDeleteRow?.(rowData);
  };

  const handleTrack = (rowData) => {
    props.onTrackRow?.(rowData);
  };

  const handleView = (rowData) => {
    props.onViewRow?.(rowData);
  };

  const handleEdit = (rowData) => {
    props.handleEditRow?.(rowData);
  };

  const handleSelectAll = (e) => {
    if (e.target.checked) {
      const newSelecteds = currentItems.map(n => n.id);
      setSelected(newSelecteds);
      props.onCheckHandler?.(newSelecteds);
    } else {
      setSelected([]);
      props.onCheckHandler?.([]);
    }
  };

  const handleRowSelect = (e, row) => {
    const selectedId = row.id;
    if (e.target.checked) {
      const newSelected = [...selected, selectedId];
      setSelected(newSelected);
      props.onCheckHandler?.(newSelected);
    } else {
      const newSelected = selected.filter(id => id !== selectedId);
      setSelected(newSelected);
      props.onCheckHandler?.(newSelected);
    }
  };

  const isSelected = (id) => {
    return selected.includes(id);
  };

  const getStatusOptions = () => {
    const statuses = [...new Set(props.rowData?.map(item => item.status))];
    return ["all", ...statuses].filter(Boolean);
  };

  const StatusFilterDropdown = () => (
    <Dropdown className="ms-2">
      <Dropdown.Toggle
        variant="outline-secondary"
        id="status-filter-dropdown"
        className="d-flex align-items-center"
      >
        <i className="fa fa-filter me-2" />
        Status: {statusFilter === "all" ? "All" : statusFilter}
      </Dropdown.Toggle>
      <Dropdown.Menu className="dropdown-menu-end">
        {getStatusOptions().map((status, index) => (
          <Dropdown.Item
            key={index}
            onClick={() => setStatusFilter(status)}
            active={status === statusFilter}
            className="d-flex align-items-center"
          >
            {status === "all" ? "All Statuses" : status}
            {status === statusFilter && <i className="fa fa-check ms-2" />}
          </Dropdown.Item>
        ))}
      </Dropdown.Menu>
    </Dropdown>
  );

  const DateSortDropdown = () => (
    <Dropdown className="ms-2">
      <Dropdown.Toggle
        variant="outline-secondary"
        id="date-sort-dropdown"
        className="d-flex align-items-center"
      >
        <i className="fa fa-calendar me-2" />
        Date: {dateSort === "none" ? "Default" : dateSort === "asc" ? "Oldest First" : "Newest First"}
      </Dropdown.Toggle>
      <Dropdown.Menu className="dropdown-menu-end">
        <Dropdown.Item
          onClick={() => setDateSort("none")}
          active={dateSort === "none"}
          className="d-flex align-items-center"
        >
          Default Order
          {dateSort === "none" && <i className="fa fa-check ms-2" />}
        </Dropdown.Item>
        <Dropdown.Item
          onClick={() => setDateSort("asc")}
          active={dateSort === "asc"}
          className="d-flex align-items-center"
        >
          Oldest First
          {dateSort === "asc" && <i className="fa fa-check ms-2" />}
        </Dropdown.Item>
        <Dropdown.Item
          onClick={() => setDateSort("desc")}
          active={dateSort === "desc"}
          className="d-flex align-items-center"
        >
          Newest First
          {dateSort === "desc" && <i className="fa fa-check ms-2" />}
        </Dropdown.Item>
      </Dropdown.Menu>
    </Dropdown>
  );

  const PaginationOptions = () => {
    const exportRef = useRef(null);


    return (
      <div
        id="export_button"
        ref={exportRef}
        className="d-inline-flex flex-row align-items-center"
      >
        <div className="pe-2">Rows per page</div>
        <Dropdown>
          <Dropdown.Toggle
            variant="outline-secondary"
            id="dropdownMenuButton1"
            className="d-flex align-items-center"
          >
            <span>{itemsPerPage}</span>
          </Dropdown.Toggle>

          <Dropdown.Menu>
            {[10, 25, 50, 100].map((item, i) => (
              <Dropdown.Item
                key={i}
                onClick={() => {
                  setItemPerPage(item);
                  setCurrentPage(1);
                }}
                active={itemsPerPage === item}
                className="d-flex align-items-center"
              >
                {item}
                {itemsPerPage === item && <i className="fa fa-check ms-2" />}
              </Dropdown.Item>
            ))}
          </Dropdown.Menu>
        </Dropdown>
      </div>
    );
  };

  const userData = JSON.parse(localStorage.getItem("userFormData")) || {};

  return (
    <div className={style.action_table_wrapper}>
      {/* Search and Filter Controls */}
      <div className={style.table_controls}>
        {!props.hideSearchBar && (
          <div className="col-md-4 mb-3">
            <div className={style.search_input_group}>
              <i className="fa fa-search" />
              <input
                type="search"
                value={searchQuery}
                onChange={handleOnSearch}
                placeholder="Search..."
                className={style.search_input}
              />
            </div>
          </div>
        )}

        <div className="col-md-auto d-flex flex-wrap justify-content-end align-items-center mb-3">
          <div className="d-flex">
            <StatusFilterDropdown />
            <DateSortDropdown />
          </div>

          {props.showCustomBtn && (
            <button
              onClick={props.handleBtnClick}
              type="button"
              disabled={props.isCustomBtnDisabled}
              className={style.custom_button}
            >
              <i className="fa fa-plus me-2" />
              <span>{props.btnName}</span>
            </button>
          )}
        </div>
      </div>

      {/* Table */}
      <div className={style.table_container}>
        <table className={style.table}>
          <thead>
            <tr className={style.table_header}>
              <th className={cn(style.th, style.checkbox_column)}>
                <input
                  type="checkbox"
                  checked={selected.length > 0 && selected.length === currentItems.length}
                  onChange={handleSelectAll}
                  ref={input => {
                    if (input) {
                      input.indeterminate = selected.length > 0 && selected.length < currentItems.length;
                    }
                  }}
                />
              </th>

              {props.columns?.map((data, colIndx) => {
                return !hideColumns.includes(data.fieldName) ? (
                  <th
                    key={colIndx}
                    className={cn(style.th, data.clsName, style.sortable_header)}
                    style={{ minWidth: data.minWidth || "150px", textAlign: data.align || 'left' }}
                    onClick={() => sortByColumn(data.fieldName)}
                  >
                    <div className="d-flex align-items-center">
                      <span>{data.titleCode || data.colName}</span>
                      {showSortedColumnIcon(data.fieldName)}
                    </div>
                  </th>
                ) : null;
              })}
            </tr>
          </thead>
          <tbody>
            {currentItems.length > 0 ? (
              currentItems.map((row, index) => {
                const isItemSelected = isSelected(row.id);
                return (
                  <tr
                    key={index}
                    className={cn(style.table_datarow, isItemSelected ? style.selected_row : '')}
                  >
                    <td className={cn(style.td, style.checkbox_column)}>
                      <input
                        type="checkbox"
                        checked={isItemSelected}
                        onChange={(e) => handleRowSelect(e, row)}
                      />
                    </td>

                    {props.columns.map((o, colIndex) =>
                      !hideColumns.includes(o.fieldName) && o.fieldName !== "action" ? (
                        <td
                          key={colIndex}
                          className={cn(style.td, o.clsName)}
                          style={{ textAlign: o.align || 'left' }}
                        >
                          {o.colName === "Status" ? (
                            <span className={cn(style.status_badge, style[_.get(row, o.fieldName)?.toLowerCase() + '_status'] || '')}>
                              {_.get(row, o.fieldName) || "No Status"}
                            </span>
                          ) : (
                            <span className={row.isRead === false ? style.unread : ''}>
                              {_.get(row, o.fieldName)}
                              {o.fieldName2 ? _.get(row, o.fieldName2) : ""}
                            </span>
                          )}
                        </td>
                      ) : !hideColumns.includes(o.fieldName) && o.fieldName === "action" ? (
                        <td key={colIndex} className={cn(style.td, o.clsName, style.action_cell)}>
                          <div className="d-flex justify-content-center" style={{ gap: "8px" }}>
                            {o.actions.includes("edit") && userData?.userType !== "CRP" && (
                              <Tooltip title="Edit" placement="top" arrow>
                                <button
                                  className={cn(style.action_button, style.edit_button)}
                                  onClick={() => handleEdit(row)}
                                  type="button"
                                  disabled={row.applicationStatus !== "Sent back" && row.applicationStatus !== "Draft"}
                                >
                                  <img src={editIcon} alt="edit" />
                                </button>
                              </Tooltip>
                            )}

                            {o.actions.includes("view") && (
                              <Tooltip title="View" placement="top" arrow>
                                <button
                                  className={cn(style.action_button, style.view_button)}
                                  onClick={() => handleView(row)}
                                  type="button"

                                >
                                  <img src={viewIcon} alt="view" />
                                </button>
                              </Tooltip>
                            )}

                            {o.actions.includes("track") && userData?.userType !== "CRP" && (
                              <Tooltip title="Track" placement="top" arrow>
                                <button
                                  className={cn(style.action_button, style.track_button)}
                                  onClick={() => handleTrack(row)}
                                  type="button"
                                >
                                  <BsGeoAltFill />
                                </button>
                              </Tooltip>
                            )}
                            {o.actions.includes("delete") && (
                              <Tooltip title="Delete this item" placement="top" arrow>
                                <button
                                  className={cn(style.action_button, style.delete_button)}
                                  onClick={() => handleDeleteRow(row)}
                                  type="button"
                                >
                                  <img src={deleteIcon} alt="delete" />
                                </button>
                              </Tooltip>
                            )}
                          </div>
                        </td>
                      ) : null
                    )}
                  </tr>
                );
              })
            ) : (
              <tr>
                <td colSpan={props.columns.length + 1} className={style.td}>
                  <div className={style.no_records}>
                    <i className="fa fa-database me-2" />
                    No records found matching your criteria
                  </div>
                </td>
              </tr>
            )}
          </tbody>
        </table>
      </div>

      {/* Table Footer */}
      <div className={style.table_footer}>
        <div className={style.selection_info}>
          Showing {indexOfFirstItem + 1}-{Math.min(indexOfLastItem, searchedData.length)} of {searchedData.length} entries
          {selected.length > 0 && (
            <span className="ms-2">
              (<span className="fw-bold">{selected.length}</span> selected)
            </span>
          )}
        </div>

        <div className="d-flex align-items-center">
          <PaginationOptions />

          <div className="btn-group ms-3">
            <button
              onClick={handlePrevPage}
              disabled={currentPage === 1}
              className={cn("btn btn-outline-secondary", style.pagination_button)}
            >
              <i className="fa fa-chevron-left" />
            </button>

            <div className={cn("px-3 d-flex align-items-center", style.page_number)}>
              Page {currentPage} of {totalPages || 1}
            </div>

            <button
              onClick={handleNextPage}
              disabled={currentPage === totalPages || totalPages === 0}
              className={cn("btn btn-outline-secondary", style.pagination_button)}
            >
              <i className="fa fa-chevron-right" />
            </button>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ActionTable;
