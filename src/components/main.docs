const renderDayContents = (day, date) => {
  const dayEvents = events.filter(event => 
    isSameDay(parseISO(event.start), date)
  );
  const isCurrentDay = isToday(date);
  
  return (
    <div className={`day-container ${isCurrentDay ? 'today' : ''}`}>
      <div className="day-number">{format(date, 'd')}</div>
      {dayEvents.length > 0 && (
        <div className="event-indicators">
          {dayEvents.map((event, i) => (
            <div 
              key={i}
              className="event-indicator"
              style={{ 
                backgroundColor: event.color,
                width: '8px',
                height: '8px'
              }}
              data-tip={`${event.title}<br/>Vessel: ${event.vessel}`}
              data-for="calendar-tooltip"
            />
          ))}
        </div>
      )}
    </div>
  );
};

// Add this to your return statement (requires react-tooltip)
import ReactTooltip from 'react-tooltip';

return (
  <div className="status-card survey-tracker-card" style={{ borderLeft: `4px solid ${color}` }}>
    {/* ... other components ... */}
    <ReactTooltip 
      id="calendar-tooltip" 
      html={true}
      effect="solid"
      className="calendar-tooltip"
    />
  </div>
);
