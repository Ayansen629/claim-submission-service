import React from 'react'
import EmployeeDashboard from './employeeDashboard'
import { OFFICER_ROLES, USER_ROLES } from '../../../utils/constants.ts'
import OfficerRoleDashboard from './officerDashboard/officerRoleDashboard'

const Employee = ({userInfo}) => {
  const { userDetails, userRole } = userInfo;
  console.log(userInfo,"userINfo admin")
  return (
    <>
      {(userRole === USER_ROLES?.ADMIN || userRole === USER_ROLES?.OFFICER_L1 || userRole === USER_ROLES?.OFFICER_L2 ) && (
        <EmployeeDashboard userDetails={userDetails} userRole={userRole} />

      )
      }
      {/* {OFFICER_ROLES.includes(userRole) && <OfficerRoleDashboard userDetails={userDetails} />} */}
    </>
  )
}

export default Employee;
