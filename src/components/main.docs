import React, { useState, useEffect, useRef } from "react";
import { useForm } from "react-hook-form";
import { Row, Col } from "react-bootstrap";
import InputText from "../../../common/InputText.tsx";
import InputSelect from "../../../common/InputSelect.tsx";
import InputRadio from "../../../common/InputRadio.tsx";
import InputFile from "../../../common/InputFile";
import Collapsible from "../../../common/Collapsible.js";
import { useTranslation } from "react-i18next";
import { useLanguageContext } from "../../../common/LanguageContext.js";

const VesselDetails = ({ formMethods, viewMode = false }) => {
  const { t } = useTranslation();
  const { changeLanguage } = useLanguageContext();
  const [formData, setFormData] = useState({});
  const isCompMount = useRef(true);

  const {
    register,
    watch,
    setValue,
    clearErrors,
    trigger,
    getValues,
    setError,
    formState: { errors },
  } = formMethods;

  const constructedBy = watch("constructedBy");
  const category = watch("category");

  useEffect(() => {
    if (isCompMount.current) {
      isCompMount.current = false;
      return;
    }
    const fieldsWithErrors = Object.keys(errors);
    trigger(fieldsWithErrors);
  }, [changeLanguage]);

  useEffect(() => {
    setTimeout(() => {
      Object.keys(formData).forEach(key => {
        if (formData[key] !== undefined) {
          setValue(key, formData[key], { shouldValidate: false });
        }
      });
    }, 0);
  }, [setValue, formData]);

  useEffect(() => {
    if (category === "A") {
      setValue("zone", "1", { shouldValidate: true });
      setValue("supervisionAuthority", "classificationSociety", { shouldValidate: true });
      clearErrors("zone");
      clearErrors("supervisionAuthority");
    }
  }, [category, setValue, clearErrors]);

  const formFields = {
    basicInfo: [
      // ... (keep all your basicInfo fields the same)
    ],
    builderDetails: [
      {
        name: 'builderName',
        label: t('Builder Name'),
        type: 'text',
        required: constructedBy === "2",
        colSize: 4,
        placeholder: t('Please enter Builder Name'),
        validation: constructedBy === "2" ? { required: t('Builder Name is required') } : {}
      },
      {
        name: 'builderAddress',
        label: t('Builder Address'),
        type: 'text',
        required: constructedBy === "2",
        colSize: 4,
        placeholder: t('Please enter Builder Address'),
        validation: constructedBy === "2" ? { required: t('Builder Address is required') } : {}
      },
      {
        name: 'builderEmail',
        label: t('Builder Email Id'),
        type: 'email',
        required: constructedBy === "2",
        colSize: 4,
        placeholder: t('Please enter Builder Email Id'),
        validation: constructedBy === "2" ? {
          required: t('Builder Email is required'),
          pattern: {
            value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
            message: t('Invalid email address')
          }
        } : {}
      },
      {
        name: 'builderMobile',
        label: t('Mobile'),
        type: 'text',
        required: constructedBy === "2",
        colSize: 4,
        placeholder: t('Please enter Mobile'),
        validation: constructedBy === "2" ? {
          required: t('Builder Mobile is required'),
          pattern: {
            value: /^[0-9]{10}$/,
            message: t('Invalid mobile number')
          }
        } : {}
      },
      {
        name: 'builderTradeLicence',
        label: t('Trade Licence'),
        type: 'text',
        required: constructedBy === "2",
        colSize: 4,
        placeholder: t('Please enter Trade Licence'),
        validation: constructedBy === "2" ? { required: t('Builder Trade Licence is required') } : {}
      },
      {
        name: 'builderGST',
        label: t('GST'),
        type: 'text',
        required: constructedBy === "2",
        colSize: 4,
        placeholder: t('Please enter GST'),
        validation: constructedBy === "2" ? { required: t('Builder GST is required') } : {}
      },
      {
        name: 'builderAuth',
        label: t('Builder Authorization'),
        type: 'select',
        required: constructedBy === "2",
        colSize: 4,
        options: [
          { value: 'auth', label: t('Authorized') },
          { value: 'rejected', label: t('Rejected') }
        ],
        validation: constructedBy === "2" ? { required: t('Builder Authorization is required') } : {}
      },
      {
        name: 'authLetter',
        label: t('Authorisation Letter'),
        type: 'file',
        required: constructedBy === "2",
        colSize: 4,
        validation: constructedBy === "2" ? { required: t('Authorization Letter is required') } : {}
      }
    ]
  };

  const DynamicField = ({ field }) => {
    if (field.showIf && !field.showIf()) return null;

    const isRequired = field.required && (!field.showIf || field.showIf());
    const fieldValidation = isRequired ? field.validation : {};

    const handleChange = (e) => {
      if (field?.onChange) field.onChange(e, setValue, watch);
    };

    switch (field.type) {
      case 'select':
        return (
          <InputSelect
            label={field.label}
            required={isRequired}
            name={field.name}
            options={field.options}
            register={register(field.name, fieldValidation)}
            errors={errors}
            value={formData[field.name] || watch(field.name) || ""}
            onChange={(value) => {
              if (viewMode || field.disabled) return;
              setValue(field.name, value, { shouldValidate: true });
              clearErrors(field.name);
              setFormData(prev => ({
                ...prev,
                [field.name]: value
              }));
            }}
            disabled={viewMode || field.disabled}
          />
        );

      case 'file':
        return (
          <InputFile
            label={field.label}
            required={isRequired}
            name={field.name}
            register={register(field.name, fieldValidation)}
            errors={errors}
            onChange={(e) => {
              if (viewMode) return;
              if (e.target.files && e.target.files[0]) {
                const file = e.target.files[0];
                const extension = file?.name?.substring(file?.name?.lastIndexOf('.'));
                const customFileName = `${field.label}${extension}`;
                file.originalName = customFileName;
                setValue(field.name, file, { shouldValidate: true });

                setFormData(prev => ({
                  ...prev,
                  [field.name]: file
                }));

                if (errors[field.name]) {
                  clearErrors(field.name);
                }

                const validTypes = ['application/pdf', 'image/jpeg', 'image/png', 'image/jpg'];
                if (!validTypes.includes(file.type)) {
                  setError(field.name, {
                    type: 'manual',
                    message: t('Only PDF, JPG or PNG files are allowed')
                  });
                } else if (file.size > 2 * 1024 * 1024) {
                  setError(field.name, {
                    type: 'manual',
                    message: t('File size should not exceed 2MB')
                  });
                }
              }
            }}
            watch={watch}
            accept=".pdf,.jpg,.jpeg,.png"
            defaultValue={formData[field.name]}
            clearErrors={clearErrors}
            disabled={viewMode}
          />
        );

      case 'radio':
        return (
          <InputRadio
            name={field.name}
            label={field.label}
            options={field.options}
            register={register}
            rules={fieldValidation}
            errors={errors}
            onChange={() => !viewMode && clearErrors(field.name)}
            disabled={viewMode}
          />
        );

      default:
        return (
          <InputText
            label={field.label}
            required={isRequired}
            name={field.name}
            type={field.type}
            placeholder={field.placeholder}
            register={register(field.name, fieldValidation)}
            onChange={handleChange}
            errors={errors}
            disabled={viewMode}
          />
        );
    }
  };

  return (
    <form autoComplete="off">
      <Collapsible title={t("Basic Information")} openStat={true}>
        <Row className="gy-3" style={{ padding: "1rem" }}>
          {formFields.basicInfo.map((field, index) => (
            <Col key={index} md={field.colSize || 12}>
              <DynamicField field={field} />
            </Col>
          ))}
        </Row>
      </Collapsible>

      {constructedBy === "2" && (
        <Collapsible title={t("Builder Details")} openStat={true}>
          <Row className="gy-3" style={{ padding: "1rem" }}>
            {formFields.builderDetails.map((field, index) => (
              <Col key={index} md={field.colSize || 12}>
                <DynamicField field={field} />
              </Col>
            ))}
          </Row>
        </Collapsible>
      )}
    </form>
  );
};

export default VesselDetails;
