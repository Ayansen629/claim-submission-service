import React, { useRef, useState, useEffect } from "react";
import { useNavigate, useParams, useLocation } from "react-router-dom";
import { useForm, useFieldArray, Controller } from "react-hook-form";
import { Container, Row, Col, Card, Button } from "react-bootstrap";
import { toast } from "react-toastify";
import {
  FaUser,
  FaMapMarkerAlt,
  FaBriefcase,
  FaFileUpload,
  FaGraduationCap,
  FaPlus,
  FaTrash,
  FaArrowLeft,
  FaDownload,
} from "react-icons/fa";
import InputText from "../../../common/InputText.tsx";
import { handleErrorMessage } from "../../../../utils/errorMessageHandler.js";
import InputSelect from "../../../common/InputSelect.tsx";
import Collapsible from "../../../common/Collapsible.js";
import { CONSTANT_REGEX, renameFile } from "../../../../utils/constants.ts";
import { useMasterData } from "../../../context/MasterDataContext.js";
import NewLoader from "../../../common/NewLoader.js";
import "./EmployeeRegistration.css";
import { getData, postFORMdata } from "../../../../services/http.service.ts";
import { URLS_ENDPOINT } from "../../../../utils/urlsConstants.ts";
import AppToggleButton from "../../../common/AppToggleButton.js";

// Helper for case-insensitive property access and uppercase/NA
const getValue = (obj, key) => {
  if (!obj) return "NOT AVAILABLE";
  let value = obj[key];
  if (value === undefined) {
    const lowerKey = key.toLowerCase();
    const foundKey = Object.keys(obj).find(
      k => k.toLowerCase() === lowerKey
    );
    value = foundKey ? obj[foundKey] : undefined;
  }
  if (value === undefined || value === null || value === "") return "NOT AVAILABLE";
  return typeof value === "string" ? value.toUpperCase() : value;
};

const EmployeeRegistration = () => {
  const navigate = useNavigate();
  const { id } = useParams();
  const location = useLocation();
  const isEditMode = !!id;
  const loaderRef = useRef();
  const { masterData } = useMasterData();

  // Get employee data from location.state if present
  const employeeData = location.state?.employee || null;

  const {
    register,
    handleSubmit,
    control,
    formState: { errors },
    setValue,
    watch,
    reset,
  } = useForm({
    mode: "onSubmit",
    defaultValues: {
      firstName: "",
      lastName: "",
      email: "",
      phoneNumber: "",
      gender: "",
      dateOfBirth: "",
      addressLine1: "",
      addressLine2: "",
      pin: "",
      district: "",
      stateUt: "",
      country: "INDIA",
      appointmentDate: "",
      employeeType: "",
      designation: "",
      addressProof: "",
      identityProof: "",
      appointmentLetter: "",
      educationDetails: [{ qualification: "", stream: "", yearOfPassing: "" }],
      professionalDetails: [
        {
          appointmentDate: "",
          employeeType: "",
          employeeStatus: "",
          designation: "",
          department: "",
          section: "",
          roles: "",
        },
      ],
      isActive: true,
    },
  });
  const [addressProofFile, setAddressProofFile] = useState(null);
  const [identityProofFile, setIdentityProofFile] = useState(null);
  const [appointmentLetterFile, setAppointmentLetterFile] = useState(null);
  const [toggleValue, setToggleValue] = useState(true);

  const { fields, append, remove } = useFieldArray({
    control,
    name: "educationDetails",
  });

  const {
    fields: professionalFields,
    append: appendProfessional,
    remove: removeProfessional,
  } = useFieldArray({
    control,
    name: "professionalDetails",
  });

  useEffect(() => {
    if (isEditMode && employeeData) {
      reset({
        firstName: getValue(employeeData, "firstName"),
        lastName: getValue(employeeData, "lastName"),
        email: getValue(employeeData, "emailId"),
        phoneNumber: getValue(employeeData, "phoneNumber"),
        gender: getValue(employeeData, "gender"),
        dateOfBirth: getValue(employeeData, "dateOfBirth"),
        addressLine1: getValue(employeeData, "addressLine1"),
        addressLine2: getValue(employeeData, "addressLine2"),
        pin: getValue(employeeData, "pin"),
        district: getValue(employeeData, "district"),
        stateUt: getValue(employeeData, "stateUt"),
        country: getValue(employeeData, "country") || "INDIA",
        isActive: getValue(employeeData, "isActive") === "NOT AVAILABLE" ? true : getValue(employeeData, "isActive"),
        educationDetails: Array.isArray(employeeData.educationDetails)
          ? employeeData.educationDetails.map(ed => ({
              qualification: getValue(ed, "qualification"),
              stream: getValue(ed, "stream"),
              yearOfPassing: getValue(ed, "yearOfPassing"),
              additionalEducationDetails: getValue(ed, "additionalEducationDetails")
            }))
          : [{ qualification: "NOT AVAILABLE", stream: "NOT AVAILABLE", yearOfPassing: "NOT AVAILABLE" }],
        professionalDetails: Array.isArray(employeeData.professionalDetails)
          ? employeeData.professionalDetails.map(pd => ({
              appointmentDate: getValue(pd, "appointmentDate"),
              employeeType: getValue(pd, "employeeType"),
              employeeStatus: getValue(pd, "employeeStatus"),
              designation: getValue(pd, "designation"),
              department: getValue(pd, "department"),
              section: getValue(pd, "section"),
              roles: getValue(pd, "role")
            }))
          : [{
              appointmentDate: "NOT AVAILABLE",
              employeeType: "NOT AVAILABLE",
              employeeStatus: "NOT AVAILABLE",
              designation: "NOT AVAILABLE",
              department: "NOT AVAILABLE",
              section: "NOT AVAILABLE",
              roles: "NOT AVAILABLE"
            }]
      });
      setToggleValue(getValue(employeeData, "isActive") === "NOT AVAILABLE" ? true : getValue(employeeData, "isActive"));
    } else if (!isEditMode) {
      setToggleValue(true);
    }
  }, [isEditMode, reset, employeeData]);

  const fetchAddressDetails = async (pin) => {
    try {
      const URL =
        process.env.REACT_APP_PUBLIC_URL +
        URLS_ENDPOINT.APP_SERVICES.GET_DETAILS_BY_PINCODE +
        pin;
      const response = await getData(URL);
      if (response?.status === 200 && response?.data?.length > 0) {
        setValue("district", response?.data[0]?.district?.toUpperCase() || "NOT AVAILABLE");
        setValue("stateUt", response?.data[0]?.stateName?.toUpperCase() || "NOT AVAILABLE");
        setValue("country", "INDIA");
      } else {
        setValue("district", "NOT AVAILABLE");
        setValue("stateUt", "NOT AVAILABLE");
        setValue("country", "NOT AVAILABLE");
        toast.info("No data found for this PIN code");
      }
      loaderRef.current?.hide();
    } catch (error) {
      setValue("district", "NOT AVAILABLE");
      setValue("stateUt", "NOT AVAILABLE");
      setValue("country", "NOT AVAILABLE");
      loaderRef.current?.hide();
      toast.error("Error fetching address details");
    }
  };

  const onSubmit = async (data) => {
    try {
      loaderRef.current?.show();

      const vesselUserDetails = {
        vesselUserDetails: {
          id: isEditMode ? id : "",
          userType: "employee",
          userCategory: "",
          firstName: data.firstName,
          lastName: data.lastName,
          emailId: data.email,
          phoneNumber: data.phoneNumber,
          panNumber: data.panNumber || "",
          gstNumber: data.gstNumber || "",
          addressDetails: data.addressLine1 + (data.addressLine2 ? ", " + data.addressLine2 : ""),
          addressLine1: data.addressLine1,
          addressLine2: data.addressLine2,
          pin: data.pin,
          district: data.district,
          stateUt: data.stateUt,
          country: data.country,
          isActive: data?.isActive,
          keycloakId: "",
          dateOfBirth: data.dateOfBirth,
          gender: data.gender,
          authorizedPerson: null,
          companyRegistration: null,
          educationDetails: data?.educationDetails?.map((ed) => ({
            uuid: isEditMode ? ed.uuid || "" : "",
            employeeUUID: isEditMode ? id : "",
            qualification: ed.qualification,
            stream: ed.stream,
            yearOfPassing: ed.yearOfPassing,
            additionalEducationDetails: ed.additionalEducationDetails || "",
          })),
          professionalDetails: data?.professionalDetails?.map((pd) => ({
            uuid: isEditMode ? pd.uuid || "" : "",
            employeeUUID: isEditMode ? id : "",
            appointmentDate: pd.appointmentDate,
            employeeType: pd.employeeType,
            employeeStatus: pd.employeeStatus,
            designation: pd.designation,
            department: pd.department,
            section: pd.section,
            role: pd.roles,
          })),
          aadharNumber: data.aadharNumber || "",
        }
      };

      const formData = new FormData();
      formData.append("request", new Blob([JSON.stringify(vesselUserDetails)], { type: 'application/json' }));

      if (addressProofFile) {
        formData.append("files", addressProofFile);
      }
      if (identityProofFile) {
        formData.append("files", identityProofFile);
      }
      if (appointmentLetterFile) {
        formData.append("files", appointmentLetterFile);
      }
      
      await submitEmpDetails(formData);

    } catch (error) {
      loaderRef.current?.hide();
      toast.error(
        error?.response?.data?.message ||
        "Error submitting form. Please try again."
      );
    }
  };

  const submitEmpDetails = async (payload) => {
    try {
      const URL = isEditMode 
        ? process.env.REACT_APP_AUTH_URL + URLS_ENDPOINT.USER_SERVICE.UPDATE_EMP + id
        : process.env.REACT_APP_AUTH_URL + URLS_ENDPOINT.USER_SERVICE.CREATE_EMP;
      
      loaderRef.current?.show();
      const response = await postFORMdata(URL, payload);
      
      const apiRes = response?.data ? { ...response?.data } : null;
      if (apiRes?.responseStatus?.status === "200") {
        loaderRef?.current?.hide();
        toast.success(
          apiRes?.responseStatus?.message || 
          (isEditMode ? "Employee updated successfully!" : "Employee registered successfully!"),
          { position: "top-right" }
        );
        handleCancel();
      } else {
        loaderRef?.current?.hide();
        toast.info(
          apiRes?.responseStatus?.message || "Something went wrong..",
          { position: "top-right" }
        );
      }
    } catch (err) {
      console.log(err);
      loaderRef?.current?.hide();
      handleErrorMessage(err?.response?.data);
    }
  };

  const handleCancel = () => {
    navigate("/iwt/employee-management");
  };

  // Helper to download file
  const handleFileDownload = (url) => {
    if (!url) return;
    const link = document.createElement("a");
    link.href = url;
    link.setAttribute("download", "");
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  };

  return (
    <>
      <NewLoader ref={loaderRef} />
      <Container fluid className="p-4">
        <Card className="mb-4">
          <Card.Header
            className="bg-primary text-white d-flex align-items-center justify-content-center position-relative"
            style={{
              minHeight: 56,
              background: "linear-gradient(135deg, rgb(0, 119, 182), rgb(2, 62, 138))",
              border: "none",
            }}
          >
            <Button
              variant="link"
              className="p-0 me-2"
              style={{
                color: "white",
                textDecoration: "none",
                position: "absolute",
                left: 0,
                top: "50%",
                transform: "translateY(-50%)",
              }}
              onClick={() => navigate("/iwt/employee-management")}
            >
              <FaArrowLeft size={22} />
            </Button>

            <div style={{ fontWeight: 600, fontSize: 22, color: "#fff" }}>
              {isEditMode ? "EDIT EMPLOYEE" : "ADD EMPLOYEE"}
            </div>

            <div style={{ width: 40 }}></div>
          </Card.Header>
          <Card.Body>
            <form onSubmit={handleSubmit(onSubmit)} className="employee-form">
              {/* Personal Details Section */}
              <Collapsible
                title={
                  <div className="d-flex align-items-center">
                    <FaUser className="me-2" style={{ color: "#2466BF" }} />
                    <span style={{ color: "#2466BF" }}>PERSONAL DETAILS</span>
                  </div>
                }
                defaultOpen={true}
              >
                <Card className="form-section-card">
                  <Card.Body>
                    <Row className="g-3">
                      <Col md={4}>
                        <InputText
                          name="firstName"
                          label="FIRST NAME"
                          placeholder="ENTER FIRST NAME"
                          required={true}
                          register={{
                            ...register("firstName", {
                              required: "First Name is required",
                            }),
                          }}
                          errors={errors}
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "firstName")}
                          </div>
                        )}
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="lastName"
                          label="LAST NAME"
                          placeholder="ENTER LAST NAME"
                          required={true}
                          register={{
                            ...register("lastName", {
                              required: "Last Name is required",
                            }),
                          }}
                          errors={errors}
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "lastName")}
                          </div>
                        )}
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="email"
                          label="EMAIL ID"
                          type="email"
                          placeholder="ENTER EMAIL ADDRESS"
                          required={true}
                          register={{
                            ...register("email", {
                              required: "Email is required",
                              pattern: {
                                value: CONSTANT_REGEX.EMAIL,
                                message: "Invalid email address",
                              },
                            }),
                          }}
                          errors={errors}
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "emailId")}
                          </div>
                        )}
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="phoneNumber"
                          label="PHONE NUMBER"
                          placeholder="ENTER 10-DIGIT PHONE NUMBER"
                          required={true}
                          maxlength={10}
                          register={{
                            ...register("phoneNumber", {
                              required: "Phone Number is required",
                              pattern: {
                                value: CONSTANT_REGEX.MOBILE,
                                message: "Phone number must be 10 digits",
                              },
                            }),
                          }}
                          errors={errors}
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "phoneNumber")}
                          </div>
                        )}
                      </Col>
                      <Col md={4}>
                        <InputSelect
                          name="gender"
                          label="GENDER"
                          required={true}
                          options={[
                            { value: "", label: "SELECT GENDER" },
                            ...(masterData?.genders?.map((type) => ({
                              value: type.valueCode,
                              label: type.valueLabel,
                            })) || [
                                { value: "MALE", label: "MALE" },
                                { value: "FEMALE", label: "FEMALE" },
                                { value: "OTHER", label: "OTHER" },
                              ]),
                          ]}
                          register={register("gender", {
                            required: "Gender is required",
                          })}
                          errors={errors}
                          value={watch("gender") || ""}
                          onChange={(value) =>
                            setValue("gender", value, { shouldValidate: true })
                          }
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "gender")}
                          </div>
                        )}
                      </Col>
                      <Col md={4}>
                        <InputText
                          name="dateOfBirth"
                          label="DATE OF BIRTH"
                          type="date"
                          placeholder="SELECT DATE OF BIRTH"
                          required={true}
                          register={{
                            ...register("dateOfBirth", {
                              required: "Date of Birth is required",
                            }),
                          }}
                          errors={errors}
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "dateOfBirth")}
                          </div>
                        )}
                      </Col>
                    </Row>
                  </Card.Body>
                </Card>
              </Collapsible>

              {/* Address Details Section */}
              <Collapsible
                title={
                  <div className="d-flex align-items-center">
                    <FaMapMarkerAlt className="me-2" style={{ color: "#2466BF" }} />
                    <span style={{ color: "#2466BF" }}>ADDRESS DETAILS</span>
                  </div>
                }
                defaultOpen={true}
              >
                <Card className="form-section-card">
                  <Card.Body>
                    <Row className="g-3">
                      <Col md={6}>
                        <InputText
                          name="addressLine1"
                          label="ADDRESS LINE-1"
                          placeholder="ENTER ADDRESS LINE 1"
                          required={true}
                          register={{
                            ...register("addressLine1", {
                              required: "Address Line-1 is required",
                            }),
                          }}
                          errors={errors}
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "addressLine1")}
                          </div>
                        )}
                      </Col>
                      <Col md={6}>
                        <InputText
                          name="addressLine2"
                          label="ADDRESS LINE-2"
                          placeholder="ENTER ADDRESS LINE 2 (OPTIONAL)"
                          register={{
                            ...register("addressLine2"),
                          }}
                          errors={errors}
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "addressLine2")}
                          </div>
                        )}
                      </Col>
                      <Col md={3}>
                        <InputText
                          name="pin"
                          label="PIN"
                          placeholder="ENTER 6-DIGIT PIN CODE"
                          required={true}
                          register={{
                            ...register("pin", {
                              required: "PIN Code is required",
                              pattern: {
                                value: /^[0-9]{6}$/,
                                message: "PIN must be 6 digits",
                              },
                            }),
                          }}
                          onChange={(e) => {
                            const pinValue = e.target.value;
                            if (pinValue.length === 6) {
                              loaderRef.current?.show();
                              fetchAddressDetails(pinValue);
                            }
                          }}
                          errors={errors}
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "pin")}
                          </div>
                        )}
                      </Col>
                      <Col md={3}>
                        <InputText
                          name="district"
                          label="DISTRICT"
                          required={true}
                          placeholder="AUTO-POPULATED"
                          register={{
                            ...register("district"),
                          }}
                          errors={errors}
                          readOnly
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "district")}
                          </div>
                        )}
                      </Col>
                      <Col md={3}>
                        <InputText
                          name="stateUt"
                          label="STATE/UT"
                          required={true}
                          placeholder="AUTO-POPULATED"
                          register={{
                            ...register("stateUt"),
                          }}
                          errors={errors}
                          readOnly
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "stateUt")}
                          </div>
                        )}
                      </Col>
                      <Col md={3}>
                        <InputText
                          name="country"
                          label="COUNTRY"
                          required={true}
                          placeholder="AUTO-POPULATED"
                          register={{
                            ...register("country"),
                          }}
                          errors={errors}
                          readOnly
                        />
                        {isEditMode && (
                          <div className="text-muted small">
                            {getValue(employeeData, "country")}
                          </div>
                        )}
                      </Col>
                    </Row>
                  </Card.Body>
                </Card>
              </Collapsible>

              {/* Education Details Section */}
              <Collapsible
                title={
                  <div className="d-flex align-items-center">
                    <FaGraduationCap className="me-2" style={{ color: "#2466BF" }} />
                    <span style={{ color: "#2466BF" }}>EDUCATION DETAILS</span>
                  </div>
                }
                defaultOpen={true}
              >
                <Card className="form-section-card">
                  <Card.Body>
                    {fields.map((field, index) => (
                      <Row key={field.id} className="g-3">
                        <Col md={4}>
                          <InputText
                            required={true}
                            name={`educationDetails.${index}.qualification`}
                            label="QUALIFICATION"
                            placeholder="ENTER QUALIFICATION"
                            register={register(`educationDetails.${index}.qualification`, {
                              required: "Qualification is required",
                            })}
                            errors={errors?.educationDetails?.[index] || {}}
                          />
                          {isEditMode && (
                            <div className="text-muted small">
                              {getValue(employeeData?.educationDetails?.[index], "qualification")}
                            </div>
                          )}
                        </Col>
                        <Col md={4}>
                          <InputText
                            name={`educationDetails.${index}.stream`}
                            label="STREAM"
                            placeholder="ENTER STREAM"
                            register={register(`educationDetails.${index}.stream`, {
                              required: "Stream is required",
                            })}
                            errors={errors?.educationDetails?.[index] || {}}
                          />
                          {isEditMode && (
                            <div className="text-muted small">
                              {getValue(employeeData?.educationDetails?.[index], "stream")}
                            </div>
                          )}
                        </Col>
                        <Col md={4}>
                          <InputText
                            name={`educationDetails.${index}.yearOfPassing`}
                            label="YEAR OF PASSING"
                            required={true}
                            placeholder="ENTER YEAR OF PASSING"
                            register={register(`educationDetails.${index}.yearOfPassing`, {
                              required: "Year of Passing is required",
                            })}
                            errors={errors?.educationDetails?.[index] || {}}
                          />
                          {isEditMode && (
                            <div className="text-muted small">
                              {getValue(employeeData?.educationDetails?.[index], "yearOfPassing")}
                            </div>
                          )}
                        </Col>
                        {index > 0 && (
                          <Col md={12} className="text-end">
                            <Button
                              variant="danger"
                              size="sm"
                              onClick={() => remove(index)}
                              className="mt-1"
                            >
                              <FaTrash /> Remove
                            </Button>
                          </Col>
                        )}
                      </Row>
                    ))}
                    <Button
                      variant="outline-primary"
                      onClick={() =>
                        append({
                          qualification: "",
                          stream: "",
                          yearOfPassing: "",
                        })
                      }
                      className="add-education-btn"
                    >
                      <FaPlus className="me-2" /> Add Education Details
                    </Button>
                  </Card.Body>
                </Card>
              </Collapsible>

              {/* Professional Details Section */}
              <Collapsible
                title={
                  <div className="d-flex align-items-center">
                    <FaBriefcase className="me-2" style={{ color: "#2466BF" }} />
                    <span style={{ color: "#2466BF" }}>
                      PROFESSIONAL DETAILS
                    </span>
                  </div>
                }
                defaultOpen={true}
              >
                <Card className="form-section-card">
                  <Card.Body>
                    {professionalFields.map((field, index) => (
                      <div key={field.id} className="professional-entry mb-4">
                        <Row className="g-3">
                          <Col md={4}>
                            <InputText
                              name={`professionalDetails.${index}.appointmentDate`}
                              label="APPOINTMENT DATE *"
                              type="date"
                              placeholder="SELECT APPOINTMENT DATE"
                              register={register(
                                `professionalDetails.${index}.appointmentDate`,
                                { required: "Appointment Date is required" }
                              )}
                              errors={errors?.professionalDetails?.[index] || {}}
                              style={{
                                borderColor:
                                  errors?.professionalDetails?.[index]?.appointmentDate && "#dc3545",
                                backgroundColor:
                                  errors?.professionalDetails?.[index]?.appointmentDate && "#fff8f8",
                              }}
                            />
                            {isEditMode && (
                              <div className="text-muted small">
                                {getValue(employeeData?.professionalDetails?.[index], "appointmentDate")}
                              </div>
                            )}
                          </Col>
                          <Col md={4}>
                            <InputSelect
                              name={`professionalDetails.${index}.employeeType`}
                              label="EMPLOYEE TYPE"
                              required={true}
                              placeholder="SELECT EMPLOYEE TYPE"
                              options={
                                masterData?.employeeTypes?.map((type) => ({
                                  value: type.valueCode,
                                  label: type.valueLabel,
                                })) || []
                              }
                              register={register(
                                `professionalDetails.${index}.employeeType`,
                                { required: "Employee Type is required" }
                              )}
                              errors={errors?.professionalDetails?.[index] || {}}
                              value={watch(`professionalDetails.${index}.employeeType`) || ""}
                              onChange={(val) =>
                                setValue(
                                  `professionalDetails.${index}.employeeType`,
                                  val,
                                  { shouldValidate: true }
                                )
                              }
                            />
                            {isEditMode && (
                              <div className="text-muted small">
                                {getValue(employeeData?.professionalDetails?.[index], "employeeType")}
                              </div>
                            )}
                          </Col>
                          <Col md={4}>
                            <InputSelect
                              name={`professionalDetails.${index}.employeeStatus`}
                              label="EMPLOYEE STATUS"
                              required={true}
                              placeholder="SELECT EMPLOYEE STATUS"
                              options={
                                masterData?.employeeStatuses?.map((type) => ({
                                  value: type.valueCode,
                                  label: type.valueLabel,
                                })) || []
                              }
                              register={register(
                                `professionalDetails.${index}.employeeStatus`,
                                { required: "Employee Status is required" }
                              )}
                              errors={errors?.professionalDetails?.[index] || {}}
                              value={
                                watch(
                                  `professionalDetails.${index}.employeeStatus`
                                ) || ""
                              }
                              onChange={(val) =>
                                setValue(
                                  `professionalDetails.${index}.employeeStatus`,
                                  val,
                                  { shouldValidate: true }
                                )
                              }
                            />
                            {isEditMode && (
                              <div className="text-muted small">
                                {getValue(employeeData?.professionalDetails?.[index], "employeeStatus")}
                              </div>
                            )}
                          </Col>
                          <Col md={4}>
                            <InputSelect
                              name={`professionalDetails.${index}.designation`}
                              label="DESIGNATION"
                              required={true}
                              placeholder="SELECT DESIGNATION"
                              options={
                                masterData?.designations?.map((type) => ({
                                  value: type.valueCode,
                                  label: type.valueLabel,
                                })) || []
                              }
                              register={register(
                                `professionalDetails.${index}.designation`,
                                { required: "Designation is required" }
                              )}
                              errors={errors?.professionalDetails?.[index] || {}}
                              value={
                                watch(
                                  `professionalDetails.${index}.designation`
                                ) || ""
                              }
                              onChange={(val) =>
                                setValue(
                                  `professionalDetails.${index}.designation`,
                                  val,
                                  { shouldValidate: true }
                                )
                              }
                            />
                            {isEditMode && (
                              <div className="text-muted small">
                                {getValue(employeeData?.professionalDetails?.[index], "designation")}
                              </div>
                            )}
                          </Col>
                          <Col md={4}>
                            <InputSelect
                              name={`professionalDetails.${index}.department`}
                              label="DEPARTMENT"
                              required={true}
                              placeholder="SELECT DEPARTMENT"
                              options={
                                masterData?.departments?.map((type) => ({
                                  value: type.valueCode,
                                  label: type.valueLabel,
                                })) || []
                              }
                              register={register(
                                `professionalDetails.${index}.department`,
                                { required: "Department is required" }
                              )}
                              errors={errors?.professionalDetails?.[index] || {}}
                              value={
                                watch(
                                  `professionalDetails.${index}.department`
                                ) || ""
                              }
                              onChange={(val) =>
                                setValue(
                                  `professionalDetails.${index}.department`,
                                  val,
                                  { shouldValidate: true }
                                )
                              }
                            />
                            {isEditMode && (
                              <div className="text-muted small">
                                {getValue(employeeData?.professionalDetails?.[index], "department")}
                              </div>
                            )}
                          </Col>
                          <Col md={4}>
                            <InputText
                              name={`professionalDetails.${index}.section`}
                              label="SECTION"
                              placeholder="ENTER SECTION"
                              register={register(
                                `professionalDetails.${index}.section`
                              )}
                              errors={
                                errors?.professionalDetails?.[index]?.section
                                  ? {
                                    professionalDetails: {
                                      [index]: {
                                        section:
                                          errors.professionalDetails[index]
                                            .section,
                                      },
                                    },
                                  }
                                  : {}
                              }
                            />
                            {isEditMode && (
                              <div className="text-muted small">
                                {getValue(employeeData?.professionalDetails?.[index], "section")}
                              </div>
                            )}
                          </Col>
                          <Col md={4}>
                            <InputSelect
                              name={`professionalDetails.${index}.roles`}
                              label="ROLES"
                              required={true}
                              placeholder="SELECT ROLE"
                              options={
                                masterData?.roles?.map((type) => ({
                                  value: type.valueCode,
                                  label: type.valueLabel,
                                })) || []
                              }
                              register={register(
                                `professionalDetails.${index}.roles`,
                                { required: "Role is required" }
                              )}
                              errors={errors?.professionalDetails?.[index] || {}}
                              value={
                                watch(
                                  `professionalDetails.${index}.roles`
                                ) || ""
                              }
                              onChange={(val) =>
                                setValue(
                                  `professionalDetails.${index}.roles`,
                                  val,
                                  { shouldValidate: true }
                                )
                              }
                            />
                            {isEditMode && (
                              <div className="text-muted small">
                                {getValue(employeeData?.professionalDetails?.[index], "role")}
                              </div>
                            )}
                          </Col>
                          <Col md={1} className="d-flex align-items-end">
                            {index > 0 && (
                              <Button
                                variant="danger"
                                size="sm"
                                onClick={() => removeProfessional(index)}
                                className="mt-1"
                              >
                                <FaTrash />
                              </Button>
                            )}
                          </Col>
                        </Row>
                      </div>
                    ))}
                    <Button
                      variant="outline-primary"
                      onClick={() =>
                        appendProfessional({
                          appointmentDate: "",
                          employeeType: "",
                          employeeStatus: "",
                          designation: "",
                          department: "",
                          section: "",
                          roles: "",
                        })
                      }
                      className="add-education-btn"
                    >
                      <FaPlus className="me-2" /> Add Professional Details
                    </Button>
                  </Card.Body>
                </Card>
              </Collapsible>

              {/* Documents Upload Section */}
              <Collapsible
                title={
                  <div className="d-flex align-items-center">
                    <FaFileUpload className="me-2" style={{ color: "#2466BF" }} />
                    <span style={{ color: "#2466BF" }}>
                      DOCUMENTS UPLOAD
                    </span>
                  </div>
                }
                defaultOpen={true}
              >
                <Card className="form-section-card">
                  <Card.Body>
                    <Row className="g-3">
                      <Col md={6}>
                        <InputText
                          name="addressProof"
                          label="ADDRESS PROOF"
                          type="file"
                          required={!isEditMode}
                          register={register("addressProof", {
                            required: !isEditMode && "Address Proof is required",
                          })}
                          errors={errors}
                          onChange={(e) => {
                            if (e.target.files && e?.target?.files[0]) {
                              const file = e.target.files[0];
                              const extension = file?.name?.substring(file?.name?.lastIndexOf('.'));
                              const fileName = `Address Proof${extension}`;
                              file.originalName = fileName;
                              setAddressProofFile(renameFile(file, fileName))
                            }
                          }}
                        />
                        <small className="text-muted">
                          Upload PDF, JPG or PNG (Max: 2MB)
                        </small>
                        {isEditMode && employeeData?.fileStoreMaps?.[0]?.downLoadFile ? (
                          <div className="mt-2" style={{ display: "flex", alignItems: "center", gap: 6 }}>
                            <Button
                              variant="link"
                              style={{ padding: 0, color: "#2466BF" }}
                              onClick={() => handleFileDownload(employeeData.fileStoreMaps[0].downLoadFile)}
                            >
                              <FaDownload style={{ marginRight: 4 }} />
                              DOWNLOAD ADDRESS PROOF
                            </Button>
                          </div>
                        ) : isEditMode && (
                          <div className="mt-2" style={{ color: "#888" }}>NOT AVAILABLE</div>
                        )}
                      </Col>
                      <Col md={6}>
                        <InputText
                          name="identityProof"
                          label="IDENTITY PROOF"
                          type="file"
                          required={!isEditMode}
                          register={register("identityProof", {
                            required: !isEditMode && "Identity Proof is required",
                          })}
                          errors={errors}
                          onChange={(e) => {
                            if (e.target.files && e?.target?.files[0]) {
                              const file = e.target.files[0];
                              const extension = file?.name?.substring(file?.name?.lastIndexOf('.'));
                              const fileName = `Identity Proof${extension}`;
                              file.originalName = fileName;
                              setIdentityProofFile(renameFile(file, fileName))
                            }
                          }}
                        />
                        <small className="text-muted">
                          Upload PDF, JPG or PNG (Max: 2MB)
                        </small>
                        {isEditMode && employeeData?.fileStoreMaps?.[1]?.downLoadFile ? (
                          <div className="mt-2" style={{ display: "flex", alignItems: "center", gap: 6 }}>
                            <Button
                              variant="link"
                              style={{ padding: 0, color: "#2466BF" }}
                              onClick={() => handleFileDownload(employeeData.fileStoreMaps[1].downLoadFile)}
                            >
                              <FaDownload style={{ marginRight: 4 }} />
                              DOWNLOAD IDENTITY PROOF
                            </Button>
                          </div>
                        ) : isEditMode && (
                          <div className="mt-2" style={{ color: "#888" }}>NOT AVAILABLE</div>
                        )}
                      </Col>
                      <Col md={6}>
                        <InputText
                          name="appointmentLetter"
                          label="APPOINTMENT LETTER"
                          type="file"
                          required={!isEditMode}
                          register={register("appointmentLetter", {
                            required: !isEditMode && "Appointment Letter is required",
                          })}
                          errors={errors}
                          onChange={(e) => {
                            if (e.target.files && e?.target?.files[0]) {
                              const file = e.target.files[0];
                              const extension = file?.name?.substring(file?.name?.lastIndexOf('.'));
                              const fileName = `Appointment Letter${extension}`;
                              file.originalName = fileName;
                              setAppointmentLetterFile(renameFile(file, fileName))
                            }
                          }}
                        />
                        <small className="text-muted">
                          Upload PDF, JPG or PNG (Max: 2MB)
                        </small>
                        {isEditMode && employeeData?.fileStoreMaps?.[2]?.downLoadFile ? (
                          <div className="mt-2" style={{ display: "flex", alignItems: "center", gap: 6 }}>
                            <Button
                              variant="link"
                              style={{ padding: 0, color: "#2466BF" }}
                              onClick={() => handleFileDownload(employeeData.fileStoreMaps[2].downLoadFile)}
                            >
                              <FaDownload style={{ marginRight: 4 }} />
                              DOWNLOAD APPOINTMENT LETTER
                            </Button>
                          </div>
                        ) : isEditMode && (
                          <div className="mt-2" style={{ color: "#888" }}>NOT AVAILABLE</div>
                        )}
                      </Col>
                    </Row>
                  </Card.Body>
                </Card>
              </Collapsible>

              {/* Form Actions */}
              <div className="d-flex justify-content-end mt-4 gap-2">
                <Col style={{ display: "flex" }}>
                  <strong style={{ marginTop: "5px" }}>USER STATUS : </strong>
                  &nbsp; &nbsp;
                  <Controller
                    name="isActive"
                    control={control}
                    defaultValue={true}
                    render={({ field }) => (
                      <AppToggleButton
                        labels={["IN-ACTIVE", "ACTIVE"]}
                        value={toggleValue}
                        onChange={(val) => {
                          field.onChange(val);
                          setToggleValue(val);
                          setValue("isActive", val);
                        }}
                      />
                    )}
                  />
                </Col>

                <Button variant="secondary" onClick={handleCancel}>
                  CANCEL
                </Button>
                <Button
                  variant="primary"
                  type="submit"
                  style={{
                    background: "linear-gradient(135deg, #0077b6, #023e8a)",
                    border: "none",
                    boxShadow: "0 4px 6px rgba(0, 0, 0, 0.1)",
                    padding: "8px 20px",
                  }}
                >
                  {isEditMode ? "UPDATE" : "SUBMIT"}
                </Button>
              </div>
            </form>
          </Card.Body>
        </Card>
      </Container>
    </>
  );
};

export default EmployeeRegistration;
