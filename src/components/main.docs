import React, { useState } from 'react';
import "./Register.css";
import { useForm } from "react-hook-form";
import { Link, useNavigate } from 'react-router-dom';
import { ToastContainer, toast } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';

const Register = () => {
  const navigate = useNavigate();
  const [otpSent, setOtpSent] = useState(false);
  const [otp, setOtp] = useState("");
  const [loading, setLoading] = useState(false);
  const [selectedRadio, setSelectedRadio] = useState("individual");

  const {
    register,
    handleSubmit,
    watch,
    reset,
    formState: { errors },
  } = useForm({
    defaultValues: {
      name: "",
      corporateName: "",
      email: "",
      mobilenumber: "",
      aadhar: "",
      pancard: "",
      cinTaNumber: "",
      userType: "individual", 
    },
  });

  const formValues = watch();

  const isFormComplete = () => {
    if (selectedRadio === "individual") {
      return (
        formValues.name &&
        formValues.email &&
        formValues.mobilenumber &&
        formValues.aadhar &&
        formValues.pancard
      );
    } else {
      return (
        formValues.corporateName &&
        formValues.email &&
        formValues.mobilenumber &&
        formValues.cinTaNumber &&
        formValues.pancard
      );
    }
  };

  const handleSendOTP = async () => {
    setLoading(true);

    // Simulate OTP sending
    setTimeout(() => {
      toast.success("OTP Sent", { position: 'top-right' });
      setOtpSent(true); // Show OTP input box
      setLoading(false);
    }, 1000);
  };

  const onSubmit = async (data) => {
    if (!otp) {
      toast.error("Please enter the OTP");
      return;
    }

    if (otp !== "123456") {
      toast.error("Invalid OTP. Please enter the correct OTP.");
      return;
    }

    // Prepare payload
    const payload = {
      name: selectedRadio === "individual" ? data.name : data.corporateName,
      email: data.email,
      mobilenumber: data.mobilenumber,
      pancard: data.pancard,
      userType: selectedRadio === "individual" ? "INDV" : "CORP",
      ...(selectedRadio === "individual" 
        ? { aadhar: data.aadhar }
        : { 
            cinTaNumber: data.cinTaNumber
          }
      )
    };

    console.log("Registration Payload:", payload);
    toast.success("Registration successful!", { position: 'top-right' });

    // Reset form and OTP state
    reset();
    setOtp("");
    setOtpSent(false);

    // Navigate to login after a delay
    setTimeout(() => {
      navigate("/login");
    }, 1000);
  };

  return (
    <div className='register-page'>
      <div className="register-container">
        <div className="register-header">
          <div className="register-textmain"> Claimants Register</div>
        </div>
        <form className="register-form" onSubmit={handleSubmit(onSubmit)}>
          <div className="radio-options">
            <div className={`radio-op ${selectedRadio === "individual" ? "active" : ""}`}>
              <input
                type="radio"
                id="individual"
                name="userType"
                value="individual"
                checked={selectedRadio === "individual"}
                onChange={() => setSelectedRadio("individual")}
              />
              <label htmlFor="individual">Individual</label>
            </div>
            <div className={`radio-op ${selectedRadio === "corporate" ? "active" : ""}`}>
              <input
                type="radio"
                id="corporate"
                name="userType"
                value="corporate"
                checked={selectedRadio === "corporate"}
                onChange={() => setSelectedRadio("corporate")}
              />
              <label htmlFor="corporate">Corporate</label>
            </div>
          </div>

          <div className="form-grid">
            <div className="form-column">
              {selectedRadio === "individual" ? (
                <>
                  <input
                    type="text"
                    {...register("name", { required: "Name is required" })}
                    placeholder="Name (as on Aadhar)"
                    className="user-input"
                  />
                  {errors.name && <span className="error">{errors.name.message}</span>}
                </>
              ) : (
                <>
                  <input
                    type="text"
                    {...register("corporateName", { required: "Corporate Name is required" })}
                    placeholder="Corporate Name"
                    className="user-input"
                  />
                  {errors.corporateName && <span className="error">{errors.corporateName.message}</span>}
                </>
              )}

              <input
                type="email"
                {...register("email", { 
                  required: "Email is required",
                  pattern: {
                    value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
                    message: "Invalid email address"
                  }
                })}
                placeholder="Email Address"
                className="user-input"
              />
              {errors.email && <span className="error">{errors.email.message}</span>}

              {selectedRadio === "individual" ? (
                <input
                  type="text"
                  {...register("aadhar", { 
                    required: "Aadhar number is required",
                    pattern: {
                      value: /^[2-9]{1}[0-9]{11}$/,
                      message: "Invalid Aadhar number"
                    }
                  })}
                  placeholder="Aadhar Number"
                  className="user-input"
                />
              ) : (
                <input
                  type="text"
                  {...register("cinTaNumber", { 
                    required: "CIN/TA is required",
                  })}
                  placeholder="CIN/TA Number"
                  className="user-input"
                />
              )}
              {selectedRadio === "individual" && errors.aadhar && (
                <span className="error">{errors.aadhar.message}</span>
              )}
              {selectedRadio === "corporate" && errors.cinTaNumber && (
                <span className="error">{errors.cinTaNumber.message}</span>
              )}
            </div>
            
            <div className="form-column">
              <input
                type="text"
                {...register("pancard", { 
                  required: "PAN is required",
                  pattern: {
                    value: /^[A-Z]{5}[0-9]{4}[A-Z]{1}$/,
                    message: "Invalid PAN format (e.g., ABCDE1234F)"
                  }
                })}
                placeholder="PAN Number"
                className="user-input"
              />
              {errors.pancard && <span className="error">{errors.pancard.message}</span>}

              <input
                type="text"
                {...register("mobilenumber", { 
                  required: "Mobile number is required",
                  pattern: {
                    value: /^[6-9][0-9]{9}$/,
                    message: "Invalid Indian mobile number"
                  }
                })}
                placeholder="Mobile Number"
                className="user-input"
              />
              {errors.mobilenumber && <span className="error">{errors.mobilenumber.message}</span>}
            </div>
          </div>

          {/* OTP Section */}
          {!otpSent && (
            <button
              type="button"
              className="send-otp-btn"
              onClick={handleSendOTP}
              disabled={!isFormComplete() || loading}
            >
              {loading ? "Sending OTP..." : "Send OTP"}
            </button>
          )}

          {otpSent && (
            <div className="otp-verification">
              <input
                type="text"
                placeholder="Enter OTP"
                value={otp}
                onChange={(e) => setOtp(e.target.value)}
                className="user-input"
              />
            </div>
          )}

          {/* Register Button */}
          <button
            type="submit"
            className={`register-btn ${!otpSent || otp !== "123456" || loading ? 'disabled' : ''}`}
            disabled={!otpSent || otp !== "123456" || loading}
          >
            {loading ? "Registering..." : "Register"}
          </button>
        </form>

        {/* Login Link */}
        <p className="login-link">
          Already have an account? <Link to="/login">Login here</Link>
        </p>
      </div>
      <ToastContainer />
    </div>
  );
};

export default Register;

/claim-user-service/_user/_registration
this is for individual
Request:

{
    "claimant": {
        "uuid": "",
        "claimantName": "Suman Gupta",
        "corporateName": null,
        "emailId": "suman.g@example.com",
        "claimantEntityType": "INDV",
        "mobileNumber": "9976543210",
        "aadharNumber": "132143214321",
        "panNumber": "NUYPS5678M",
        "cinOrTaNumber": null,
        "roleCode": "CLAIMANT",
        "active": true,
        "auditDetails": {
            "createdBy": "system_user",
            "createdDate": "2025-04-09T10:30:00",
            "lastModifiedBy": "system_user",
            "lastModifiedDate": "2025-04-09T10:30:00"
        }
    },
    "corporateDebtor": null
}

Response:

{
    "status": {
        "code": "201",
        "message": "CREATE"
    },
    "otp": "123456",
    "users": [
        {
            "claimant": {
                "uuid": "718a0722-ea0b-4bc4-a022-4e2a6d138d52",
                "claimantName": "Suman Gupta",
                "corporateName": null,
                "emailId": "suman.g@example.com",
                "claimantEntityType": "INDV",
                "mobileNumber": "9976543210",
                "aadharNumber": "132143214321",
                "panNumber": "NUYPS5678M",
                "cinOrTaNumber": null,
                "roleCode": "CLAIMANT",
                "auditDetails": {
                    "createdBy": "718a0722-ea0b-4bc4-a022-4e2a6d138d52",
                    "lastModifiedBy": "718a0722-ea0b-4bc4-a022-4e2a6d138d52",
                    "createdTime": "2025-04-09 16:09:45.368",
                    "lastModifiedTime": "2025-04-09 16:09:45.368"
                },
                "active": true
            },
            "corporateDebtor": null
        }
    ]
}

this is for corporate


Request:

{
    "claimant": {
        "uuid": "",
        "claimantName": null,
        "corporateName": "Xenon Technologies Pvt Ltd",
        "emailId": "contact@xenontech.com",
        "claimantEntityType": "CORP",
        "mobileNumber": "9001122334",
        "aadharNumber": null,
        "panNumber": "XENPT6789L",
        "cinOrTaNumber": "L12345MH2005PLC098765",
        "roleCode": "CLAIMANT",
        "active": true,
        "auditDetails": {
            "createdBy": "admin_system",
            "createdDate": "2025-04-09T14:25:30",
            "lastModifiedBy": "admin_system",
            "lastModifiedDate": "2025-04-09T14:25:30"
        }
    },
    "corporateDebtor": null
}

Response:

{
    "status": {
        "code": "201",
        "message": "CREATE"
    },
    "otp": "123456",
    "users": [
        {
            "claimant": {
                "uuid": "7b89cc87-b0fc-4c46-8895-b011b782a756",
                "claimantName": null,
                "corporateName": "Xenon Technologies Pvt Ltd",
                "emailId": "contact@xenontech.com",
                "claimantEntityType": "CORP",
                "mobileNumber": "9001122334",
                "aadharNumber": null,
                "panNumber": "XENPT6789L",
                "cinOrTaNumber": "L12345MH2005PLC098765",
                "roleCode": "CLAIMANT",
                "auditDetails": {
                    "createdBy": "7b89cc87-b0fc-4c46-8895-b011b782a756",
                    "lastModifiedBy": "7b89cc87-b0fc-4c46-8895-b011b782a756",
                    "createdTime": "2025-04-09 16:16:37.101",
                    "lastModifiedTime": "2025-04-09 16:16:37.101"
                },
                "active": true
            },
            "corporateDebtor": null
        }
    ]
}
