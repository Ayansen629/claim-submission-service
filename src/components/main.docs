import React, { useState, useEffect } from "react";
import { Row, Col, Button, Card, Form } from "react-bootstrap";
import { useTranslation } from "react-i18next";
import InputFile from "./InputFile";
import InputText from "./InputText";
import { renameFile } from "../../utils/constants";
import { BsDownload, BsFileEarmarkPdf, BsPlus, BsTrash } from "react-icons/bs";
import Collapsible from "../common/Collapsible";

interface DocumentsProps {
  formMethods: any;
  viewMode?: boolean;
}

const Documents: React.FC<DocumentsProps> = ({ formMethods, viewMode = false }) => {
  const { t } = useTranslation();
  const [workOrderFiles, setWorkOrderFiles] = useState([{ id: 1 }]);
  const [drawingsFiles, setDrawingsFiles] = useState([{ id: 1 }]);
  
  const {
    register,
    watch,
    setValue,
    clearErrors,
    getValues,
    setError,
    formState: { errors },
  } = formMethods;

  useEffect(() => {
    if (viewMode) {
      const workOrderIds = [];
      const drawingIds = [];
      
      for (let i = 1; i <= 50; i++) {
        const workOrderFile = getValues(`workOrder_file_${i}`);
        const drawingFile = getValues(`drawings_file_${i}`);
        
        if (workOrderFile && workOrderFile.name) {
          workOrderIds.push({ id: i });
        }
        if (drawingFile && drawingFile.name) {
          drawingIds.push({ id: i });
        }
      }
      
      if (workOrderIds.length > 0) setWorkOrderFiles(workOrderIds);
      if (drawingIds.length > 0) setDrawingsFiles(drawingIds);
    }
  }, [viewMode, getValues]);

  const DocumentCard = ({ fileName, description, fileSize, onDownload }) => (
    <Card className="mb-2 border-0 shadow-sm">
      <Card.Body className="p-3">
        <div className="d-flex justify-content-between align-items-center">
          <div className="d-flex align-items-center flex-grow-1">
            <div className="bg-danger bg-opacity-10 rounded-circle p-2 me-3">
              <BsFileEarmarkPdf className="text-danger" size={20} />
            </div>
            <div className="flex-grow-1">
              <h6 className="mb-1 text-primary fw-semibold text-truncate" style={{ maxWidth: '200px' }}>
                {fileName}
              </h6>
              <p className="mb-0 text-muted small">
                {description}
                {fileSize && (
                  <span className="ms-2 badge bg-light text-dark">
                    {(fileSize / 1024 / 1024).toFixed(1)} MB
                  </span>
                )}
              </p>
            </div>
          </div>
          <Button
            variant="outline-primary"
            size="sm"
            onClick={() => onDownload(fileName)}
            className="rounded-circle"
          >
            <BsDownload size={14} />
          </Button>
        </div>
      </Card.Body>
    </Card>
  );

  const FileUploadSection = ({ name, label, onRemove, showRemove }) => (
    <Card className="mb-3 border-2 border-dashed border-primary border-opacity-25 bg-light">
      <Card.Body className="p-3">
        <div className="mb-3">
          <InputFile
            label={label}
            name={name}
            errors={errors}
            onChange={(e) => {
              if (e.target.files?.[0]) {
                const file = e.target.files[0];
                if (file.size > 10 * 1024 * 1024) {
                  setError(name, {
                    type: 'manual',
                    message: t('File size should not exceed 10MB')
                  });
                  return;
                }
                const renamedFile = renameFile(file, `Document_${Date.now()}${file.name.slice(file.name.lastIndexOf('.'))}`);
                clearErrors(name);
                setValue(name, renamedFile, { shouldValidate: true });
              }
            }}
            watch={watch}
            compact={true}
            onRemove={() => {
              setValue(name, null);
              clearErrors(name);
            }}
          />
        </div>
        <div className="mb-3">
          <InputText
            label={t("Description")}
            name={name.replace('_file_', '_desc_')}
            placeholder={t("Enter file description")}
            register={register(name.replace('_file_', '_desc_'))}
            errors={errors}
          />
        </div>
        {showRemove && (
          <div className="text-end">
            <Button
              variant="outline-danger"
              size="sm"
              onClick={onRemove}
              className="rounded-pill"
            >
              <BsTrash className="me-1" size={14} />
              {t("Remove")}
            </Button>
          </div>
        )}
      </Card.Body>
    </Card>
  );

  const FileSection = ({ title, files, type, icon }) => {
    const filesWithData = files.filter(file => watch(`${type}_file_${file.id}`)?.name);

    return (
      <Card className="h-100 shadow-sm border-0">
        <Card.Header className="bg-light border-0 py-3">
          <div className="d-flex justify-content-between align-items-center">
            <h5 className="mb-0 text-primary fw-bold d-flex align-items-center">
              {icon}
              <span className="ms-2">{title}</span>
            </h5>
            <span className="badge bg-primary rounded-pill">
              {filesWithData.length} {t("files")}
            </span>
          </div>
        </Card.Header>

        <Card.Body className="p-0">
          <div style={{ maxHeight: "500px", overflowY: "auto" }} className="p-3">
            {viewMode ? (
              filesWithData.length > 0 ? (
                filesWithData.map((fileInput) => {
                  const fileData = watch(`${type}_file_${fileInput.id}`);
                  const descData = watch(`${type}_desc_${fileInput.id}`);
                  
                  return (
                    <DocumentCard
                      key={fileInput.id}
                      fileName={fileData.name}
                      description={descData || `${title} document`}
                      fileSize={fileData.size}
                      onDownload={() => handleDownload(fileData.name)}
                    />
                  );
                })
              ) : (
                <div className="text-center py-5 text-muted">
                  <BsFileEarmarkPdf size={48} className="opacity-50 mb-3" />
                  <p className="mb-0">{t("No files uploaded")}</p>
                </div>
              )
            ) : (
              files.map((fileInput, idx) => (
                <FileUploadSection
                  key={fileInput.id}
                  label={`${t("File")} ${idx + 1}`}
                  name={`${type}_file_${fileInput.id}`}
                  onRemove={() => removeFileInput(type, fileInput.id)}
                  showRemove={files.length > 1}
                />
              ))
            )}
          </div>
        </Card.Body>

        {!viewMode && (
          <Card.Footer className="bg-light border-0 text-center py-3">
            <Button
              variant="primary"
              size="sm"
              onClick={() => addFileInput(type)}
              className="rounded-pill px-4"
            >
              <BsPlus className="me-1" size={18} />
              {t("Add")} {title}
            </Button>
          </Card.Footer>
        )}
      </Card>
    );
  };

  const SingleFileSection = ({ title, name, icon }) => (
    <Card className="h-100 shadow-sm border-0">
      <Card.Header className="bg-light border-0 py-3">
        <h5 className="mb-0 text-primary fw-bold d-flex align-items-center">
          {icon}
          <span className="ms-2">{title}</span>
        </h5>
      </Card.Header>
      <Card.Body className="p-3">
        {viewMode ? (
          watch(name) ? (
            <DocumentCard
              fileName={watch(name).name}
              description={`${title} document`}
              fileSize={watch(name).size}
              onDownload={() => handleDownload(watch(name).name)}
            />
          ) : (
            <div className="text-center py-4 text-muted">
              <BsFileEarmarkPdf size={32} className="opacity-50 mb-3" />
              <p className="mb-0">{t("No file uploaded")}</p>
            </div>
          )
        ) : (
          <InputFile
            label={title}
            name={name}
            errors={errors}
            onChange={(e) => {
              if (e.target.files?.[0]) {
                const file = e.target.files[0];
                if (file.size > 10 * 1024 * 1024) {
                  setError(name, {
                    type: 'manual',
                    message: t('File size should not exceed 10MB')
                  });
                  return;
                }
                const renamedFile = renameFile(file, `${title.replace(/\s+/g, '_')}_${Date.now()}${file.name.slice(file.name.lastIndexOf('.'))}`);
                clearErrors(name);
                setValue(name, renamedFile, { shouldValidate: true });
              }
            }}
            watch={watch}
            onRemove={() => {
              setValue(name, null);
              clearErrors(name);
            }}
            description={t("Max size: 10MB")}
          />
        )}
      </Card.Body>
    </Card>
  );

  const addFileInput = (type) => {
    const newId = (type === 'workOrder' ? workOrderFiles : drawingsFiles).reduce((max, file) => 
      Math.max(max, file.id), 0) + 1;
    
    if (type === 'workOrder') {
      setWorkOrderFiles([...workOrderFiles, { id: newId }]);
    } else {
      setDrawingsFiles([...drawingsFiles, { id: newId }]);
    }
  };

  const removeFileInput = (type, id) => {
    if (type === 'workOrder') {
      setWorkOrderFiles(workOrderFiles.filter(file => file.id !== id));
    } else {
      setDrawingsFiles(drawingsFiles.filter(file => file.id !== id));
    }
    setValue(`${type}_file_${id}`, null);
    setValue(`${type}_desc_${id}`, "");
    clearErrors([`${type}_file_${id}`, `${type}_desc_${id}`]);
  };

  const handleDownload = (fileName) => {
    console.log(`Downloading ${fileName}`);
    // Actual download implementation would go here
  };

  return (
    <Collapsible title={t("Documents")} openStat={true}>
      <div className="p-3 bg-light rounded-3">
        <Row className="g-4">
          <Col lg={6} md={12}>
            <FileSection
              title={t("Work Order")}
              files={workOrderFiles}
              type="workOrder"
              icon={<BsFileEarmarkPdf className="text-primary" />}
            />
          </Col>

          <Col lg={6} md={12}>
            <FileSection
              title={t("Drawings")}
              files={drawingsFiles}
              type="drawings"
              icon={<BsFileEarmarkPdf className="text-success" />}
            />
          </Col>

          <Col md={6}>
            <SingleFileSection
              title={t('Tonnage Booklets')}
              name="tonnageBooklets"
              icon={<BsFileEarmarkPdf className="text-warning" />}
            />
          </Col>

          <Col md={6}>
            <SingleFileSection
              title={t('Stability Booklets')}
              name="stabilityBooklets"
              icon={<BsFileEarmarkPdf className="text-info" />}
            />
          </Col>

          <Col md={12}>
            <Card className="shadow-sm border-0">
              <Card.Body className="p-3">
                <Form.Check 
                  type="checkbox"
                  id="termsAccepted"
                  label={t('I have thoroughly read and understood all the terms and conditions as mentioned in the Schedule.')}
                  {...register("termsAccepted", { 
                    required: t('You must accept the terms and conditions') 
                  })}
                  disabled={viewMode}
                  feedback={errors.termsAccepted?.message}
                  feedbackType="invalid"
                  className={errors.termsAccepted ? 'is-invalid' : ''}
                />
              </Card.Body>
            </Card>
          </Col>
        </Row>
      </div>
    </Collapsible>
  );
};

export default Documents;
