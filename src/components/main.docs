import React from "react";
import { Card, Row, Col, Table, Badge } from "react-bootstrap";
import { useTranslation } from "react-i18next";
import { BsCheckCircleFill, BsClock, BsXCircleFill } from "react-icons/bs";

const PaymentDetailsView = ({ supervisionAuthority, formData }) => {
  const { t } = useTranslation();

  const paymentData = {
    invoiceNumber: "INV-2024-001",
    invoiceDate: "2024-01-15",
    dueDate: "2024-01-30",
    totalAmount: 590,
    paidAmount: 590,
    status: "paid", 
    paymentMethod: "offline",
    transactionId: "TXN123456789",
    paymentDate: "2024-01-16",
    challanNumber: "CH-2024-001"
  };

  const getStatusBadge = (status) => {
    switch (status) {
      case 'paid':
        return <Badge bg="success" className="d-flex align-items-center gap-1">
          <BsCheckCircleFill size={12} /> {t("Paid")}
        </Badge>;
      case 'pending':
        return <Badge bg="warning" className="d-flex align-items-center gap-1">
          <BsClock size={12} /> {t("Pending")}
        </Badge>;
      case 'failed':
        return <Badge bg="danger" className="d-flex align-items-center gap-1">
          <BsXCircleFill size={12} /> {t("Failed")}
        </Badge>;
      default:
        return <Badge bg="secondary">{t("Unknown")}</Badge>;
    }
  };

  const calculateFees = () => {
    const workOrderValue = parseFloat(formData?.workOrderValue || 1000000);
    
    if (supervisionAuthority === "iwt") {
      const drawingFee = Math.max(workOrderValue * 0.001, 500);
      let supervisionFee;
      
      if (workOrderValue <= 1000000) {
        supervisionFee = Math.max(workOrderValue * 0.01, 1000);
      } else if (workOrderValue <= 4000000) {
        supervisionFee = 10000 + (workOrderValue - 1000000) * 0.005;
      } else {
        supervisionFee = 25000 + (workOrderValue - 4000000) * 0.0025;
      }
      
      return {
        drawingFee: drawingFee,
        supervisionFee: supervisionFee,
        total: drawingFee + supervisionFee
      };
    } else {
      const drawingFee = Math.max(workOrderValue * 0.0005, 500);
      return {
        drawingFee: drawingFee,
        supervisionFee: 0,
        total: drawingFee
      };
    }
  };

  const fees = calculateFees();

  return (
    <div className="container py-4">
      <Row className="g-4">
        {/* Payment Summary Card */}
        <Col lg={6}>
          <Card className="h-100 shadow-sm border-0">
            <Card.Header className="bg-primary text-white py-3">
              <h5 className="mb-0 fw-bold text-white">{t("Payment Summary")}</h5>
            </Card.Header>
            <Card.Body className="p-4">
              <div className="mb-3 d-flex justify-content-between align-items-center">
                <span className="fw-semibold">{t("Invoice Number")}:</span>
                <span className="text-primary fw-bold">{paymentData.invoiceNumber}</span>
              </div>
              
              <div className="mb-3 d-flex justify-content-between align-items-center">
                <span className="fw-semibold">{t("Invoice Date")}:</span>
                <span>{paymentData.invoiceDate}</span>
              </div>
              
              <div className="mb-3 d-flex justify-content-between align-items-center">
                <span className="fw-semibold">{t("Status")}:</span>
                {getStatusBadge(paymentData.status)}
              </div>
              
              <hr />
              
              <div className="mb-2 d-flex justify-content-between">
                <span>{supervisionAuthority === "iwt" ? t("Drawing Approval Fee") : t("Drawing Scrutiny Fee")}:</span>
                <span>₹{fees.drawingFee.toFixed(2)}</span>
              </div>
              
              {supervisionAuthority === "iwt" && (
                <div className="mb-2 d-flex justify-content-between">
                  <span>{t("Supervision Fee")}:</span>
                  <span>₹{fees.supervisionFee.toFixed(2)}</span>
                </div>
              )}
              
              <hr />
              
              <div className="d-flex justify-content-between align-items-center">
                <span className="fw-bold fs-5">{t("Total Amount")}:</span>
                <span className="fw-bold fs-5 text-success">₹{fees.total.toFixed(2)}</span>
              </div>
            </Card.Body>
          </Card>
        </Col>

        {/* Payment Details Card */}
        <Col lg={6}>
          <Card className="h-100 shadow-sm border-0">
            <Card.Header className="bg-success text-white py-3">
              <h5 className="mb-0 fw-bold text-white">{t("Payment Details")}</h5>
            </Card.Header>
            <Card.Body className="p-4">
              <div className="mb-3">
                <label className="fw-semibold text-muted small">{t("Payment Method")}</label>
                <div className="mt-1">
                  <Badge bg="info" className="px-3 py-2">
                    {paymentData.paymentMethod === 'offline' ? t("Offline Payment") : t("Online Payment")}
                  </Badge>
                </div>
              </div>

              {paymentData.paymentMethod === 'offline' && (
                <div className="mb-3">
                  <label className="fw-semibold text-muted small">{t("Challan Number")}</label>
                  <div className="mt-1 p-2 bg-light rounded">
                    <span className="fw-medium">{paymentData.challanNumber}</span>
                  </div>
                </div>
              )}

              <div className="mb-3">
                <label className="fw-semibold text-muted small">{t("Transaction ID")}</label>
                <div className="mt-1 p-2 bg-light rounded">
                  <span className="fw-medium font-monospace">{paymentData.transactionId}</span>
                </div>
              </div>

              <div className="mb-3">
                <label className="fw-semibold text-muted small">{t("Payment Date")}</label>
                <div className="mt-1 p-2 bg-light rounded">
                  <span className="fw-medium">{paymentData.paymentDate}</span>
                </div>
              </div>

              <div className="mb-3">
                <label className="fw-semibold text-muted small">{t("Amount Paid")}</label>
                <div className="mt-1 p-3 bg-success bg-opacity-10 rounded border border-success border-opacity-25">
                  <span className="fw-bold text-success fs-5">₹{paymentData.paidAmount.toFixed(2)}</span>
                </div>
              </div>
            </Card.Body>
          </Card>
        </Col>

       
        <Col lg={12}>
          <Card className="shadow-sm border-0">
            <Card.Header className="bg-light py-3">
              <h5 className="mb-0 fw-bold text-primary">{t("Fee Breakdown")}</h5>
            </Card.Header>
            <Card.Body className="p-0">
              <Table responsive className="mb-0">
                <thead className="bg-light">
                  <tr>
                    <th className="py-3 px-4">{t("Description")}</th>
                    <th className="py-3 px-4">{t("Calculation")}</th>
                    <th className="py-3 px-4 text-end">{t("Amount")}</th>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    <td className="py-3 px-4 fw-semibold">
                      {supervisionAuthority === "iwt" ? t("Drawing Approval Fee") : t("Drawing Scrutiny Fee")}
                    </td>
                    <td className="py-3 px-4 text-muted">
                      {supervisionAuthority === "iwt" 
                        ? t("0.1% of work order value (Min ₹500)")
                        : t("0.05% of work order value (Min ₹500)")
                      }
                    </td>
                    <td className="py-3 px-4 text-end fw-semibold">₹{fees.drawingFee.toFixed(2)}</td>
                  </tr>
                  
                  {supervisionAuthority === "iwt" && (
                    <tr>
                      <td className="py-3 px-4 fw-semibold">{t("Supervision Fee")}</td>
                      <td className="py-3 px-4 text-muted">
                        {t("Based on work order value tiers")}
                      </td>
                      <td className="py-3 px-4 text-end fw-semibold">₹{fees.supervisionFee.toFixed(2)}</td>
                    </tr>
                  )}
                  
                  <tr className="table-success">
                    <td className="py-3 px-4 fw-bold">{t("Total")}</td>
                    <td className="py-3 px-4"></td>
                    <td className="py-3 px-4 text-end fw-bold fs-5">₹{fees.total.toFixed(2)}</td>
                  </tr>
                </tbody>
              </Table>
            </Card.Body>
          </Card>
        </Col>
      </Row>
    </div>
  );
};

export default PaymentDetailsView;
export const VESSEL_COLUMNS = [
    {
        colName: "Registration No.",
        fieldName: "registrationNo",
        clsName: "column-text-start",
    },
    {
        colName: "Vessel Name",
        fieldName: "vesselName",
        clsName: "column-text-start",
    },
    {
        colName: "Registered Year",
        fieldName: "registeredYear",
        clsName: "column-text-start",
    },
    {
        colName: "Last Survey Date",
        fieldName: "lastSurveyDate",
        clsName: "column-text-start",
    },
     {
    colName: "Action",
    fieldName: "action",
    actions: [
        'view'
        // 'uploadReports','vesselRegistration','provisionalCertificate','revisedDrawing','paySupervisionFee'
    ],
    clsName: "col-sm-2 column-text-start actions-column",
  },
];


export const INBOX_COLUMNS = [
    {
        colName: "Application No",
        fieldName: "appNo",
        clsName: "column-text-start",
    },
    {
        colName: "Application Type",
        fieldName: "appType",
        clsName: "column-text-start",
    },
    {
        colName: "Date of Submission",
        fieldName: "date",
        clsName: "column-text-start",
    },
    {
        colName: "Status",
        fieldName: "status",
        clsName: "column-text-start",
        render: (value) => (
            <span className={`status-badge ${value.toLowerCase()}`}>{value}</span>
        ),
    },
    {
        colName: "Pending With",
        fieldName: "pendingWith",
        clsName: "column-text-start",
    },
];



export const TRANSACTION_COLUMNS = [
    {
        colName: "Name of person from whom title is derived",
        fieldName: "from",
        clsName: "column-text-start",
    },
    {
        colName: "No. of shares Affected",
        fieldName: "shares",
        clsName: "column-text-start",
    },
    {
        colName: "Date and Hours of Registry",
        fieldName: "dateAndHour",
        clsName: "column-text-start",
    },
    {
        colName: "Nature and Date of Transaction",
        fieldName: "nature",
        clsName: "column-text-start",
    },
    {
        colName:
            "Name, Residence and Occupation of Transferee, Mortgagee or other person acquiring Title or power",
        fieldName: "transferee",
        clsName: "column-text-start",
    },
    {
        colName: "Number of Transaction",
        fieldName: "transactionNo",
        clsName: "column-text-start",
    },
    { colName: "Remarks", fieldName: "remarks", clsName: "column-text-start" },
];


export const MOCK_TRANSACTION_DATA = Array.from({ length: 20 }, (_, i) => ({
    from: `Person ${i + 1}`,
    shares: `${(i % 10) + 1}`,
    dateAndHour: `2025-0${(i % 9) + 1}-${(i % 28) + 1} ${8 + (i % 10)}:00`,
    nature: ["Transfer", "Mortgage", "Sale"][i % 3] + ` - 2025-0${(i % 9) + 1}`,
    transferee: `Transferee ${i + 1}, City ${i % 5}, Occupation ${["Business", "Bank", "Govt"][i % 3]
        }`,
    transactionNo: `${i + 1}`,
    remarks: ["First transfer", "Mortgage created", "Ownership changed"][i % 3],
}));



export const vesselTypesChartData = {
  labels: ['Passenger', 'Cargo', 'Tanker', 'Fishing', 'Others'],
  datasets: [
    {
      label: 'Type of Inland Vessels',
      data: [45, 25, 15, 10, 5],
      backgroundColor: [
        '#36A2EB',
        '#FF6384',
        '#FFCE56',
        '#4BC0C0',
        '#9966FF',
      ],
      borderWidth: 1,
    },
  ],
};
 
import React, { useEffect, useState } from "react";
import DynamicTabs from "../../../common/DynamicTabs.js";
import WelcomeBox from "../../../common/WelcomeBox.js";
import StatusCard from "../../../common/StatusCard.js";
import ActionTable from "../../../common/ActionTable.tsx";
import {
    BsExclamationTriangle,
    BsClipboardData,
    BsSearch,
    BsFilter,
} from "react-icons/bs";
import { useLanguageContext } from "../../../common/LanguageContext.js";
import "./vesselOwner.css";
import {
    Card,
    Form,
    InputGroup,
    Button,
    Row,
    Col,
    Table,
} from "react-bootstrap";
import { CHART_OPTIONS, CHART_TYPES, getUserRoleName, headerStyle } from "../../../../utils/constants.ts";
import VerticalMarquee from "../../../common/VerticalMarquee.js";
import chart_icon from "../../../../assets/icons/chart_icon.png";
import {
    INBOX_COLUMNS,
    MOCK_TRANSACTION_DATA,
    VESSEL_COLUMNS,
    vesselTypesChartData,
} from "./vesselconstants.js";
import InputText from "../../../common/InputText.tsx";
import { useTranslation } from "react-i18next";
import InputDateRangePicker from "../../../common/InputDateRangePicker";
import ActionModal from "../../../common/ActionModal.tsx";
import DynamicChart from "../../../common/DynamicChart";
import { Link } from "react-router-dom";
import { useNavigate } from "react-router-dom";

const generateInboxData = () => {
    const statuses = ["Completed", "Processing", "Rejected"];
    const appTypes = [
        "Registration",
        "Survey",
        "Manning",
        "Certification",
        "Training",
    ];
    const owners = [
        "John Doe",
        "Jane Smith",
        "Robert Johnson",
        "Emily Davis",
        "Michael Wilson",
    ];

    const pendingWithLevels = [
        "Level 1 Approver",
        "Level 2 Approver",
        "Level 3 Approver",
    ];

    return Array.from({ length: 20 }, (_, i) => ({
        id: i + 1,
        appNo: `APP-2025-${(i + 1).toString().padStart(3, "0")}`,
        appType: appTypes[Math.floor(Math.random() * appTypes.length)],
        owner: owners[Math.floor(Math.random() * owners.length)],
        vesselNo: `WB${1420 + i}`,
        date: new Date(
            2025,
            Math.floor(Math.random() * 12),
            Math.floor(Math.random() * 28) + 1
        )
            .toISOString()
            .split("T")[0],
        status: statuses[Math.floor(Math.random() * statuses.length)],
        pendingWith:
            pendingWithLevels[Math.floor(Math.random() * pendingWithLevels.length)],
    }));
};

const generateVesselData = (count = 20) => {
    const vesselNames = [
        "Sea Hawk",
        "Blue Pearl",
        "Ocean Spirit",
        "Wave Rider",
        "Aqua Nova",
        "Neptune",
        "Storm Chaser",
        "Marine Star",
    ];

    return Array.from({ length: count }, (_, i) => {
        const regNo = `REG-${1000 + i}`;
        return {
            id:regNo,
            registrationNo: (
                <Link 
                    to={`/vessel-registration/new-vessel/edit/${regNo}`}
                    style={{ color: '#007bff', textDecoration: 'underline',cursor: 'pointer' }}
                >
                    {regNo}
                </Link>
            ),
            vesselName: vesselNames[Math.floor(Math.random() * vesselNames.length)],
            registeredYear: Math.floor(Math.random() * 23) + 2000,
            lastSurveyDate: new Date(
                Date.now() - Math.random() * (Date.now() - new Date(2020, 0, 1))
            )
                .toISOString()
                .split("T")[0],
        };
    });
};

const VesselOwner = ({ userDetails, userRole }) => {
    //   const tabs = Array.from({ length: 8 }, (_, i) => ({
    //     title: `Vessel ${i + 1}`,
    //     content: <VesselContent />,
    //     onLoad: () => console.log(`Loading Vessel ${i + 1}...`),
    //   }));
    console.log(userRole);
    const { t } = useTranslation();
      const { changeLanguage } = useLanguageContext();
      
    const navigate = useNavigate();

    return (
        <>
            <WelcomeBox
                userName={
                    userDetails
                        ? userDetails?.firstName + " " + userDetails?.lastName + " User"
                        : ""
                }
                title={getUserRoleName(userRole)}
                desc={t("Here’s your vessel performance overview.")}
            />

            {/* <DynamicTabs
        tabs={tabs}
        onTabChange={(i) => console.log(`Switched to tab ${i}`)}
      /> */}

      <VesselContent navigate={navigate} />
        </>
    );
};


const VesselContent = ({ vesselNumber }) => {
    const { t } = useTranslation();
      const { changeLanguage } = useLanguageContext();
      // For Vessel Table
const vesselColumns = VESSEL_COLUMNS.map(col => ({
  ...col,
  colName: t(col.colName)
}));

// For Inbox Table
const inboxColumns = INBOX_COLUMNS.map(col => ({
  ...col,
  colName: t(col.colName)
}));
    const [inboxData, setInboxData] = useState([]);
    const [filteredInboxData, setFilteredInboxData] = useState([]);
    const [inboxCurrentPage, setInboxCurrentPage] = useState(1);
    const [inboxSearchTerm, setInboxSearchTerm] = useState("");
    const itemsInboxPerPage = 5;

    const [vesselData, setVesselData] = useState([]);
    const [filteredVesselData, setFilteredVesselData] = useState([]);
    const [vesselCurrentPage, setVesselCurrentPage] = useState(1);
    const [vesselSearchTerm, setVesselSearchTerm] = useState("");
    const itemsVesselPerPage = 5;
    const [isModalOpen, setIsModalOpen] = useState(false);
     const navigate = useNavigate();
      const handleViewAction = ( rowData) => {

           
            const regNo = rowData.id || 'REG-1000'; 
            navigate(`/vessel-registration/new-vessel/view/${regNo}`);
      
    };


    //   const mockItems = [
    //     {
    //       id: 1,
    //       title: "Welcome to the Portal",
    //       description: "Scroll for updates",
    //       type: "text",
    //     },
    //     {
    //       id: 2,
    //       title: "Policy Update",
    //       type: "document",
    //       documentUrl: "/docs/policy.pdf",
    //     },
    //     {
    //       id: 3,
    //       title: "Release Notes",
    //       description: "Version 2.0 released",
    //       type: "text",
    //     },
    //     {
    //       id: 4,
    //       title: "HR Guidelines",
    //       type: "document",
    //       documentUrl: "/docs/hr-guidelines.pdf",
    //     },
    //   ];

    useEffect(() => {
        const data = generateInboxData();
        setInboxData(data);
        setFilteredInboxData(data);

        const vdata = generateVesselData();
        setVesselData(vdata);
        setFilteredVesselData(vdata);
    }, []);

    useEffect(() => {
        const results = inboxData?.filter(
            (item) =>
                item.appNo?.toLowerCase()?.includes(inboxSearchTerm?.toLowerCase()) ||
                item.appType?.toLowerCase()?.includes(inboxSearchTerm?.toLowerCase()) ||
                item.owner?.toLowerCase()?.includes(inboxSearchTerm?.toLowerCase()) ||
                item.vesselNo
                    ?.toLowerCase()
                    ?.includes(inboxSearchTerm?.toLowerCase()) ||
                item.status?.toLowerCase()?.includes(inboxSearchTerm?.toLowerCase()) ||
                item.pendingWith
                    ?.toLowerCase()
                    ?.includes(inboxSearchTerm?.toLowerCase())
        );
        setFilteredInboxData(results);
        setInboxCurrentPage(1);
    }, [inboxSearchTerm, inboxData]);

    const handleTablePageChange = (page) => {
        setInboxCurrentPage(page);
    };

    useEffect(() => {
        const results = vesselData?.filter(
            (item) =>
                item?.registrationNo
                    ?.toString()
                    ?.toLowerCase()
                    ?.includes(vesselSearchTerm?.toLowerCase()) ||
                item?.vesselName
                    ?.toLowerCase()
                    ?.includes(vesselSearchTerm?.toLowerCase()) ||
                item?.registeredYear
                    ?.toString()
                    ?.toLowerCase()
                    ?.includes(vesselSearchTerm?.toLowerCase()) ||
                item?.lastSurveyDate
                    ?.toLowerCase()
                    ?.includes(vesselSearchTerm?.toLowerCase())
        );
        setFilteredVesselData(results);
        setVesselCurrentPage(1);
    }, [vesselSearchTerm, vesselData]);

    const handleVesselTablePageChange = (page) => {
        setVesselCurrentPage(page);
    };

    const handleCardClick = (cardInfo, index) => {
        if (index === 0) {
            navigate("/user-registration/view");
        } else if (index === 1) {
            setIsModalOpen(true);
        }
    };

    const statData = [
        {
            title: t("Total Registered Vessel Count"),
            value: 35,
            icon: BsClipboardData,
            color: "#28a745",
            subtitle: t("(Total Number of Registered Vessels)"),
        },

        {
            title: t("Graphical Representation"),
            imageSrc: chart_icon,
            color: "#6ec1e4",
            subtitle: t("(Total number of registered vessel as per 'Type')"),
        },

        {
            title: t("Survey Due"),
            value: 20,
            icon: BsExclamationTriangle,
            color: "#E8B60E",
            subtitle: t("(Total Number of Vessel having Survey due within next 30Days)"),
        },
        {
            title: t("Insurance Due"),
            value: 15,
            icon: BsExclamationTriangle,
            color: "#902424",
            subtitle: t("(Total Number of Vessel having Insurance due within next 30Days)"),
        },
    ];
    //   const [transactionSearch, setTransactionSearch] = useState("");
    //   const tableData = {
    //     regNo: "WB1423",
    //     vesselName: "SALT AND LIGHT",
    //     vesselType: "Passenger",
    //     yearOfRegistry: "2007",
    //     currentStatus: "Active",
    //     portOfRegistry: "Kolkata",
    //     lastSurvey: "25-10-2025",
    //   };

    // Filtered data for Transaction Info
    // const filteredTransactionData = MOCK_TRANSACTION_DATA.filter((item) =>
    //     Object.values(item).some((val) =>
    //         String(val)?.toLowerCase()?.includes(transactionSearch.toLowerCase())
    //     )
    // );

    return (
        <>
            {/* Vessel Info table */}

            {/* <Card className="bg-transparent" style={{ marginBottom: 0 }}>
                <Card.Header style={headerStyle}>Vessel Information</Card.Header>
            </Card>
            <Table
                responsive="md"
                striped
                bordered
                hover
                className="custom-vessel-table"
            >
                <thead>
                    <tr className="custom-header">
                        <th>Regi No</th>
                        <th>Vessel Name</th>
                        <th>Vessel Type</th>
                        <th>Year of Registry</th>
                        <th>Current Status</th>
                        <th>Port of Registry</th>
                        <th>Last Survey</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>{tableData.regNo}</td>
                        <td>{tableData.vesselName}</td>
                        <td>{tableData.vesselType}</td>
                        <td>{tableData.yearOfRegistry}</td>
                        <td>
                            <span className="status-badge">{tableData.currentStatus}</span>
                        </td>
                        <td>{tableData.portOfRegistry}</td>
                        <td>{tableData.lastSurvey}</td>
                    </tr>
                </tbody>
            </Table> */}

            <div className="row g-3">
                {statData.map((item, index) => (
                    <div className="col-md-4 col-lg-3 col-sm-12" key={index}>
                        <StatusCard
                            {...item}
                            onClick={() => handleCardClick(item, index)}
                        />
                    </div>
                ))}

                {/* <div className="col-md-4 col-lg-3 col-sm-12">
                    <VerticalMarquee items={mockItems} speed={40} />
                </div> */}
            </div>

            <br />

            {/*           Transaction Subsequent To Registration */}
            {/* <Card
                className="bg-transparent"
                style={{ marginBottom: 0, marginTop: "1rem" }}
            >
                <Card.Header style={headerStyle}>
                    Transaction Subsequent To Registration
                    <div style={{ float: "right", width: 260 }}>
                        <input
                            type="search"
                            value={transactionSearch}
                            onChange={(e) => setTransactionSearch(e.target.value)}
                            placeholder="Search Transactions"
                            className="form-control"
                        />
                    </div>
                </Card.Header>
                <div style={{ position: "relative", bottom: "2rem" }}>
                    <ActionTable
                        columns={TRANSACTION_COLUMNS}
                        rowData={filteredTransactionData}
                        tableHeader={null}
                        hideExportBtn={false}
                        hideSearchBar={true}
                        pagination={false}
                        noDataText="No transactions found."
                    />
                </div>
            </Card> */}


            {/* -----------------------------------   VESSEL SEARCH    --------------------------- */}

            <Card>
                <Card.Header
                    style={headerStyle}
                    className="dashboard-card-header d-flex justify-content-between align-items-center flex-wrap"
                >
                    <div className="d-flex align-items-center">
                        <h6 className="mb-0 me-2">{t("Vessel Information")}</h6>
                    </div>
                </Card.Header>

                <form
                    style={{ padding: "2rem", margin: 0 }}
                // onSubmit={handleSubmit(onSubmitFilterForm)}
                >
                    <Row>
                        <Col md={3}>
                            <InputText
                                type="text"
                                name="regisNo"
                                maxlength={13}
                                label={t("Registration No.")}
                                placeholder={t("Enter Registration No.")}
                            // register={{
                            //     ...register("regisNo", {}),
                            // }}
                            // errors={errors}
                            />
                        </Col>

                        <Col md={3}>
                            <InputText
                                type="text"
                                name="vesselName"
                                label={t("Vessel Name")}
                                maxlength={80}
                                placeholder={t("Enter Vessel name..")}
                            // register={{
                            //     ...register("vesselName", {}),
                            // }}
                            // errors={errors}
                            />
                        </Col>

                        <Col md={4}>
                            <InputDateRangePicker
                                label={t("Date Range")}
                                // value={selectedRange}
                                // onChange={(range) => setValue("dateRange", range)}
                                // register={register}
                                name="dateRange"
                            // error={errors.dateRange}
                            />
                        </Col>

                        <Col md={2}>
                            <button
                                style={{ position: "relative", top: "1.5rem" }}
                                className="generic-btn-submit"
                                type="button"
                            >
                                {t("Filter")}
                            </button>
                        </Col>
                    </Row>
                </form>

                <Card className="bg-transparent dashboard-card">
                    <Card.Header
                        style={headerStyle}
                        className="dashboard-card-header d-flex justify-content-between align-items-center flex-wrap"
                    >
                        <div className="d-flex align-items-center">
                            <h6 className="mb-0 me-2">{t("Vessel List")}</h6>
                        </div>
                        <div className="d-flex align-items-center">
                            <Button variant="outline-secondary" className="filter-btn me-2">
                                <BsFilter size={18} />
                                <span className="ms-1 d-none d-sm-inline">Filter</span>
                            </Button>
                            <Form className="inbox-search">
                                <InputGroup>
                                    <Form.Control
                                        type="text"
                                        placeholder="Search Vessels..."
                                        aria-label="Search"
                                        value={vesselSearchTerm}
                                        onChange={(e) => setVesselSearchTerm(e.target.value)}
                                    />
                                    <Button variant="primary">
                                        <BsSearch />
                                    </Button>
                                </InputGroup>
                            </Form>
                        </div>
                    </Card.Header>

                    <div style={{ position: "relative", bottom: "2rem" }}>
                        <ActionTable
                            columns={vesselColumns}
                             onViewRow={handleViewAction}
                            rowData={filteredVesselData}
                            tableHeader={null}
                            hideExportBtn={true}
                            hideSearchBar={true}
                            pagination={true}
                            inboxCurrentPage={vesselCurrentPage}
                            itemsInboxPerPage={itemsVesselPerPage}
                            onPageChange={handleVesselTablePageChange}
                            noDataText={
                                <div className="no-results text-center py-4">
                                    <BsSearch size={24} className="mb-2" />
                                    <p>No results found</p>
                                    <small>
                                        Try adjusting your search to find what you're looking for.
                                    </small>
                                </div>
                            }
                        />
                    </div>
                </Card>
            </Card>
            {/* -----------------------------------  VESSEL SEARCH END  --------------------------- */}


            {/* -----------------------------------   INBOX    --------------------------- */}

            <Card className="bg-transparent mt-4 dashboard-card">
                <Card.Header
                    style={headerStyle}
                    className="dashboard-card-header d-flex justify-content-between align-items-center flex-wrap"
                >
                    <div className="d-flex align-items-center">
                        <h5 className="mb-0 me-2">{t("Inbox")}</h5>
                        <span className="badge bg-primary rounded-pill">
                            {filteredInboxData.length}
                        </span>
                    </div>
                    <div className="d-flex align-items-center">
                        <Button variant="outline-secondary" className="filter-btn me-2">
                            <BsFilter size={18} />
                            <span className="ms-1 d-none d-sm-inline">{t("Filter")}</span>
                        </Button>
                        <Form className="inbox-search">
                            <InputGroup>
                                <Form.Control
                                    type="text"
                                    placeholder="Search inbox..."
                                    aria-label="Search inbox"
                                    value={inboxSearchTerm}
                                    onChange={(e) => setInboxSearchTerm(e.target.value)}
                                />
                                <Button variant="primary">
                                    <BsSearch />
                                </Button>
                            </InputGroup>
                        </Form>
                    </div>
                </Card.Header>

                <div style={{ position: "relative", bottom: "2rem" }}>
                    <ActionTable
                        columns={inboxColumns}
                        rowData={filteredInboxData}
                        tableHeader={null}
                        hideExportBtn={true}
                        hideSearchBar={true}
                        pagination={true}
                        inboxCurrentPage={inboxCurrentPage}
                        itemsInboxPerPage={itemsInboxPerPage}
                        onPageChange={handleTablePageChange}
                        noDataText={
                            <div className="no-results text-center py-4">
                                <BsSearch size={24} className="mb-2" />
                                <p>No results found</p>
                                <small>
                                    Try adjusting your search to find what you're looking for.
                                </small>
                            </div>
                        }
                    />
                </div>
            </Card>

            {/* -----------------------------------   INBOX  END  --------------------------- */}



            {/* -----------------------------------   CHART    --------------------------- */}
            <ActionModal
                show={isModalOpen}
                close={setIsModalOpen}
                title="Inland Vessel Types"
                size={"md"}
                isFooterHide={true}>

                <DynamicChart
                    type={CHART_TYPES[3]}
                    data={vesselTypesChartData}
                    options={CHART_OPTIONS}
                />
            </ActionModal>
            {/* -----------------------------------   CHART END  --------------------------- */}

        </>
    );
};

export default VesselOwner;

