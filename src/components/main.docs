import { BrowserRouter } from "react-router-dom";
import "bootstrap/dist/css/bootstrap.min.css";
import "bootstrap/dist/js/bootstrap.bundle.min";
import PublicRoutes from "./routes/PublicRoutes";
import PrivateRoutes from "./routes/PrivateRoutes";
import { ToastContainer } from "react-toastify";
import { ApplicationContextProvider } from "./components/context/ApplicationContext"; // Import the ApplicationContextProvider

function App() {
  // changes
  return (
    <BrowserRouter>
      {/* Wrap the entire application with ApplicationContextProvider */}
      <ApplicationContextProvider>
        <ToastContainer autoClose={2000} style={{ color: "black" }} />
        <PublicRoutes />
        <PrivateRoutes />
      </ApplicationContextProvider>
    </BrowserRouter>
  );
}

export default App;


import { Routes, Route } from "react-router-dom";
import PublicLayout from "../components/layouts/PublicLayout";
import Login from "../components/public/Login";
import Register from "../components/public/Register";
import CorporateDebtorRegister from "../components/public/CorporateRegister";


const PublicRoutes = () => {
  return (
    <Routes>
      <Route element={<PublicLayout />}>
        <Route path="/login" element={<Login />} />
        <Route path="/register/claimants" element={<Register />} />
        <Route path="/register/corporate-debtor" element={<CorporateDebtorRegister />} />
       
      </Route>
    </Routes>
  );
};

export default PublicRoutes;
