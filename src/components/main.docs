import React from "react";
import Select from "react-select";
import { Form } from "react-bootstrap";
import "./InputSelect.css";

interface InputSelectProps {
  label: string;
  required?: boolean;
  name: string;
  options: Array<{ value: string | number; label: string }>;
  register?: any;
  errors?: any;
  disabled?: boolean;
  onChange?: (value: string | number, selObj?: any) => void;
  className?: string;
  style?: React.CSSProperties;
  icon?: React.ElementType;
  value?: any;
  isMulti?: boolean;
  isClearable?: boolean;
  placeholder?: string;
}

const InputSelect: React.FC<InputSelectProps> = ({
  label,
  required,
  name,
  options,
  register,
  errors = {},
  disabled,
  onChange,
  className = "",
  style = {},
  icon: IconComponent,
  value,
  isMulti = false,
  isClearable = false,
  placeholder = "-- Select --",
}) => {
  const fieldKey = name.includes(".") ? name.split(".").pop() : name;
  const hasError = errors && errors[fieldKey];
  const errorMessage = hasError?.message;

  const selectedOption = options.find((opt) => String(opt.value) === String(value)) || null;

  const handleChange = (selected: any) => {
    onChange?.(selected ? selected.value : "", selected);
  };

  // Custom menu portal styles to prevent overlapping
  const menuPortalStyles = {
    menuPortal: (base: any) => ({ ...base, zIndex: 9999 }),
  };

  return (
    <Form.Group className={`mb-4 ${className}`} controlId={name}>
      <Form.Label className="fw-semibold d-block">
        {label}
        {required && <span className="text-danger ms-1">*</span>}
      </Form.Label>

      <div className="position-relative">
        {IconComponent && (
          <div
            className={`input-icon position-absolute ${
              hasError ? "text-danger" : value ? "text-success" : "text-primary"
            }`}
            style={{
              left: "10px",
              top: "50%",
              transform: "translateY(-50%)",
              zIndex: 2,
            }}
          >
            <IconComponent size={18} />
          </div>
        )}

        <Select
          options={options}
          value={selectedOption}
          onChange={handleChange}
          isDisabled={disabled}
          isMulti={isMulti}
          placeholder={placeholder}
          isClearable={isClearable}
          className={`react-select-container ${hasError ? "is-invalid" : ""}`}
          classNamePrefix="react-select"
          menuPosition="fixed" // This ensures the menu is positioned relative to the viewport
          menuPlacement="auto" // Automatically chooses between "top" and "bottom"
          menuShouldScrollIntoView={true} // Ensures options are visible when scrolling
          menuPortalTarget={typeof document !== "undefined" ? document.body : undefined} // Renders menu in a portal
          styles={{
            control: (base, state) => ({
              ...base,
              borderColor: hasError
                ? "#dc3545"
                : state.isFocused
                ? "#80bdff"
                : "#ced4da",
              boxShadow:
                state.isFocused && !hasError
                  ? "0 0 0 0.2rem rgba(0, 123, 255, 0.25)"
                  : "none",
              paddingLeft: IconComponent ? "40px" : "12px",
              backgroundColor: hasError ? "#fff8f8" : "white",
              minHeight: "calc(1.5em + 1rem + 2px)",
              "&:hover": {
                borderColor: hasError ? "#dc3545" : "#ced4da",
              },
            }),
            menu: (base) => ({
              ...base,
              zIndex: 9999,
              position: "absolute", // Changed to absolute since we're using a portal
            }),
            menuPortal: (base) => ({ ...base, zIndex: 9999 }), // Ensure portal has high z-index
            ...menuPortalStyles,
          }}
        />
      </div>

      {hasError && (
        <Form.Text className="text-danger small mt-1 d-block">
          {errorMessage}
        </Form.Text>
      )}
    </Form.Group>
  );
};

export default InputSelect;
